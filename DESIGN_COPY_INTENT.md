# 連続コピーの収集条件（コピー意図の判定）検討メモ

> **状態: 実装済み・実機確認待ち。**
> 「操作した直後」を既定として実装し、自動テストとビルドは成立している。
> §9、特に末尾で挙げる 3 項目は未確認。

「Ctrl+C 以外の方法でコピーしたものも、連続コピーで拾えるようにしたい」の検討。

**結論を先に書く。** 収集条件を「Ctrl+C の直後」から
**「利用者が操作した直後」**へ広げる。マウスの低レベルフックを 1 つ足して、
既存の *コピー意図* の仕組みへ入口を増やすだけで済む。
併せて、旧来の「Ctrl+C のみ」と、判定を捨てる「すべて」を設定で選べるようにする。

---

## 1. 何を決めるのか

`SequentialCopyPasteSession` は、収集フェーズのあいだ
`WM_CLIPBOARDUPDATE` を購読している。**通知そのものは今も全部届いている。**
そのうちどれを 1 件として採るかを絞っているだけで、
**この線引きをどこに引くか**が今回の論点になる。

```
すべての WM_CLIPBOARDUPDATE
  ├─ Ctrl+C の直後            ← 選択可能な旧来の条件
  ├─ 利用者が操作した直後      ← 現在の既定
  └─ 全部                      ← 案 C
```

---

## 2. 現状の仕組み

キーボードの低レベルフックで Ctrl+C の押下を捕まえ、**意図の印**を立てる。

```csharp
private void ArmCopyIntent()
{
    // すでにキューへ入っていた古い WM_CLIPBOARDUPDATE を誤って拾わないよう、
    // Ctrl+C を押した時点のシーケンス番号も覚える。
    _copyIntentSequenceNumber = GetClipboardSequenceNumber();
    Interlocked.Exchange(
        ref _copyIntentExpiresAt,
        Environment.TickCount64 + (long)CopyIntentLifetime.TotalMilliseconds);
}
```

`WndProc` 側はこれを**消費**してから収集する。

```csharp
if (!TryConsumeCopyIntent())
{
    return IntPtr.Zero;
}
```

条件は二重になっている。

| 条件 | 何を防いでいるか |
| --- | --- |
| 2 秒の有効期限 | ずっと前の Ctrl+C を根拠に、無関係な更新を拾うこと |
| シーケンス番号が変わったこと | Ctrl+C より前に発生してキューに残っていた通知を拾うこと |
| **`Interlocked.Exchange` で 1 回だけ消費** | **1 回のコピーで複数回来る通知を、複数件として拾うこと** |

**3 つ目が地味に効いている。** §5-(a) で述べるとおり、Excel やブラウザは
1 回のコピーでフォーマットを順に載せ、その都度通知を投げることがある。
消費式にしてあるので、2 回目以降は印が無くなっていて自動的に弾かれる。
**この性質は案 B でもそのまま残り、案 C では失われる。** 案の比較で効いてくる。

キーリピート対策（`_copyKeyDown`）も入っていて、長押しで印が立て直されることはない。

---

## 3. Windows は「誰がコピーしたか」を教えてくれない

前提として押さえておく。`WM_CLIPBOARDUPDATE` が伝えるのは
**「中身が変わった」だけ**で、利用者の操作の結果なのか、
裏で動いているアプリの仕業なのかは区別されない。

だから「意図」はこちらで推測するしかない。使える材料は次のとおり。

| 材料 | 取り方 | 分かること |
| --- | --- | --- |
| キー入力 | `WH_KEYBOARD_LL` | **現在使用中。** Ctrl+C 等が押されたか |
| マウス入力 | `WH_MOUSE_LL` | クリックされたか（**案 B で追加**） |
| シーケンス番号 | `GetClipboardSequenceNumber` | **現在使用中。** 実際に中身が変わったか |
| クリップボードの所有者 | `GetClipboardOwner` → `GetWindowThreadProcessId` | どのプロセスが載せたか |
| 除外フォーマット | `GetDataObject().GetFormats()` | **載せた側が「監視するな」と言っているか**（§7） |

`GetClipboardOwner` は「自分が載せたものを弾く」用途に使えそうに見えるが、
**クリップボードを開く必要がある経路が混ざる**うえ、遅延レンダリング中は
所有者が生きていないこともある。§8-3 のシーケンス番号方式のほうが軽くて確実なので、
今回は使わない。

---

## 4. 今、取りこぼしているもの

Ctrl+C 限定であることによる取りこぼしの実例。

| 場面 | 実際の操作 |
| --- | --- |
| ブラウザ | 右クリック →「コピー」「リンクのアドレスをコピー」 |
| Excel / Word | 右クリック → コピー、リボンのコピーボタン |
| Web アプリ | ページ内のコピーボタン（コードブロック、API キー、注文番号） |
| パスワード管理ツール | 一覧のコピーボタン |
| エクスプローラー | 右クリック →「パスのコピー」 |
| ターミナル | `Ctrl+Insert`、`Ctrl+Shift+C`、**選択しただけでコピーされるもの** |
| 各種アプリ | 独自ショートカット（`Ctrl+Shift+C` など） |

**連続コピーを使いたい場面は、ブラウザや業務アプリから値を拾い集めるときが多い。**
そこで右クリック → コピーが拾えないのは、体感としてかなり効く。

---

## 5. 線を緩めたときに混ざってくるもの

実害の大きい順に 4 つ。

### (a) 1 回のコピーで通知が複数回来る

Excel やブラウザは、テキスト・HTML・画像…と形式を順に載せることがあり、
その都度 `WM_CLIPBOARDUPDATE` が飛ぶ。**1 回のコピーが 2〜3 件として入り、
以降の順番が全部ずれる。** 連続コピーにとっては最も痛い壊れ方。

**案 B ではこれは起きない**（§2 の消費式がそのまま効く）。
**案 C では別途デバウンスが要る**（§8-4）。ここが両案の実質的な差になる。

### (b) 自アプリの定型文コピー

収集中にトレイメニューから定型文をコピーすると、それ自身が収集される。
**現状は Ctrl+C を伴わないので偶然弾けている。**
線を緩めるなら、**案 B / C を問わず塞ぐ必要がある**（§8-3）。

案 B ではとくに重要で、**トレイメニューのクリックそのものがマウス操作**なので、
対策なしでは確実に混ざる。

### (c) 常駐ツールの自動書き込み

- パスワード管理ツールの自動クリア（一定時間後に空にする）
- 他のクリップボード履歴ツールの書き戻し
- リモートデスクトップ / 仮想マシンのクリップボード同期

**利用者が何も触っていないタイミングで飛ぶ。**
案 B はここを弾ける。案 C は素通りする。**環境依存なので、
どれだけ実害があるかは実際に使ってみないと分からない。**

### (d) 画像・ファイルのコピー

`ClipboardService.GetText()` が空を返すので、現状のまま弾かれる。
「文字列をコピーしてください」のトーストが出るだけで、実害はない。

### この機能は「セッション中だけ」であることをどこまで根拠にできるか

**混入の話をするときは、この前提を毎回確認したほうがよい。**
連続コピーは常駐監視ではない。利用者がトレイメニューから明示的に開始し、
10 分で自動終了し、集めた値はメモリ上にしか無く、貼り終われば消える。
**実際の 1 セッションは数十秒から数分**で終わる。

| 懸念 | 「セッション中だけ」で弱まるか |
| --- | --- |
| (c) 常駐ツールの自動書き込み | **弱まる。** 数分の窓に自動クリアや同期が挟まる確率は低い |
| (a) 多重通知 | 弱まらない。コピーのたびに必ず起きる |
| (b) 自アプリの定型文コピー | 弱まらない。セッション中にこそ起きる |
| マウスフックの負荷 | **弱まらない。** 10 分間 PC 全体が重いのは避けたい |
| §7 のプライバシー | §7-1 で扱う |

**したがって案 C のリスクも、単純に「全部拾う」と書いたときの印象より小さい。**
それでも案 B を採るのは、(a) の多重通知対策が既存の仕組みでただで付いてくるからで、
安全性の差が理由ではない（§6-C、§11）。

---

## 6. 三つの案

| | A. キーを増やす | **B. 操作直後まで** | C. 判定を捨てる |
| --- | --- | --- | --- |
| 右クリック → コピー | ✗ | ✓ | ✓ |
| アプリのコピーボタン | ✗ | ✓ | ✓ |
| 選択即コピー（ターミナル） | ✗ | ✓ | ✓ |
| 操作を伴わないコピー | ✗ | ✗ | ✓ |
| (a) 多重通知 | 起きない | **起きない** | デバウンスが要る |
| (b) 自アプリ | 起きない | 対策が要る | 対策が要る |
| (c) 常駐ツール | 起きない | ほぼ弾ける | **素通り** |
| 実装量 | 数行 | マウスフック + α | α のみ |

### A. コピー系のキーを増やす

`Ctrl+Insert` / `Ctrl+X` / `Shift+Delete` を `ArmCopyIntent` の対象に足す。
`VkC` の判定に仮想キーを 2 つ増やすだけで、リスクはほぼ無い。

**ただし §4 の表の大半が拾えないまま。**
一番欲しい「右クリック → コピー」に届かないので、これ単独では目的を果たさない。
**案 B に含める形で入れる**（`Ctrl+Insert` はキー由来のほうが確実に拾える）。

### B. 「利用者が操作した直後」まで広げる ← 採用

`WH_MOUSE_LL` を足し、**ボタンの押下と離上**で `ArmCopyIntent()` を呼ぶ。

- **押下**: 右クリックメニューの「コピー」、ツールバーのコピーボタン
- **離上**: ドラッグ選択で確定するもの（**選択即コピーのターミナル**）

**移動とホイールは対象にしない。** 移動で印を立てると、
マウスを動かしているあいだ常に印が立っていることになり、実質 C と同じになる。

弱点は 2 つ。**操作直後にたまたま常駐ツールが書き込むと 1 件混ざる**ことと、
**トレイメニュー操作自体がマウス操作**であること（§5-(b)）。
後者は §8-3 の対策で塞ぐ。

### C. 判定を捨てる

`TryConsumeCopyIntent()` の呼び出しを外し、全更新を収集対象にする。
取りこぼしはゼロになるが、§5-(a) のデバウンスと §5-(c) の素通りを引き受けることになる。

**実装量は B より少ない**（マウスフックが要らない）。
**B を作れば C は分岐 1 つで足りる**ので、設定として両方持つ追加コストは小さい。

---

## 7. プライバシー: 拾ってはいけない中身がある

**線を緩める以上、これは避けて通れない。**

`DESIGN_RICH_COPY.md` §6-7 と同じ話が、ここでも成立する。
パスワード管理ソフトは「これを履歴に残すな」という印をクリップボードに載せる。

| 形式名 | 意味 |
| --- | --- |
| `ExcludeClipboardContentFromMonitorProcessing` | 監視ソフトは触るな |
| `CanIncludeInClipboardHistory` | DWORD が 0 なら履歴に残すな |
| `CanUploadToCloudClipboard` | DWORD が 0 ならクラウドへ上げるな |
| `Clipboard Viewer Ignore` | 古い時代からの同じ意味の慣習 |

**現状は Ctrl+C 限定なので、この問題は実質的に発生していない。**
利用者が自分でパスワードを選択して Ctrl+C したなら、それは明示的な意図だからだ。

**線を緩めた瞬間に話が変わる。** パスワード管理ツールの**コピーボタン**が
収集対象になり、パスワードが連続コピーの一覧に載って、トーストにも表示され、
のちに意図しない場所へ貼られうる。

**したがって、案 B / C では除外形式の判定を必ず入れる。**
1 つでも載っていたら収集せず、`captureRejected` 相当の通知を出す。
**判定は空文字判定より先**に行う（中身を読まずに帰る）。

> なお `DESIGN_RICH_COPY.md` §9-7 に「パスワード管理ソフトで実際に載るか」の
> 確認項目がある。**同じ確認で両方の判断がつく。**

### 7-1. 採らなかった案: 収集はして、表示だけ伏せる

**この節は一度議論して、結論として上の「収集を断る」を維持した。**
戻ってくる可能性のある判断なので、退けた案とその理由を残す。

退けた案はこうだった。

- 連続コピーは `DESIGN_RICH_COPY.md` のスナップショットと違い、**ディスクに保存しない**。
  メモリ上の一時セッションで、寿命はクリップボードそのものに近い
- 収集を断ると、**ID とパスワードを順に集めてログイン画面へ順番に貼る**という、
  連続コピーの中心的な使い道が塞がれる
- 現状の Ctrl+C 限定でも、詳細画面で選択して Ctrl+C すれば収集されている。
  案 B で新しく増えるのは「コピーボタン経由」だけ
- 実害は**収集時のトーストに中身が出る**ことに集約される。
  ならば収集はして、トーストとメニューの表示だけ伏せ字にすればよい

**それでも「収集を断る」を採る理由。**

- **伏せ字は、載ってしまったことを隠すだけで、載ったこと自体は変わらない。**
  連続コピーの一覧は、以後の Ctrl+V で順に貼り出される。
  貼り先を間違えたときに出ていくものが平文であることは変わらない
- パスワードを連続コピーで扱いたい場面は、**素直にパスワード管理ソフトの
  自動入力を使うほうが安全**で、このアプリが受け持つべき仕事ではない
- **判定を入れないより、入れて後から緩めるほうが安全側**。
  断る実装があれば、伏せ字案へ移すのはトースト側の分岐だけで済む

**その代わり、断ったことが利用者に伝わる文言にする**（§8-7）。
「追加できません」だけでは、何が起きたのか分からない。

---

## 8. 実装の骨子

### 8-1. 収集条件を設定として持つ

```csharp
// Models/SequentialCaptureTrigger.cs
public enum SequentialCaptureTrigger
{
    /// <summary>Ctrl+C など、コピー系のキー操作の直後だけ収集する（従来の挙動）。</summary>
    CopyKey,

    /// <summary>キーまたはマウスの操作の直後に起きた更新を収集する。</summary>
    UserInput,

    /// <summary>クリップボードの更新をすべて収集する。</summary>
    Any,
}
```

```csharp
// Models/AppSettings.cs
/// <summary>連続コピーが 1 件として収集する範囲。</summary>
public SequentialCaptureTrigger SequentialCaptureTrigger { get; set; }
    = SequentialCaptureTrigger.UserInput;
```

**ここは既存の設計方針から意図的に外れる点なので、記録しておく。**
`DESIGN_RICH_COPY.md` §7-1 では「追加するプロパティの既定値が
全部『従来どおり』に一致する」ことを利点として挙げていた。
**今回は既定値を `UserInput` にするので、既存の設定ファイルでも挙動が変わる。**

- `Version` を上げる必要はない。項目が無ければ既定値が入るだけで、読み込みは壊れない
- 従来の挙動に戻したい利用者は `CopyKey` を選べる
- **変える価値のほうが大きい**と判断する。`CopyKey` のままでは、
  §4 の取りこぼしに気付かないまま「このアプリの連続コピーは使えない」で終わる

### 8-2. マウスフック

```csharp
private const int WhMouseLl = 14;
private const int WmMouseMove = 0x0200;
private const uint LlmhfInjected = 0x00000001;
```

**フックプロシージャは、まず `wParam` だけを見て早く帰る。**

```csharp
private IntPtr MouseHookProc(int code, IntPtr wParam, IntPtr lParam)
{
    if (code < 0 || _disposed || wParam.ToInt32() == WmMouseMove)
    {
        // 移動は毎秒数百回来る。ここで構造体を読むと入力全体が重くなる。
        return CallNextHookEx(_mouseHook, code, wParam, lParam);
    }
    …
}
```

**ここが今回いちばんの要注意点。** 低レベルフックは、
**システム全体の入力がこのアプリのメッセージループを経由する**ことを意味する。
キーボードフックは既に張っているが、マウス移動は頻度が桁違いに多い。
`Marshal.PtrToStructure` を移動でも呼ぶような書き方をすると、
**PC 全体のマウスが重くなる。**

さらに、フックの応答が `LowLevelHooksTimeout`（既定 300 ms）を超えると
**Windows がフックを黙って外す。** 外れても通知は来ないので、
「しばらく使っていたら右クリックコピーを拾わなくなった」という形で表面化する。
フック内で重い処理をしないこと。

対象にするメッセージ:

| メッセージ | 拾いたいもの |
| --- | --- |
| `WM_LBUTTONDOWN` / `WM_LBUTTONUP` | メニュー項目の選択、コピーボタン、ドラッグ選択の確定 |
| `WM_RBUTTONDOWN` / `WM_RBUTTONUP` | コンテキストメニューを開く操作そのもの |
| `WM_MBUTTONDOWN` / `WM_XBUTTONDOWN` | アプリ独自の割り当て |

`(flags & LLMHF_INJECTED) != 0` は既存のキーボード側と同じく除外する。
**自動化ツールが動かしたマウスを利用者の意図と見なさない。**

なお、`SequentialCopyPasteSession` はセッション中しかフックを張らない。
**常駐している間ずっとマウスを監視するわけではない**ので、
既存の「必要なときだけ監視する」という姿勢は保たれる。

### 8-3. 自アプリの書き込みを除外する（B / C 共通・必須）

`ClipboardService` に「最後に自分が書き込んだ直後のシーケンス番号」を持たせる。

```csharp
// Services/ClipboardService.cs
private static uint _lastSelfWriteSequence;
private static long _lastSelfWriteAt;

/// <summary>直前のクリップボード更新が、このアプリ自身の書き込みかどうか。</summary>
public static bool IsSelfWrite(uint sequenceNumber)
    => (sequenceNumber != 0 && sequenceNumber == _lastSelfWriteSequence)
        || Environment.TickCount64 - _lastSelfWriteAt < SelfWriteGraceMs;
```

`TryCopy` が成功した直後に記録する。`SetDataObject` は同期的に更新するので、
戻った時点で新しい番号が取れる。ただし `copy: true` のレンダリングで
追加の更新が起きる余地があるため、**時間の猶予（500 ms 程度）も併用**する。

セッション側は、収集の前にこれを見て自分の書き込みなら帰る。

> 貼り付けフェーズでは既に `BeginPasting()` が `StopListening()` を呼んでいるので、
> **自分が貼り付けのために書いた内容は元から収集されない。**
> ここで塞ぎたいのは**収集中に定型文コピーを使った場合**。

### 8-4. デバウンス（C のときだけ必要）

`Any` では消費式の印が無いので、多重通知がそのまま複数件になる。

- 直前に収集した時刻を覚え、**300 ms 以内の更新は無視する**
- **後から来たほうを採用しない。** プレーンテキストは先に載ることが多く、
  あとの通知ほど内容が完全とは限らない。最初の 1 件を採る
- 人が意図的に 300 ms 以内へ 2 回コピーすることはまず無いので、
  取りこぼしにはならない

幅の根拠は §9-1 の実測で決める。

### 8-5. 触るファイル

| ファイル | 変更 |
| --- | --- |
| `Models/SequentialCaptureTrigger.cs` | 新規。enum |
| `Models/AppSettings.cs` | プロパティ 1 つ |
| `Services/ClipboardService.cs` | 自アプリ書き込みの記録と判定 |
| `Services/SequentialCopyPasteSession.cs` | マウスフック、条件分岐、除外形式の判定、デバウンス |
| `TrayIconManager.cs` | 設定をセッションへ渡す。開始トーストとメニューの文言 |
| `ViewModels/SettingsViewModel.cs` | プロパティと選択肢 |
| `SettingsWindow.xaml` | 設定 UI |

### 8-6. 設定 UI の置き場所

フッターの `ActionSettingsPopup` と同じ形で、**小さな `Popup` を 1 つ足す**のが素直。
チェックボックス 1 個で済む話ではなく 3 択で、しかも説明が要るため、
フッターに直接並べると窮屈になる。

```
[ ] コピーしたときに通知を表示する   [ホットキーの設定] [アクションの表示] [連続コピー] [スプリントの設定]
```

ポップアップの中身はラジオボタン 3 つと、それぞれ 1 行の説明。

```
連続コピーで 1 件として集める操作

( ) Ctrl+C を押したときだけ
    最も確実です。右クリックの「コピー」やアプリのコピーボタンは集まりません。

(•) キーやマウスを操作した直後のコピー          ← 既定
    右クリックの「コピー」やコピーボタンも集まります。
    裏で動くアプリが自動で書き換えた内容は集めません。

( ) クリップボードが変わったときは常に
    取りこぼしがない代わりに、他のアプリが自動で書き込んだ内容も集まります。
```

### 8-7. 文言の見直し

現在の案内は Ctrl+C 前提で書かれているので、既定を変えるなら合わせる。

| 場所 | 現在 | 変更後（`CopyKey` 以外） |
| --- | --- | --- |
| 開始トースト | 「A でデータを順番に Ctrl+C し…」 | 「A でデータを順番にコピーし…」 |
| メニューのツールチップ | 「A でデータを順番にコピーしてください」 | そのままでよい |

`CopyKey` を選んでいるときは従来の文言のままにする。

**§7 で断ったときの文言を分ける。** 既存の `captureRejected` は
「文字列をコピーしてください。空またはテキスト以外の内容は追加されません」で、
除外形式に当たった場合にこれを出すと**理由が伝わらない**。
利用者から見れば「文字列をコピーしたのに追加されない」ことになる。

```
連続コピーに追加できません
コピー元が、他のアプリでの保存を許可していません（パスワード管理ソフトなど）
```

`captureRejected` に理由を渡す形にする。`ClipboardCaptureRejection` が
`FailedPattern` で理由を区別しているのと同じ作りにすれば揃う。

---

## 9. 実機で確認すること

上から順に、確認できないと判断が固まらない順。

1. **Excel で右クリック → コピーしたとき、`WM_CLIPBOARDUPDATE` が何回飛ぶか。**
   §8-4 のデバウンス幅の根拠になる。ブラウザ、Word、メモ帳でも測る
2. **Web ページのコピーボタン（Clipboard API 経由）で、クリックから更新までどれだけ遅れるか。**
   2 秒の窓で足りるか。非同期の書き込みなので、ここが一番怪しい
3. **収集中にトレイメニューから定型文をコピーして、§8-3 の除外が効くか。**
   案 B では必ず踏む経路
4. **パスワード管理ソフトのコピーボタンで、§7 の除外形式が実際に載るか。**
   `DESIGN_RICH_COPY.md` §9-7 と同じ確認
5. **マウスを動かすだけ・ホイールを回すだけで印が立たないこと**（§8-2）
6. **マウスフックを張ったまま普通に PC を操作して、入力が重くならないこと。**
   併せて、長時間使ってフックが外れないこと（`LowLevelHooksTimeout`）
7. リモートデスクトップ / 仮想マシンのクリップボード同期が、案 B で弾けるか（§5-(c)）
8. ターミナル（PuTTY / Windows Terminal）の選択即コピーが、離上の印で拾えるか
9. 古い設定ファイル（この項目が無い JSON）を読んで、既定が `UserInput` になること

---

## 10. 誤収集からの回復

線を緩める以上、**混ざることを前提に回復手段を見ておく。**

現状あるのは「最後のコピーを取り消す(&U)」で、末尾 1 件を消す。
混入の典型は「余計なものが 1 件入った」なので、
**気付いた直後なら現状の機能で足りる。**

足りないのは、**混入に気付かないまま次のコピーへ進んだ場合**。
`コピー → 混入 → コピー` の並びになると、末尾を消しても直せない。

**今回の範囲外とするが、記録しておく。** 必要になったら、
実行中セッションのメニューに収集済みの一覧を出し、個別に削除できるようにする。
`AddActiveSessionItems` に項目を並べるだけなので、追加は難しくない。
**まず案 B で運用してみて、実際に混入がどれくらい起きるかを見てから判断する。**

---

## 11. 判断

- **案 B を採用し、設定で A（`CopyKey`）と C（`Any`）も選べるようにする。**
  三者は同じ判定関数の分岐で、B を作れば他は数行で付いてくる
- **既定は `UserInput` にする。** 既存の設定ファイルでも挙動が変わるが、
  取りこぼしの実害のほうが大きい。従来の挙動は `CopyKey` として残す
- **案 B の利点は、既存の「消費式の印」がそのまま使えること。** §5-(a) の
  多重通知対策がただで付いてくる。案 C だけがデバウンスを必要とする
- **難所は 2 つ。** マウスフックの軽さ（§8-2、移動で構造体を読まない）と、
  自アプリの書き込み除外（§8-3、案 B では確実に踏む）
- **§7 の除外形式は最初から入れる。** 線を緩めることは、
  パスワード管理ツールのコピーボタンを収集対象に含めることを意味する。
  あとから足す種類のものではない。
  **「収集して表示だけ伏せる」案は一度検討して退けた**（§7-1）。
  緩めるほうへは後から動けるが、逆は動きにくい
- **案 C を採らないのは安全性が理由ではない。** §5 末尾のとおり、
  セッションが短いので (c) のリスクは小さい。
  B を採るのは (a) の多重通知対策がただで付いてくるからで、この区別は残しておく
- **常時監視にはしない。** フックもクリップボード購読も、
  セッション中だけ張るという既存の姿勢は変えない

### 削るとしたらどこから

**下から削る。**

```
8-6. 設定 UI          ← 既定値だけ決め打ちにすれば、まずは動く
C.   Any の分岐       ← B で足りるなら要らない
8-4. デバウンス       ← C を作らないなら不要
A.   Ctrl+Insert 等   ← B のマウス側でだいたい拾える
--- ここから下は削らない ---
B.   マウスフック     ← 目的そのもの
8-3. 自アプリ除外     ← 案 B では確実に混ざる
7.   除外形式の判定   ← 安全に関わる。機能を削っても、これは削らない
```

---

## 12. 実装の記録

**一通り実装した。** 設計から変えた点と、書いてある以上に決めたことを残す。

### 12-1. 設計どおりに入れたもの

| | 場所 |
| --- | --- |
| `SequentialCaptureTrigger`（3 択、既定 `UserInput`） | `Models/SequentialCaptureTrigger.cs` |
| 設定への追加 | `AppSettings.SequentialCaptureTrigger` |
| マウスフック（移動は `wParam` だけ見て即 return） | `SequentialCopyPasteSession.MouseHookProc` |
| `Any` のデバウンス（300 ms） | `SequentialCopyPasteSession.TryAcceptUpdate` |
| 自アプリの書き込み除外 | `ClipboardService.IsSelfWrite` / `RecordSelfWrite` |
| 除外形式の判定 | `ClipboardService.IsMonitoringExcluded` |
| 断ったときの文言 | `TrayIconManager` の `captureRejected` |
| 設定 UI | フッターの「連続コピーの設定」ポップアップ |

### 12-2. 設計から変えたこと

**除外形式の判定と本文の読み取りを、1 回のクリップボード読み取りにまとめた。**
§7 は「判定は空文字判定より先に行う」としか書いていなかったが、
素直に書くと `GetDataObject()` を 2 回開くことになる。
コピー直後はロックが競り合う時間帯なので、
`ClipboardService.TryReadForCapture` が 1 回開いて
「拒否 / 空 / 文字列」の 3 値を返す形にした。順序の要件は満たしている。

**`CanIncludeInClipboardHistory` と `CanUploadToCloudClipboard` は、値まで見る。**
§7 の表は「1 つでも載っていたら断る」と読める書き方だったが、
この 2 つは DWORD が 0 のときだけ拒否で、1 なら許可を意味する。
載っていることだけを根拠に断ると、明示的に許可しているアプリからコピーできなくなる。

**読み取れなかった場合も、拒否とは見なさない。** 当初は「安全側」として
拒否に倒したが、この 2 つは「載っていること」ではなく「値」に意味があり、
**読めなかったという事実は何も語っていない。**
ここで断ると、許可しているアプリからのコピーが
「保存を許可していません」という誤った理由で落ちる。
本当に拒みたいアプリは、載っているだけで拒否になる
`ExcludeClipboardContentFromMonitorProcessing` も併せて載せる
（パスワード管理ソフトは実際に 3 つとも載せる）ので、守りは薄くならない。

**`Ctrl+Insert` などのコピーキーは、`UserInput` でも有効にした。**
§6-A では「案 B に含める形で入れる」とだけ書いていた。
実装では `IsCopyCombination` を両方の設定で通し、
`UserInput` ではさらに「V 以外のキーが押されたこと」でも印を立てる。
`Ctrl+V` を除いてあるのは、貼り付けの直前に自分で印を立てないため。

**設定 UI はラジオボタン 3 つではなく、コンボボックス + 説明にした。**
§8-6 のスケッチはラジオボタンだったが、
項目の「形式」選択（`ClipFormatOptions`）が既に
コンボボックス + 直下に説明文という形になっている。
同じ画面に 2 つの流儀を混ぜる理由がないので、既存に揃えた。

### 12-3. 実装後のレビューで見つけて直したもの

**印を立て直すたびにシーケンス番号を取り直していた。** これが一番危なかった。
`ArmCopyIntent` は呼ばれるたびに番号を上書きしていたが、
「操作した直後」ではキーもマウスも印を立てるため、
**コピーが済んで通知を待っているあいだに次の入力が来ると、
番号がコピー後の値に置き換わり、その更新が「操作より前のもの」と判定されて落ちる。**
マウスのボタンを離す、キーリピートが始まる、といった普通の操作で踏む。
しかも印は 1 回で消費されるので、黙って 1 件失われる。
**消費していない印が生きているあいだは、期限を延ばすだけにして番号は据え置く**ように直した。

**`AppSettings.Clone()` に追加した項目を写し忘れていた。**
設定画面は `_settings.Clone()` を渡して開くので、
**どの設定を選んでも画面には既定値が出て、保存すると選択が消える。**
`ClipItem.Clone()` について `DESIGN_RICH_COPY.md` §7-2 が
「`Clone()` への追加を忘れないこと」と書いていたのと同じ穴を、
`AppSettings` 側で踏んだ。

**新しいポップアップが Esc の処理から漏れていた。**
`SettingsWindow.xaml.cs` は開いているポップアップを列挙して閉じているが、
そこに加えていなかったため、**このポップアップだけを開いて Esc を押すと
`IsCancel` のキャンセルボタンが反応して設定画面ごと閉じる。**
既存のコメントがまさにこの症状を警告していた。

**貼り付けに移ってもマウスフックを張ったままだった。**
`BeginPasting()` はクリップボードの購読だけ外していた。
`ArmCopyIntent` が段階で弾くので実害は無いが、
「必要な間だけ張る」という方針からは外れるので、ここで外すようにした。

**自アプリ判定の時間猶予が、番号で判定できる場合にも効いていた。**
自分がコピーした直後 500 ms 以内は、番号が違っても自分の書き込みと見なしていた。
**番号で確実に判定できるときは番号だけで決める**ように直した。
猶予は番号を取得できなかったときの保険に戻した。

### 12-4. ついでに直したこと

**「まだ 1 件も集めていないのに Ctrl+V」の通知が、実態と合っていなかった。**
以前は収集失敗と同じ `captureRejected` を呼んでいたため、
何もコピーしていない状態で `Ctrl+V` を押すと
「文字列をコピーしてください。空またはテキスト以外の内容は追加されません」と出ていた。
理由を `SequentialCaptureRejection` で分けたので、
「まだ貼り付けるものがありません」を出せるようになった。

### 12-5. ビルドと自動テスト

`net10.0-windows` のビルドは成立済み。
収集条件に隣接する文字列ロジックと設定の回帰テストも既存テストプロジェクトで実行できる。

### 12-6. §9 の実機確認は未完了

OS の入力フックと実アプリのタイミングは自動テストでは代替できない。**とくに次の 3 つは、
通らなければ設計に戻る必要がある。**

1. §9-2 Web ページのコピーボタンで、2 秒の窓に間に合うか
2. §9-6 マウスフックを張ったまま操作して重くならないか
3. §9-3 収集中にトレイメニューから定型文をコピーして、混ざらないか
