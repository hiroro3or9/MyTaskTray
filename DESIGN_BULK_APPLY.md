# 複数行への一括適用 設計メモ

> **状態: 4-a 実装済み・自動テスト済み、4-b 未実装。**
> 複数件を改行でまとめてコピーする部分は実装済み。
> 「1 件ずつ貼り付け」と複数件サブメニュー、Windows 実機確認は残っている。

対象: `Services/ClipboardMatcher.cs`・`TrayIconManager*.cs`・`Models/ClipItem.cs`。
問い: **「1 行コピー → 変換 → 貼り付け」の繰り返しを、1 回で終わらせられるようにすべきか。**

---

## 0. 前提: 足りないのは文法ではなく「照合の単位」

いま `{match:…}` でできることは、こうである:

```
表示条件:     正規表現   ^(\w+)\t(\w+)$
コピー文字列: {match:2}-{match:1}

クリップボード: hoge01→hoge02        →  hoge02-hoge01
```

これがクリップボードに 3 行あると、こうなる:

```
クリップボード                いま               やりたいこと
  hoge01→hoge02       →  hoge02-hoge01      hoge02-hoge01
  hoge03→hoge04                              hoge04-hoge03
  hoge05→hoge06                              hoge06-hoge05
```

**「1 行しか取れない」の正体は `ClipboardMatcher.Match(item, clipboard)` にある。**
この関数はクリップボード**全体を 1 件**として照合し、`{match:1}` `{match:id}` は
その 1 件から作られる。取り出し方（正規表現・キャプチャ・テンプレート）はもう足りていて、
**足りないのは「何件取り出すか」だけ**である。

### 選別の問い

> それは「**取り出し方**」の話か、「**何件取り出すか**」の話か。

`DESIGN_BASE_SYNTAX.md` §0 で `@` の意味を先に固定したのと同じ考え方。
取り出し方の話なら差し込みの文法で扱い、件数の話なら**照合の単位**で扱う。
今回は後者しかない。**差し込みの文法は 1 文字も足さない。**

### 0-2. 初版（`{rows:}` 案）を破棄した理由

このメモの初版は、クリップボードを行と列に割って
`{rows:雛形}` `{cell:n}` という差し込みを新設する案だった。**破棄する。**

- **入力を列に割る前提が間違っていた。** 実際の入力は既に正規表現で切られている。
  `{cell:n}` は `{match:n}` の劣化版（区切りを推測するぶん当てにならない）でしかない
- **同じことを 2 通り書けるようにするのは負債。**
  利用者は既に `{match:n}` を書いて動かしている。覚え直しが発生する
- **`TemplateEngine` の中核を触ることになる。** 初版では `ValueTransform` の
  `depth == 0` 条件を作り替える必要があり（HTML 形式の項目で雛形まで実体参照になるため）、
  `MaxDepth` と再帰展開の前提にも手が入った

今回の案は **`TemplateEngine.cs` を 1 行も触らない**（末尾の「コードで確認した点」）。
ループが差し込みの**外側**に来るので、エスケープも入れ子の深さも既存のまま効く。

---

## 一覧

| # | 項目 | 内容 | 価値 | コスト | 優先度 |
| --- | --- | --- | --- | --- | --- |
| 1 | `MatchEach()` | 照合を行ごとに回し、結果を並べて返す | 高 | 小 | **高** |
| 2 | `ApplyToEachLine` | 項目ごとの設定。既定オフ | — | 小 | **高（#1 の必須条件）** |
| 3 | 受け取り方のサブメニュー | 「まとめてコピー」／「1 件ずつ貼り付け」 | 高 | 中 | **高** |
| 4 | 一致しない行は飛ばす | 見出し行・空行が自然に落ちる | 中 | 小 | **高（#1 と不可分）** |
| 4-2 | 正規表現は全体へ繰り返し当てる | 1 件が複数行にまたがれる。#8 の見送りを撤回 | 高 | 小 | **高** |
| 5 | タイムアウトを項目単位へ | 行ごと 200 ms だと最悪時間が行数倍になる | — | 小 | **高（#1 の必須条件）** |
| 6 | ツールチップを件数表示へ | 20 行を並べても 200 文字で切れて読めない | 中 | 小 | 中 |
| 7 | 行ごとに違う変換 | 1 行目だけ別の形にする、など | 低 | 高 | 見送り |
| 8 | ~~行をまたぐ照合（`Regex.Matches`）~~ | **見送りを撤回し、#4-2 として採用した** | 高 | 小 | — |

---

## 優先度: 高

### 1. `MatchEach()` — 照合を件ごとに回す

`ClipboardMatcher` に入口を 1 つ足す。**`Match()` 本体は触らない。**

> **この節は初版の設計である。** ここでは「1 件 = 1 行」としているが、
> 正規表現条件については #4-2 で全体への繰り返し照合に差し替えた。
> 下の擬似コードは `MatchEveryLine()`（正規表現以外の条件）に相当する。

```csharp
/// <summary>
/// 表示条件に合う「件」を並べて返す。
/// ApplyToEachLine が false なら、従来どおり全体で 1 件だけ見る。
/// </summary>
public static IReadOnlyList<ClipboardMatchResult> MatchEach(ClipItem item, string clipboard)
{
    if (!item.ApplyToEachLine)
    {
        ClipboardMatchResult single = Match(item, clipboard);
        return single.IsMatch ? [single] : [];
    }

    List<ClipboardMatchResult> results = [];
    foreach (string line in SplitLines(clipboard))     // 空行はここでは捨てない
    {
        ClipboardMatchResult result = Match(item, line);
        if (result.IsMatch) { results.Add(result); }   // #4
        if (results.Count >= MaxLines) { break; }      // #5
    }

    return results;
}
```

`{match:…}` の値の作り方（キャプチャ名・番号・`value`）も、
正規表現の組み立ても、既存の `Match()` がそのまま担当する。

#### なぜ「行ごと」で、`Regex.Matches`（一致箇所すべて）ではないのか

一致箇所を全部拾う方式のほうが一般的に見えるが、**採らない**。

- **正規表現条件でしか使えない。** 行ごとなら、Web URL・日付・数値・メールアドレスといった
  組み込み条件にもそのまま効く。「URL が 20 行あるので全部 `feature/` 名に変換したい」が、
  正規表現を書かずに済む。これは表現力の差ではなく、**書かなくていい量の差**である
- **`^` `$` が直感どおりに効く。** 行に切ってから照合するので、`^(\w+)\t(\w+)$` が
  そのまま書ける。全体を 1 本の文字列として `Regex.Matches` すると
  `RegexOptions.Multiline` の有無を利用者が意識することになる
- **元の作業と 1 対 1 で対応する。** 「1 行コピーして変換して貼る」を繰り返していたのだから、
  1 行が 1 件である

#### 決めておくこと

- **各行は `TrimEnd()` だけ掛ける。** 既存の `Match()` は `clipboard.Trim()` しているが、
  これは「コピーの前後に付いた余分な空白を落とす」ためのもの。
  行の**先頭**まで落とすと、`→hoge01→hoge02`（1 列目が空のタブ区切り）が
  `hoge01→hoge02` に化けて列がずれる。行末の空白と `\r` だけ落とす
- **`\r\n` と `\n` はどちらも行の区切りとして読む**
- **連結は `\n` に統一する**（#3 の「まとめてコピー」）

### 2. `ApplyToEachLine` — なぜ項目ごとの設定が要るのか

「常に行ごと」にはできない。**条件の種類によって、正しい単位が違う**からである。

| 表示条件 | 全体で 1 件 | 行ごと |
| --- | --- | --- |
| JSON | **必須。整形済み JSON は複数行にまたがる** | 壊れる（1 行ずつが JSON として読めない） |
| 日付 | 自然（`{date@clip}` は文中から拾う） | 使える |
| Windowsのパス | 自然 | 使える |
| Web URL | | 使える |
| 数値 | | 使える |
| メールアドレス | | 使える |
| 文字列がある | | 使える |
| 正規表現 | | **本命** |

JSON 1 つで「常に行ごと」は否決される。整形済み JSON をコピーして
「JSONをMinify」を選んだとき、行に切ると条件そのものが成立しない。

`ClipItem` に `bool ApplyToEachLine`（既定 `false`）を足し、設定画面にチェックを 1 つ出す。
**既定オフなので、いまある項目の挙動は一切変わらない。**

#### 「常に表示」の項目にはチェックを出さない

`RebuildMenu()` は「常に表示」の項目に `EmptyCaptures` を渡し、
**クリップボードを読んでいない**:

```csharp
if (item.IsSeparator || item.ClipboardCondition == ClipboardMatchKind.Always)
{
    regular.Add(new MenuEntry(item, EmptyCaptures, _menuContext));
    continue;
}
```

ここでチェックを許すと、クリップボードを読まないまま「行ごと」に回すことになり、
**0 件になって黙って何も起きない**。`DESIGN_BASE_SYNTAX.md` §2 の
`ContainsClipboard()` の穴と同じ形である。

読みに行くよう分岐を足すこともできるが、**そもそも「常に表示」は条件が無い**ので
行に切る根拠が無い。行ごとに回したければ「文字列がある」を選べばよく、
そちらはクリップボードを読む。**チェックを出さないほうを採る。**

### 3. 受け取り方 — 複数行のときだけサブメニューを出す

貼り付け先が連続した行のときと、バラバラのときで欲しいものが違う。両方出す。

```
（項目名）
  ├ まとめてコピー（3 件）        → 改行でつないでクリップボードへ。Ctrl+V 一発
  └ 1 件ずつ貼り付け（3 件）      → Ctrl+V を押すたびに 1 件ずつ入る
```

- **1 行しかコピーしていないときはサブメニューを出さない。** クリックで即コピー、
  いままでどおり。「クリックしたら即コピー」という前提（`DESIGN_BASE_SYNTAX.md` §7 で
  `@ask` を対象外にした理由）を、複数行のときだけ緩める
- **「1 件ずつ貼り付け」は `SequentialCopyPasteSession` の貼り付けフェーズを再利用する。**
  収集フェーズを飛ばす入口が要る:

  ```csharp
  public bool StartPasting(IReadOnlyList<string> items)
  ```

  `Ctrl+V` のフック・進捗表示・自動終了・10 分のタイムアウトは既存のものがそのまま効く
- 作業モードになるので、「作業モード中は競合するコピー項目や別の作業ツールを無効にする」
  という既存の規則もそのまま乗る

#### 決めておくこと

- **形式（`ClipFormat`）は「まとめてコピー」にだけ効く。**
  各行を `TemplateEngine.Expand()` で展開してから `\n` で連結し、
  `ClipboardService.TryCopy(value, item.Format)` へ渡す。
  HTML / Markdown の項目でも、既存の `ValueTransform` が行ごとの展開の中で効く
  （**初版で問題になった二重エスケープの話が、丸ごと消える**）
- 「1 件ずつ貼り付け」では各件がプレーンテキストになる。
  貼り付け先が 1 セルずつなので書式を載せる意味が薄く、
  `SequentialCopyPasteSession` の既存の扱いとも揃う
- **連番（`{seq}`）は項目につき 1 回だけ進める。** 全行で同じ値になる。
  行番号が欲しいという要求が出たら、そのとき別に設計する
  （`{seq}` を行ごとに進めると、設定ファイルへの書き込みが行数ぶん走る）

### 4. 一致しない行は飛ばす

見出し行や空行が自然に落ちる。

- **何件処理したかを通知に出す。** 「3 件をコピーしました」。
  飛ばした結果が行数と合わないので、件数が出ないと気付けない
- **0 件のときはコピーせず知らせる。**
  クリップボードが空のとき・`{date@clip}` が読めないときと同じ扱い
- 飛ばした行数も出すかは実機で決める（「3 件をコピーしました（2 行は対象外）」）

### 4-2. 正規表現は行に切らず、全体へ繰り返し当てる

1 件が複数行にまたがる入力を扱う。

```
表示条件:     正規表現   ^(\w+)[\r\n]+(\w+)$
コピー文字列: {match:1}─{match:2}

  hoge01                hoge01─hoge02
  hoge02        →       hoge03─hoge04
  hoge03
  hoge04
```

**専用の文法を足さない。** パターンに改行を書くだけで、
`{match:1}`・`{match:名前}` は 1 行のときとまったく同じに使える。

#### これは #8「行をまたぐ照合」の見送りを撤回したものである

初版では #1 で「行ごと」を選び、#8（`Regex.Matches` 方式）を見送った。
理由は「行という単位を捨てると組み込み条件が全部使えなくなる」だった。
**この理由は正しいが、結論が間違っていた。** 二者択一ではなかったからである。

行に切ると、正規表現に改行を書けなくなる。
だから「2 行で 1 件」を表す手段が別に要り、`{match:lineN}` と
「雛形に書いた最大番号がかたまりの行数」という規則と、端数の扱いを発明することになった。
**行に切ったせいで生まれた機械**であって、要件から生まれたものではない。

しかもその機械には**塞げない穴**があった。`{match:…}` の `:` の右は
丸ごと 1 つのキーとして辞書を引く（`TryGetNamedValue`）ため、
`{match:line4:名前}` は書けない。**2 行目のキャプチャに手が届かない。**
届かせるには入れ子の名前を許すことになり、
エンジンで最も広く使われている切り出し規則に例外を持ち込む。

全体へ繰り返し当てれば、この機械は丸ごと要らない。
`{match:lineN}`・`GetMatchLineCount()`・`Group()`・`DroppedLines` を削除し、
`TemplateEngine.cs` への追加も撤回した。**足すのではなく減って解決した。**

#### 条件の種類で切り方を変える

見送りの理由そのもの（組み込み条件が使えなくなる）は、いまも有効である。
だから**捨てるのではなく、条件で分ける**。

| 表示条件 | 1 件の単位 | 理由 |
| --- | --- | --- |
| 正規表現 | 一致箇所 | 「いくつ一致するか」を言える |
| Web URL・日付・数値・メール・文字列がある | 1 行 | 「文字列全体がその形か」の判定で、繰り返す先が無い |
| JSON | （複数行では使わない） | #2 |

`Regex.Matches` は前の一致の終わりから続くので、
1〜2 行目・3〜4 行目…という組み分けは**何も指定しなくても成立する**。
端数（最後に 1 行余る）は最後の一致が成立しないだけなので、
数える必要すら無くなった。

#### `Multiline` を自動で付ける

複数行に適用するときだけ `RegexOptions.Multiline` を付け、
`^` と `$` を各行の先頭・末尾にする。

`(?i)` を自分で書く既存の流儀（README）からは外れる。それでも自動にする理由は、
**付けないと既存の項目が黙って壊れる**からである。
1 行用に書いた `^(\w+)\t(\w+)$` の項目でチェックを入れると、
`^`/`$` が文字列全体の先頭・末尾の意味になって一致しなくなり、
**項目がメニューから消える**。消えたことに気付く手がかりが無い。

`(?m)` を書かせる案は「隠れたモードが無く予測しやすい」が、
その予測が要るのは**壊れたあと**である。ここは自動に倒す。

#### `{match:value}` の意味

| 照合のしかた | `{match:value}` |
| --- | --- |
| 1 件として照合（従来） | 照合した文字列全体 |
| 全体へ繰り返し当てる | **その一致箇所**（＝件の単位） |
| 行ごと | その行 |

どれも「その件の元になった文字列」で一貫している。
一致部分だけが欲しい場合は、どの場合も `{match:0}` で取れる。

### 5. タイムアウトを「行ごと」から「項目ごと」へ

`ClipboardMatcher` の `RegexTimeout` は 200 ms で、
**いまは 1 回の照合に対する上限**である。行ごとに回すと、そのまま
**最悪時間が行数倍**になる（200 行なら 40 秒）。メニューが固まる。

しかもこの照合は**メニューを開くたびに走る**。`RebuildMenu()` が
表示判定のために `ClipboardMatcher.Match()` を呼んでいるからである。

**項目 1 つあたりの合計を 200 ms にする。** 途中で超えたらそこで打ち切り、
それまでに集まった件だけを返す。「タイムアウトは一致しなかったものとして扱う」という
既存の方針とも揃う（既存の 1 件の場合の挙動は変わらない）。

併せて**行数の上限**（200 行程度）を置く。超えた分は処理せず、通知で知らせる。

---

## 優先度: 中

### 6. ツールチップを件数表示にする

`BuildToolTip()` は展開結果を `Truncate(expanded, 200)` に通しており、
この `Truncate()` は改行を `⏎` に置き換えて 1 行にしてから 200 文字で切る。

**20 行を展開しても、先頭 2〜3 行分しか見えない。**
「メニューのツールチップには展開後の値が出るので、選ぶ前に結果を確認できる」という
README の約束が、この項目では成り立たない。

チェックの入った項目では、展開結果を並べる代わりに
**件数と先頭 1 件**を出すほうがよい:

```
{match:2}-{match:1}
→ 3 件: hoge02-hoge01 …
```

`BuildToolTip()` に分岐が 1 つ増えるだけ。#1〜#5 を入れて実際に見てから決める。

---

## 見送り

### 7. 行ごとに違う変換

「1 行目は見出しとして別の形にする」「奇数行と偶数行で分ける」など。

**`{if:}` が要る**（`DESIGN_IF.md` で検討中、未実装）。
そちらの結論が出るまで判断できない。

そして、そもそも需要が薄い。**項目を 2 つに分ければ書ける**からである
（`DESIGN_IF.md` §0 の選別の問いがそのまま効く）。

### 8.（撤回）行をまたぐ照合（`Regex.Matches` 方式）

**見送りを撤回した。#4-2 として採用してある。**

見送った理由は「行という単位を捨てると組み込み条件が全部使えなくなる」だった。
理由自体は正しかったが、**捨てるか行に切るかの二者択一だと思い込んでいた**。
条件の種類で分ければ両方成り立つ。経緯は #4-2 に書いた。

---

## 実装順の推奨

```
1. #2  ClipItem.ApplyToEachLine ＋ 設定画面のチェック（「常に表示」では出さない）
2. #5  タイムアウトを項目単位へ ＋ 行数の上限
3. #1  MatchEach()
   #4  一致しない行は飛ばす／件数の通知      （#1 と不可分。同時）
   #4-2 正規表現は全体へ繰り返し当てる       （条件の種類で切り方を分ける）
4. #3  サブメニュー
   4-a  「まとめてコピー」                    これだけで実用になる
   4-b  「1 件ずつ貼り付け」                  StartPasting() の追加が要る
5. #6  ツールチップ                           実際に見てから
```

#5 は複数行への適用を入れるかどうかに関わらず意味のある整理で、**単独でリリースできる**。

4-a と 4-b は分けてよい。4-a だけでも「20 回の往復が 1 回になる」という
いちばん大きい価値は出る。4-b は `SequentialCopyPasteSession` に手が入るので、
4-a を使ってみて「貼り付け先がバラバラで困る」が実際に出てから入れてもよい。

---

## 実装後のまとめ（4-a まで）

`#2 → #5 → #1・#4・#4-2 → 4-a` を実装した。触ったのは次の 6 ファイル。

| ファイル | 変更 |
| --- | --- |
| `Models/ClipItem.cs` | `ApplyToEachLine` / `CanApplyToEachLine` / `UsesEachLine` を追加 |
| `Services/ClipboardMatcher.cs` | `ClipboardMatchRows`・`MatchEach()`・`MatchEveryRegex()`・`MatchEveryLine()`・`SplitLines()` を追加。`Match()` を `MatchValue()` に、キャプチャの組み立てを `BuildRegexCaptures()` に切り出し |
| `TrayIconManager.cs` | `MenuEntry` が件の並びと説明文を持つように。`CopyToClipboard()` が件ごとに展開して改行連結。`DescribeBulk()` を追加 |
| `ViewModels/SettingsViewModel.cs` | プレビューと条件の説明を `MatchEach()` へ |
| `SettingsWindow.xaml` | チェックと「複数行」バッジ |
| `README.md` | 「複数行にまとめて適用する」の節と設定ファイルの項目 |

**`Services/TemplateEngine.cs` は 1 行も触っていない**（§0-2 の見込みどおり）。
一度は `GetMatchLineCount()` を足したが、#4-2 の設計を入れ替えたときに撤回した。

### 設計から変えた点: サブメニューを出さない

§3 では「複数行のときだけサブメニューを出し、
『まとめてコピー』と『1 件ずつ貼り付け』から選ばせる」としていた。
**4-a だけを入れる段階では、サブメニューを出さない。**

選択肢が 1 つしか無いサブメニューは、クリックを 1 回増やすだけで何も選ばせていない。
「クリックしたら即コピー」を保つほうが素直である。

したがっていまの動きはこうなる:

```
チェックを入れた項目をクリック
  → 一致した行の数だけ展開し、改行でつないでコピーする（1 行なら従来と同じ）
```

**サブメニューは 4-b と同時に入れる。** そのとき初めて選択肢が 2 つになる。

### 決めたとおりに実装した点

- 各行は `TrimEnd()` のみ。先頭は落とさない（1 列目が空のタブ区切りがずれないように）
- 一致しない行は飛ばし、通知に「（3 件）」と入った件数を出す
- タイムアウトは項目単位の 200 ms。`Stopwatch` で残りを計り、`MatchRegex()` へ渡す
- 件数の上限は 200。超えたら `Truncated` を立て、ツールチップと設定画面で伝える
- `{seq}` と時刻は 1 回のコピーにつき 1 つ。全行で同じ値になり、連番は 1 つだけ進む
- ツールチップは件数と先頭 1 件（#6 も同時に入れた。全件を並べると読めないため）
- 正規表現条件は全体へ繰り返し当て、それ以外は行ごと（#4-2）
- 「常に表示」ではチェックを出さない。加えて `UsesEachLine` が条件も見るので、
  チェックを入れたあとに条件を戻しても事故にならない

### 実装して分かった点

- **`ContainsClipboard()` の穴は開かなかった。** `{match:…}` の値は
  `PopulateMenu()` が `MenuEntry` に載せて運ぶので、コピー文字列に `{clip}` が無くても届く。
  `DESIGN_BASE_SYNTAX.md` §2 と同じ穴を警戒していたが、
  クリップボードを読む場所が差し込みの外側にあるおかげで再発しなかった
- **`ValueTransform` にも手が要らなかった。** ループが差し込みの外側にあるため、
  各件の展開はどれも `depth == 0` から始まる。初版（`{rows:}`）で作り替えが必要だった箇所が、
  そもそも登場しない
- **`{clip}` と組み合わせると壊れる。** `{clip}` はクリップボード全体を指すので、
  全行がそのまま毎行に入る。文法上は書けてしまうため、README に注意書きを入れた
- 差し替え点は読みどおり `PopulateMenu()` の 1 か所で済んだが、
  そこから `CopyToClipboard()` までの 5 つのメソッドで
  引数の型を `IReadOnlyDictionary<…>` から `IReadOnlyList<IReadOnlyDictionary<…>>` へ広げる必要があった。
  選択肢のプレビュー（`AskChoices` / `ChoicePrompt`）だけは代表 1 件で足りるので、
  `Representative()` を挟んで単数のままにしてある

### 実装して分かった点（#4-2）

- **`MenuEntry.Truncated`（bool）ではなく、完成した説明文を持ち回る形**（`BulkHint`）にした。
  件数・打ち切りといった材料が揃うのは `PopulateMenu()` だけなので、そこで作るのが素直だった
- **キャプチャの組み立てを `BuildRegexCaptures()` に切り出した。**
  1 件として照合する場合と全体へ繰り返し当てる場合で
  `{match:1}` の作り方がずれると、書き方が場所によって変わってしまう
- **`{match:value}` だけは意味が変わる。** 繰り返し当てるときは
  「照合した文字列全体」ではなく「その一致箇所」になる。
  そうしないと全件に全文が入る。`{match:0}` は 3 通りとも一致部分で変わらない
- **削除した機械のほうが多い。** `{match:lineN}` を撤回したことで、
  `GetMatchLineCount()`・`Group()`・`WithLines()`・`DroppedLines`・端数の説明文が消えた。
  差分は増えるどころか減っている
- **`Multiline` だけでは足りなかった。`\r\n` を `\n` へ揃える必要がある。**
  `$` が一致するのは `\n` の直前だけで、`\r` の手前では一致しない。
  Windows のクリップボードは `\r\n` なので、揃えずに当てると
  4 行へ `^(\w+)$` を当てて **1 件しか取れない**（最終行だけが `\r` を持たないため）。
  `Multiline` を自動で付ける判断（#4-2）は、これとセットで初めて意味を持つ。
  実際に手を動かして確かめるまで気付かなかった穴で、
  **「^…$ の項目が黙って消える」を防ぐつもりが、別の理由で黙って消えるところだった**

### 現在の検証状況

`ClipboardMatcher` の複数件照合、改行正規化、`Multiline`、件数上限と、
`ApplyToEachLine` の未保存状態は C# の自動テストで確認済み。ビルドも成立している。
残るのは、トレイメニューと設定画面を通す次の実機確認である。

- 20 行のクリップボードで、通知に「（20 件）」が出ること
- チェックを入れた項目が、1 行だけコピーしているときに従来どおり即コピーすること
- チェックを入れたあと表示条件を「常に表示」へ戻すと、チェックが隠れ、動きも従来へ戻ること
- 設定画面の「コピーして試す」が、プレビューと同じ複数行の結果をコピーすること
- 一覧の「複数行」バッジが、条件を変えたときに追従して消えること
- `^(\w+)[\r\n]+(\w+)$` を書いた項目で、4 行のクリップボードが 2 件になること
- 1 行用に書いた `^…$` の項目にチェックを入れても、
  `Multiline` が自動で付くおかげでメニューから消えないこと（#4-2）
- 5 行を 2 行組で拾ったとき、最後の 1 行が黙って落ちるのが実際に許容できるか
  （最後の一致が成立しないだけなので、件数以外の手がかりは出ない）

---

## 実装時にコードで確認した点

次は設計・実装時に確認した境界と、現在の実装結果。

| 確認したこと | 結果 |
| --- | --- |
| `TemplateEngine` に手を入れずに済むか | **済む。** 展開は `CopyToClipboard()` が `Matches = captures` を渡して呼ぶだけ。件ごとに `captures` を差し替えて `Expand()` を回せばよい |
| `{match:…}` の値はどこで作られるか | `ClipboardMatcher.MatchEach()` が件ごとのキャプチャを返し、`PopulateMenu()` → `MenuEntry` → `ActivateClipItem()` → `CopyToClipboard()` と渡す |
| 「常に表示」の項目でクリップボードは読まれるか | **読まれない。** `EmptyCaptureRows` が渡る（§2 の根拠） |
| 表示判定はいつ走るか | **メニューを開くたび、項目ごとに。** `PopulateMenu()` が `ClipboardMatcher.MatchEach(item, clipboard())` を呼ぶ |
| ツールチップはいつ組み立てられるか | **同じくメニューを開くたび。** `CreateClipMenuItem()` が `ToolTipText` に `BuildToolTip()` の結果を直接入れ、遅延評価しない |
| ツールチップに 20 行が出るか | **出ない。** `Truncate(expanded, 200)` が改行を `⏎` にして 200 文字で切る（§6 の根拠） |
| 貼り付けフェーズだけを使えるか | `SequentialCopyPasteSession` は `Start()`（収集）→ `TryBeginPasting()` の順で、収集を飛ばす入口が無い。`StartPasting(IReadOnlyList<string>)` の追加が要る |
| 設定画面のプレビューはどうなるか | **対応済み。** `SettingsViewModel.Preview.cs` のプレビューと「コピーして試す」はどちらも `MatchEach()` を使う |

### 実機で確認したいこと

- 200 行のクリップボードで、メニューを開く速さが体感で変わらないか（#5 の上限が効いているか）
- 「1 件ずつ貼り付け」で、Excel の下方向の移動（`Ctrl+V` → `↓` → `Ctrl+V`）が
  既存の連続ペーストと同じ感触で回るか
- チェックを入れた項目が、1 行だけコピーしているときに
  **サブメニューを出さずに即コピーする**こと（#3 の前提）
- 設定画面のプレビューが、複数件の結果をどう表示するか
