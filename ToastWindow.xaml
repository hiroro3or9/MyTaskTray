<Window x:Class="MyTaskTray.ToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" Topmost="True" ResizeMode="NoResize"
        SizeToContent="WidthAndHeight" ShowActivated="False"
        FontFamily="Yu Gothic UI" FontSize="12.5"
        Foreground="{DynamicResource Brush.Text}"
        Opacity="0"
        MouseLeftButtonDown="OnClicked">
    <Border Margin="14" Padding="14,11" CornerRadius="8" MaxWidth="380"
            Background="{DynamicResource Brush.Toast.Bg}"
            BorderBrush="{DynamicResource Brush.Toast.Border}"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18" ShadowDepth="3" Opacity="0.3" Color="Black" />
        </Border.Effect>
        <StackPanel Orientation="Horizontal">
            <Border Width="3" CornerRadius="2" Margin="0,1,11,1"
                    Background="{DynamicResource Brush.Accent}" />
            <StackPanel>
                <TextBlock x:Name="TitleText" FontWeight="SemiBold" />
                <TextBlock x:Name="BodyText" Margin="0,3,0,0" MaxWidth="320"
                           TextTrimming="CharacterEllipsis"
                           Foreground="{DynamicResource Brush.Text.Secondary}" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
