# 差し込み `{choice}`（決まった選択肢から選ぶ）設計メモ

対象: `Services/TemplateEngine.cs` の差し込み構文に、
**コピーするその場で、あらかじめ決めておいた選択肢から選ぶ**差し込みを足す。

```
{choice:挨拶:お世話になっております|お疲れ様です}。{choice:部署:営業部|開発部}の田中です。
                → 「お疲れ様です。開発部の田中です。」
```

決まっていること:

- 記法は `{choice:名前:選択肢|選択肢}`（名前付き）
- **将来 `{choices:…}`（複数選択・既定の区切りは `、`）を足す。**
  v1 では実装しないが、記法も UI もそれが入る形にしておく（§1-4、§3-6、§7）

このメモでは残りの論点――**選ばせ方の UI**、**既存パーサとの衝突**、**落とし穴**――を詰める。

サンドボックスに .NET SDK が無いため、実装コストはコード読解による見積もり。
パーサの衝突だけは既存ロジックを Python に移して実際に動かして確認した（§2）。

---

> **現況（このメモは設計時のまま残し、結果を追記する形で更新している）**
>
> | 項目 | 状態 |
> | --- | --- |
> | `{choice:…}`（単一選択） | **実装済み** |
> | `{choices:…}`（複数選択） | **実装済み**。§1-4 と §7 の「v1 では実装しない」は設計時点の記述 |
> | 選択肢の中の差し込み | **実装済み**（§2-5）。当初は不可としていた |
> | `}}` の既存バグ | **修正済み**（§2-4）。当初は「触らない」としていた |
> | `{choices}` の区切り文字の指定 | **未対応**。`、` 固定（§1-4） |
> | 前回の選択を覚える | **未対応**。次の候補（§7） |
> | 実機確認 | §9 のうち 3 件は利用者が再現・修正済み。残りは未確認 |

---

## 0. 前提: これは何の穴を埋めるのか

いまある「値をあとから決める」手段は 3 つで、どれも**値がアプリの外にある**ことを前提にしている。

| いまの手段 | 値はどこから来るか |
| --- | --- |
| `{clip}` | いまコピーしてある文字列 |
| `{input:名前}` | 利用者がこれからコピーしてくる文字列 |
| `{match:名前}` | スマートアクションの正規表現が取り出した値 |

`{choice}` が埋めるのは、**値が「わかっている数個のうちのどれか」である**場合。
この場合、上の 3 つはどれも使えない。`{input:挨拶}` と書いても、
利用者は「お疲れ様です」という文字列をどこかからコピーしてこなければならず、本末転倒になる。

### 項目を分ければいいのでは？

いまはそうするしかない。だが**組み合わせの数だけ項目が要る**。

```
お世話になっております。営業部の田中です。
お疲れ様です。営業部の田中です。
お世話になっております。開発部の田中です。
お疲れ様です。開発部の田中です。
```

2 択 × 2 択で 4 項目。3 か所になれば 8 項目で、メニューが埋まる。
しかも共通部分（「の田中です。」）を直すには 4 項目すべてを直すことになる。
**掛け算で増えるものを足し算に戻す**のが `{choice}` の値打ちで、
選択肢が 1 か所しかないなら項目を 2 つ作ったほうが速い、という判断も同時に成り立つ。

複数選択（`{choices}`）まで見ると、この差はさらに開く。
5 個から任意個を選ぶのは 2⁵ = 32 通りで、項目に展開する道はそもそも無い。

### 選別の問い

> その値は、**項目を書いている時点で候補が列挙できる**か？

Yes なら `{choice}`。No なら `{input:}` か `{clip}`。

---

## 1. 記法

```
{choice:名前:選択肢1|選択肢2[|選択肢3…]}    定義（1 つ選ぶ）
{choice:名前}                            参照（同じ選択を 2 か所以上に差し込む）

{choices:名前:選択肢1|選択肢2|選択肢3}      定義（複数選べる・v2）
{choices:名前}                           参照（v2）
```

`{input:名前}` / `{input:名前:/正規表現/}` と同じ「名前が先、詳細が後」の形に揃える。

```
{choice:担当:田中|佐藤}さんへ引き継ぎました。
以降のご連絡は{choice:担当}さんまでお願いいたします。
```

### 1-1. なぜ無名形式（`{choice:様|御中}`）を採らないか

短く書けるが、**同じ選択を 2 か所に差し込めない**。
上の例のように「同じ担当者名を文中で 2 回使う」は定型文では普通に起きる。
無名だと 2 つの独立した選択になり、「田中さんへ引き継ぎました…佐藤さんまで」という壊れた文が作れてしまう。

両対応（`:` が 1 つなら無名、2 つなら名前付き）も考えたが、
`{choice:敬称}`（参照）と `{choice:様|御中}`（無名の 2 択）を
`:` の数だけで見分けることになり、**書き手の意図と文法が一致しない**。
名前に `|` を入れた人（＝無名のつもりで書いた人）が、
黙って「`様|御中` という名前の未定義の選択への参照」として扱われる。

名前付き一本にすれば、この誤りは「定義していない名前を参照した」として一貫して扱える。

### 1-2. 名前と選択肢の切り分け

**最初の `:` までが名前、残りが選択肢。**

- 名前に `:` は使えない（`{input:}` と同じ制約）
- 名前に `|` も使えない。§1-1 の書き間違いを検出できるようにするため
- 選択肢に `:` は使える（2 つめ以降の `:` は区切りにしない）
- 名前は 1〜80 文字、`{` `}` 改行を含まない、大文字小文字を区別しない（`{input:}` と同じ）

### 1-3. 選択肢の書き方

| 書きたいもの | 書き方 | 備考 |
| --- | --- | --- |
| 区切りの `\|` そのもの | `\\|` | `{replace:}` と同じエスケープ |
| 波かっこ | `{{` `}}` | 既存のエスケープ |
| 空文字 | `{choice:敬称:\|様}` | 「付けない / 様」。**許す**（§5-4） |
| 前後の空白 | 落とさない | 「, 」のような区切り文字を選ばせたい |

選択肢の数は **2 個以上 20 個以下**。
1 個以下は選ばせる意味がないので書き間違いとみなし、20 個超はメニューが破綻する。
どちらも既存の方針どおり**書いたままの文字列を残す**。

### 1-4. 複数選択（`{choices}`）のための余地

**v1 で実装しないが、v1 が決めることで v2 が縛られる。** 先に決めておく。

#### なぜ別のトークン名にするか

`{choice}` に印を足す形（`{choice*:…}` など）は採らない。`InnerRegex()` の
名前部が `[A-Za-z]+` なので記号を足せず、名前の中に印を入れると §1-2 の制約と噛み合わない。

そして何より、**返すものが違う**。`{choice}` は選択肢そのものを返し、
`{choices}` は選ばれた複数を連結した文字列を返す。0 個選択という状態もある。
`ExpandToken()` の `switch` は名前で振り分けているので、
**名前が違えば分岐がそのまま分かれる。** 読み手にとっても一目で違いが分かる。

#### v1 で守ること

| 決めごと | 理由 |
| --- | --- |
| `choices` という名前を**他の用途で使わない** | v2 の予約席 |
| 選択肢の文法（`\|` 区切り・`\\|` エスケープ・空の選択肢可・2〜20 個）を**両者で同一にする** | パーサを共有する。`TryParseChoiceToken()` は複数形フラグを受け取るだけにする |
| `{choice:名前}` の参照形式を**両者で同じ形にする** | `{choices:分類}` も同じく参照できる |
| 選択の結果を持ち回る器を `Dictionary<string, string>` にする | 複数選択も**連結後の 1 つの文字列**として入る。器を変えずに済む |

最後の 1 つが効く。`{choices}` の結果を `IReadOnlyList<string>` で持ち回ると、
`ExpandContext` に 2 種類目の辞書が要る。**連結を展開より前に済ませて
`Dictionary<string, string>` に入れれば、v2 でエンジンの器は 1 行も変わらない。**

#### 区切り文字は v2 で決める（未決）

既定は `、`。それ以外を指定する記法は、**v1 では決めない**。
`{choices:名前:A|B|C}` の中に区切りを書く場所が無く、無理に足すと
選択肢と見分けがつかなくなるためで、候補だけ挙げておく:

| 候補 | 例 | 難点 |
| --- | --- | --- |
| 名前の後ろに付ける | `{choices:分類/, :A\|B}` | `/` が名前に使えなくなる |
| 別トークンで包む | `{join:, \|{choices:分類:A\|B}}` | 冗長。連結済みの文字列は分解できない |
| 書式として最後に置く | `{choices:分類:A\|B::, }` | `::` が見分けにくい |

実際に「`、` 以外が要る」場面が出てから決める。
**既定の `、` だけで v2 を出しても機能として成立する**ので、ここで止めてよい。

---

## 2. 既存パーサとの衝突（確認済み）

`{choice}` は `|` を区切りに使う。`|` は既に `{calc:式|書式}` と
`{replace:元|検索|置換}` が使っているので、衝突しないかを実際に確かめた。

### 2-1. `InnerRegex()` は変更不要

```
^(?<name>[A-Za-z]+)(?:@(?<base>[A-Za-z]+))?(?:(?<sign>[+\-])(?<num>\d+)(?<unit>[A-Za-z]*))*(?::(?<fmt>.*))?$
```

| 中身 | name | fmt |
| --- | --- | --- |
| `choice:敬称:様\|御中` | `choice` | `敬称:様\|御中` |
| `choice:敬称` | `choice` | `敬称` |
| `choices:分類:A\|B\|C` | `choices` | `分類:A\|B\|C` |

`fmt` が `.*` なので全部そのまま入る。**`{choices}` も同じ経路で通る**（名前が `[A-Za-z]+` に収まる）。
`{input:名前:/正規表現/}` が `TryParseInputToken()` で `trimmed` を読み直しているのと同じ形で、
`TryParseChoiceToken()` を足せばよい。**正規表現の変更は要らない。**

### 2-2. `|` の分割は巻き込まない

`SplitCalcFormat()` も `TrySplitTextTransformArguments()` も
波かっこの深さを数えてから `|` を見ているので、入れ子の `{choice}` は割られない。

| 入力 | 結果 |
| --- | --- |
| `{calc:{choice:x:1\|2}*10\|#,##0}` | 式=`{choice:x:1\|2}*10` 書式=`#,##0` |
| `{replace:{choice:x:a\|b}\|a\|A}` | 元=`{choice:x:a\|b}` 検索=`a` 置換=`A` |
| `{replace:{clip}\|{choice:x:a\|b}\|X}` | 3 引数に正しく分割 |

**変更不要。**

### 2-3. `RejectBase` / `RejectOffset` が要る

`{choice@clip:a|b}` は base=`clip`、`{choice+1:a|b}` は sign=`+` として**通ってしまう**。
`{input}` `{match}` と同じく、両方を明示的に弾く。

### 2-4. 落とし穴: `}}` が閉じかっこと読まれない（**既存のバグ**）

`FindClosingBrace()` は `}}` を無条件にエスケープとして読み飛ばす。
このため**入れ子の差し込みがトークンの末尾で終わると、閉じかっこが見つからない**。

| テンプレート | `FindClosingBrace` | |
| --- | --- | --- |
| `{calc:{seq}*1}` | 13（正しい） | 内側の後ろに `*1` がある |
| `{calc:{seq}}` | **-1** | `}}` をエスケープと誤読 |
| `{choice:日付:{date}\|{date+1}}` | **-1** | 同上 |

`{calc:{seq}}` が既に壊れているので `{choice}` が持ち込んだ問題ではないが、
**いまは誰も踏んでいない**。既存の入れ子は `{calc:{seq}*100}` `{calc:ceil({month@sprint-3mo}/3)}`
`{replace:{clip}|a|A}` のように、内側の後ろに必ず何かが続くからである。

`{choice}` は選択肢が末尾に来るので、`{choice:日付:{date}|{date+1}}` と自然に書ける。
**踏みやすさが跳ね上がる。**

#### 直せるか → **直した（当初の見立ては悲観しすぎだった）**

あいまいさ自体は本物である。`}}` が深さ 2 の位置に現れたとき、
「内側を閉じてから外側を閉じる」と「エスケープした `}` 1 文字」は**文法上どちらも成り立つ**。
「閉じられるなら閉じる」という規則を入れると、今度は
`{choice:x:a{{b}}c|d}`（選択肢に `{b}` という文字を書いた場合）が壊れる。

当初はここから「**バックトラック**が要る、中核なので触らない」と結論したが、
**どちらを先に試すかを決めるだけで済んだ。**

```
まずエスケープとして読む（従来どおり）。
それで最後まで閉じられなかったときだけ、閉じ優先で読み直す。
```

読み直しが走るのは**すでに -1（総崩れ）になっている場合に限られる**ので、
いま正しく読めている書き方の結果は 1 つも変わらない。
原理上「壊れていたものが直る」方向にしか動かない。

| テンプレート | 修正前 | 修正後 |
| --- | --- | --- |
| `{calc:{seq}}` | -1 | 11 |
| `{calc:{daysuntil:2026-12-31}}` | -1 | 28 |
| `{replace:{clip}\|a\|{clip}}` | -1 | 24 |
| `{choice:x:a{{b}}c\|d}` | 19 | 19（変わらず） |
| `{choice:x:a}}b\|c}` | 16 | 16（変わらず） |

14 パターンで確認済み。

#### 副作用 → そのまま機能にした

読めるようになったことで、**`{choice:期限:{date}|{date+1}}` も解析が通ってしまう。**
だが選択肢を展開しなければ `{date}` という文字がそのままコピーされる。
「展開されず書いたまま残る」より悪い――**黙って違う文字列を出す**ことになる。

いったん「選択肢に `{` があれば受け付けない」で塞いだあと、
**安全な範囲だけ展開する**ことにした（§2-5）。

### 2-5. 選択肢の中の差し込み（対応済み）

`}}` が直った（§2-4）ことで解析は通るようになった。あとは**展開するかどうか**の判断になる。

#### 可否は「メニューを組み立てる時点で値が決まるか」だけで決まる

```
項目をクリック
  ↓
① 選択メニューを組み立てて選ばせる   ← ここで展開する
  ↓
② {input:} のキャプチャ（クリップボードを 2 分占有）
  ↓
③ CopyToClipboard で本体を展開してコピー
```

①で使えるのは `now`・項目の連番・クリップボード（メニューを開いた時点でキャッシュ済み）・
スプリント設定・スマートアクションの捕捉値。

| 差し込み | 可否 | 理由 |
| --- | --- | --- |
| `{date}` `{time}` `{year}` など | ○ | `now` だけで決まる |
| `{clip}` `{date@clip}` | ○ | 読み取り済み |
| `{match:名前}` | ○ | 判定はメニューを開くときに済んでいる |
| `{calc:…}` `{guid}` `{random}` `{replace:…}` | ○ | 中身が①で決まるなら決まる |
| `{input:名前}` | **✗** | ②は①のあと。①の時点で値が無い（順序を変えない理由は §4-3） |
| `{choice:…}` `{choices:…}` | **✗** | 自己参照。中の選択を先に解決しないとメニューを出せないが、それは選択肢の中にいる |
| `{seq}` | **✗** | 値は出せる。だが `ContainsSequence()` はテンプレート全体を見るので、<br>**その選択肢を選ばなくても連番が進む**。「選ばなかったのに番号が飛んだ」になる |

`{seq}` だけは「①で値が決まらない」からではなく、**外側の副作用と噛み合わない**から弾いている。
弾く判定は `ContainsToken()` をそのまま使える（`ContainsClipboard()` などと同じ仕組み）。

#### 展開は①で 1 回だけ

③で展開し直すと、`{guid}` や `{time}` のように評価のたびに変わるものが
**メニューで見た値と食い違う**。`{guid}` に至っては毎回別の値になる。

そこで①で展開し、以降は**表示にも値にもその文字列だけを使う**。
`JoinChoices()` も展開後の一覧を受け取る形にした。
時刻は選択肢ごとに `DateTime.Now` を読まず、メニュー 1 枚で 1 つに揃える
（`{time:HH:mm:ss}` を並べたときに選択肢どうしで秒がずれないように）。

#### `ValueTransform` は渡さない

ここが一番間違えやすい。展開した値は、このあとコピー時に
「差し込まれた値」として一度エスケープされる（深さ 0 の規則）。
①でも掛けると `&amp;lt;` のように**二重になる**。
HTML 形式の項目で `{clip}` を含む選択肢を選ぶと壊れるので、
`ResolveChoiceOption()` は `ValueTransform` と `Choices` を明示的に外している。

---

## 3. 選ばせ方（UI）

ここが本題。

### 3-1. 案 A: サブメニューに組み合わせを並べる

```
┌────────────────┐
│ 1 名乗り     ▶ │──┬─ お世話に… / 営業部
│ 2 …            │  ├─ お世話に… / 開発部
└────────────────┘  ├─ お疲れ様… / 営業部
                    └─ お疲れ様… / 開発部
```

- **良い点**: 窓が出ない。2 クリックで終わる
- **悪い点**: **組み合わせの数だけ行が並ぶ**。2 択 × 2 択で 4 行、3 択が 3 か所で 27 行
- **悪い点**: `お世話に… / 営業部` という並びだけでは、どちらが「挨拶」でどちらが「部署」か分からない
- **悪い点**: §0 で「掛け算を足し算に戻す」と言ったのに、UI が掛け算のまま
- **致命的**: `{choices}`（複数選択）は組み合わせが 2ⁿ になり、**展開できない**

**採らない。**

### 3-2. 案 B: 選択ウィンドウ（小窓）

```
┌─ 選択 ────────────────────┐
│ 挨拶  ( )お世話に… (•)お疲れ様…│
│ 部署  (•)営業部    ( )開発部   │
│ コピーされる文字列:            │
│ お疲れ様です。営業部の田中です。 │
│        [キャンセル]  [OK]   │
└────────────────────────────┘
```

- **良い点**: 選択肢が何か所あっても破綻しない。名前が見える。**結果を一覧で確認してから確定できる**
- **良い点**: `{choices}` はチェックボックスにするだけで済む
- **悪い点**: **新しい窓を 1 つ作ることになる**。前面化・フォーカスの戻し・
  カーソル位置への配置・DPI 換算を、`QuickAddWindow` から写すとはいえもう一組持つ
- **悪い点**: キーボードで選ぶとき、ラジオボタン群のあいだを Tab で移動することになる

### 3-3. 案 C: 逐次メニュー — **採用**

**ホットキーで出るメニューと同じものを、選択肢ごとに出し直す。**

```
（項目をクリック / 番号キーで選ぶ）
        ↓
┌─ 挨拶 (1/2) ──────┐        ┌─ 部署 (2/2) ──────┐
│ 1　お世話に…      │  ──→   │ 1　営業部          │  ──→  コピー完了
│ 2　お疲れ様です   │        │ 2　開発部          │
│ ───────────────  │        │ ───────────────  │
│    ← 戻る         │        │    ← 戻る         │
└───────────────────┘        └───────────────────┘
```

#### 訂正

**このメモの初版で案 C を「遅いから採らない」と書いたが、これは誤りだった。**

初版の理由は「選ぶ → メニューが閉じる → **トレイをもう一度開く** → 選ぶ」だったが、
`ShowMenuAtCursor()` は**任意の `ContextMenuStrip` をカーソル位置に出せる**。
次のメニューはプログラムから出せるので、トレイを開き直す必要は無い。
前提を確かめずに書いていた。

#### なぜこれが一番良いか

**1. 新しい UI 部品が 1 つも要らない。**
案 B が「`QuickAddWindow` から写す」と言っていた難所は、
`ShowMenuAtCursor()` に**既に実装されて動いている**:

| 難所 | 既存の解決 |
| --- | --- |
| ポップアップが前面に来ない | `MenuHostWindow` を `SetForegroundWindow` してから表示 |
| 閉じたあとフォーカスが戻らない | `RestoreForeground(previousForeground)` |
| タスクバーにボタンが出る | `SetMenuOwner(menu.Handle, _menuHost.Handle)` |
| 閉じ残る | 表示後に `PostMessage(WM_NULL)` |
| キーボードで選べない | `SelectFirstEnabledItem()` + 矢印キー・Enter |

写すのではなく**そのまま呼ぶ**。案 B との差はここが一番大きい。

**2. 番号キーが効く。**
`AssignNumberAccessKeys()` / `EnableNumberKeys()` をそのまま使えば、
選択肢に `1` `2` … が振られる。

```
ホットキー → 3（項目）→ 2（挨拶）→ 1（部署）→ コピー完了
```

**手を動かさず、4 打鍵で終わる。** 案 B のラジオボタン + Tab より速い。
これはこのアプリが既に持っている強みで、案 B はそれを捨てることになる。

**3. 足し算で増える。**
選択肢 n か所 × m 個なら、メニューは n 枚 × m 行。案 A の mⁿ 行に対して n×m 行。

**4. `{choices}`（複数選択）が同じ形で入る。** → §3-6

#### 弱点と手当て

| 弱点 | 手当て |
| --- | --- |
| 結果の文字列を一覧で確認できない | **各選択肢の `ToolTipText` に、それを選んだ場合の完成形を出す。**<br>`BuildToolTip()` が既に同じことをしている。<br>一覧で見るより、選択肢ごとに見えるほうがむしろ良い |
| 前の選択に戻れない | 2 枚目以降に「← 戻る」を置く。1 行足すだけ |
| いま何を選んでいるか分からない | 先頭に無効な見出し項目を置く（`挨拶 (1/2)`）。<br>セッション中のメニューが既に同じ形（`入力待ち: {名前} ({n}/{total})`） |
| メニューの表示位置が選ぶたびに動く | §3-5 |

### 3-4. 実装上の唯一の難所: フォーカスの戻しが連鎖を壊す

`RestoreForeground()` は `Dispatcher.BeginInvoke(DispatcherPriority.Background, …)` で遅延し、
「**前面がまだ `_menuHost` のままなら**、元のウィンドウへ戻す」という条件で動く。

メニューを続けて出すと、この条件が**裏目に出る**:

```
1 枚目が閉じる       → RestoreForegroundOnce が発火、Background 優先度で復帰を予約
2 枚目を表示         → SetForegroundWindow(_menuHost) で前面は _menuHost
予約した復帰が走る   → 前面は _menuHost だ → 元のウィンドウへ戻す
                    → 2 枚目のメニューがキー入力を受け取れなくなる
```

**連鎖の途中では復帰を登録しない**必要がある。
`ShowMenuAtCursor()` に「この表示は連鎖の途中かどうか」を渡し、
`menu.Closed += RestoreForegroundOnce` を**最後の 1 枚でだけ**登録する。

> **【重要な見落とし】この分析は 1 枚足りなかった。**
> 危ないのは連鎖の 2 枚目以降だけではなく、**1 枚目**だった。
> 選択メニューを起動した<strong>トレイメニュー自身</strong>が、
> 閉じるときに同じ復帰を予約する。詳細と対処は §10 の「出た不具合」を参照。

```csharp
private void ShowMenuAtCursor(
    ContextMenuStrip menu,
    bool fromHotKey,
    IntPtr previousForeground = default,   // 連鎖では 1 枚目のものを引き継ぐ
    bool restoreForegroundOnClose = true)  // 連鎖の途中では false
```

既定値を付ければ既存の 2 か所の呼び出しは変えなくてよい。
**ここを見落とすと「2 枚目からキーボードが効かない」という形で出る。**
実機でしか再現しないので、§9 の確認項目に入れてある。

### 3-5. メニューを出す位置

`menu.Show(Cursor.Position)` は**そのときのカーソル位置**に出す。
選択肢をマウスでクリックすると、カーソルはその項目の上にある。
そのまま次を出すと、メニューが 1 枚ごとに右下へずれていく。

- マウスで選ぶ人には「押した場所に次が出る」＝**移動距離ゼロ**で都合がよい
- ただし 3 枚目、4 枚目で画面端に達する（WinForms が自動で反転するが、見た目は落ち着かない）
- キーボードで選ぶ人にはカーソルが動かないので、そもそも起きない

**1 枚目の位置を覚えて、2 枚目以降も同じ位置に出す**ことを勧める。
「メニューがその場で切り替わる」ように見えて、枚数が増えても安定する。
ただし移動距離ゼロの利点は失う。**どちらが良いかは実機で試して決める**（§9-5）。

### 3-6. `{choices}`（複数選択）が同じ形で入るか

入る。**WinForms のメニューはチェック付き項目を作れる。**

```
┌─ 出席者 (1/1) ─ 複数選べます ─┐
│ ✓ 1　田中                     │
│   2　佐藤                     │
│ ✓ 3　鈴木                     │
│ ─────────────────────────── │
│   4　決定                     │
└───────────────────────────────┘
                → 「田中、鈴木」
```

必要なのは 2 つだけ:

1. `ToolStripMenuItem.Checked` を自分で反転する（`CheckOnClick` でもよい）
2. **項目をクリックしてもメニューを閉じない。**
   `Closing` で `e.CloseReason == ToolStripDropDownCloseReason.ItemClicked` のとき `e.Cancel = true`

番号キーは `PerformClick()` を呼ぶ経路なので、**そのままチェックの反転として効く**。
「決定」にも番号が振られるので、`3 → 1 → 3 → 4` のような打鍵で確定できる。

連結は**定義順**（クリック順ではない）。
`{choices:出席者:田中|佐藤|鈴木}` と書いた順に並べる。
クリック順にすると、同じものを選び直しただけで結果の文字列が変わってしまう。

0 個選択のまま「決定」は**許す**。空の選択肢を許す（§5-4）のと同じ理屈で、
「何も付けない」は正当な結果である。ただし選び忘れと区別できるよう、
件数と結果を「決定」のツールチップに出す（見出しに入れられない理由は §10）。

**案 B（窓）でもチェックボックスにするだけなので実現できる。**
つまり複数選択は案 C を選ぶ決め手ではないが、**案 C を選んでも困らない**ことは確認できた。
決め手は §3-3 の 1〜3（新しい部品が要らない・番号キー・足し算）のほう。

### 3-7. 判断

**案 C（逐次メニュー）を採る。**

案 B は最後まで有力で、「結果を一覧で確認してから確定できる」点だけは案 C に無い。
それでも案 C を採るのは、**このアプリの操作が既にメニューに統一されていて、
その上で解決済みの問題（前面化・フォーカス・番号キー）を作り直さずに済む**から。
選択肢ごとのツールチップ（§3-3）で、確認の弱さも実用上は埋まる。

案 B は**捨てずに残す**。実機で「メニューが何枚も出るのはうるさい」と感じたら、
エンジン側（§4-1）は共通なので、UI だけ差し替えられる。

---

## 4. 実装

### 4-1. `Services/TemplateEngine.cs`

`{input:}` の実装をほぼそのまま写せる。

```csharp
internal sealed record ChoiceDefinition(
    string Name,
    IReadOnlyList<string> Options,
    bool AllowMultiple);          // v1 では常に false。{choices} の受け皿

internal static IReadOnlyList<ChoiceDefinition> GetChoiceDefinitions(string template);
private static bool TryParseChoiceToken(string inner, out ChoiceDefinition? definition);
public static bool ContainsChoice(string template);
```

`ExpandToken()` に `case "choice":`（v2 で `case "choices":` を並べる）を足す。
`context.Choices` に選択結果（`名前 → 差し込む文字列`）が無ければ `original` を返す
――設定画面のプレビューやツールチップは選択前に評価されるので、この経路は必ず通る（§5-1）。

`ExpandContext` に `IReadOnlyDictionary<string, string>? Choices` を足す。
**複数選択の結果も連結済みの文字列としてここに入れる**（§1-4）ので、v2 で器は変わらない。

`Expand()` のオーバーロードは既に 5 段ある。**ここで引数の袋を導入する**ことを勧める。
これ以上位置引数を足すと呼び出し側が読めなくなる。

```csharp
public sealed record ExpandValues
{
    public Func<string>? Clipboard { get; init; }
    public SprintSchedule? Sprint { get; init; }
    public IReadOnlyDictionary<string, string>? Inputs { get; init; }
    public IReadOnlyDictionary<string, string>? Matches { get; init; }
    public IReadOnlyDictionary<string, string>? Choices { get; init; }
    public Func<string, string>? ValueTransform { get; init; }
}
```

既存のオーバーロードは残したまま新しい入口を足せば、置き換えは段階的にできる。

#### 定義が食い違う場合

```
{choice:敬称:様|御中}…{choice:敬称:さん|殿}
```

**最初の定義を採り、以降は参照として扱う。**
`{input:}` は同じ名前の正規表現を全部集める（and 条件）が、
選択肢を合併すると「様・御中・さん・殿」の 4 択になり、書き手の意図と違う。
黙って捨てることになるので、設定画面に一行出す（§5-2）。

同じ名前を `{choice}` と `{choices}` の両方で定義した場合も、同じく**最初が勝つ**。

#### 定義の無い参照

`{choice:敬称}` だけ書いて定義が無い場合は**書いたまま残す**。既存の方針どおり。

### 4-2. `TrayIconManager.cs` — 選択の連鎖

新しいファイルは要らない。既存のメニュー生成の隣に置く。

```csharp
/// <summary>選択の連鎖の途中の状態。メニューを 1 枚出すたびに進む。</summary>
private sealed record ChoicePrompt(
    ClipItem Item,
    IReadOnlyList<ChoiceDefinition> Definitions,
    Dictionary<string, string> Selected,
    int Index,
    Point Origin,                 // 1 枚目の位置（§3-5）
    IntPtr PreviousForeground,    // 連鎖の最後で戻す先（§3-4）
    Action<IReadOnlyDictionary<string, string>> Completed);
```

- `menu.Show()` は**その場で戻る**（`ShowDialog` ではない）。
  連鎖はループではなく、項目の `Click` から次の 1 枚を出す形で進める
- 選択肢を選ばずに閉じた場合（Esc・別の場所をクリック）は**中止**。
  `Click` で立てたフラグを `Closed` で見て区別する。トーストは出さない
  （`QuickAddWindow` を Esc で閉じたときと同じ）
- 連鎖の途中では `restoreForegroundOnClose: false`（§3-4）

#### `ActionSessionManager` には登録しない

`{input:}` のキャプチャは 2 分間クリップボードを占有するので登録が要るが、
選択の連鎖は**ドロップダウンが入力を掴んでいるあいだだけ**で、
閉じれば必ず終わる。登録すると `HasActiveSession` のガードが
連鎖のあいだ全部のメニュー項目を無効にしてしまい、噛み合わない。

### 4-3. `ActivateClipItem()` の順序

```csharp
// 既存の HasActiveSession ガードは、この手前のまま動かさない。
// 先に選ばせてから「使用できません」と言うのは筋が悪い

IReadOnlyList<ChoiceDefinition> choices = TemplateEngine.GetChoiceDefinitions(item.Text);
if (choices.Count > 0)
{
    // 選択が先、入力が後。理由は下記
    AskChoices(item, choices, selected => ContinueActivate(item, clipboard, captures, selected));
    return;
}

ContinueActivate(item, clipboard, captures, null);
```

#### なぜ選択が先で入力が後か

`{input:}` のセッションは**クリップボードを 2 分間占有する**（利用者のコピー操作を全部拾う）。
その最中にメニューを何枚も出すと、その間もタイマーが動き続ける。

選択を先に済ませれば、セッションは開始から完了まで中断なく進む。
また**中止した場合、セッションはまだ始まっていない**ので巻き戻す状態が無い。
逆順にすると「収集済みの入力を破棄するか」を決めることになる。

### 4-4. その他

| 場所 | 変更 |
| --- | --- |
| `Models/ClipItem.cs` | `UsesChoices`（`UsesInputs` と同じ形。設定画面の出し分け用） |
| `TrayIconManager.BuildToolTip()` | `inputHint` と同じ形で `選択: 挨拶 → 部署` を足す |
| `README.md` | 「複数回キャプチャ」の隣に 1 節 |

---

## 5. 落とし穴

### 5-1. プレビューに何を出すか（**要判断**）

`SettingsWindow.xaml` のプレビュー欄には
**「プレビュー（実際にコピーされる文字列）」**というラベルが**画面に出ている**。
`{choice}` は選択前なので、素直に作れば `{choice:挨拶:お世話になっております|お疲れ様です}` が書いたまま出る。
`{input:名前}` と同じ挙動で、一貫はしている。

**だが情報を捨てている。** `{input:}` は編集時点で値が原理的に分からないのに対し、
`{choice}` は**候補がテンプレートに書いてある**。エンジンが知っている情報を出さないのは惜しい。

| 案 | プレビュー表示 | ラベルとの整合 |
| --- | --- | --- |
| a. 書いたまま（`{input:}` と同じ） | `{choice:挨拶:お世話になっております\|…}。…` | 合う（が役に立たない） |
| b. 先頭の選択肢で展開 | `お世話になっております。営業部の田中です。` | **食い違う** |
| c. 先頭で展開 + 注記 | 同上 + 注記 | 注記で埋める |

**c を勧める。** b だけだと、画面に出ているラベルが黙って嘘になる。
注記があれば `{seq}` の「次の番号」と同じ「代表値を見せている」枠に収まる。

`{choices}` は**先頭 1 個だけを選んだ場合**とする（0 個だと空になって何も見えない）。

注記の置き場は §5-2 の `ChoiceStatus` と兼ねられる。

#### 「コピーして試す」の扱い

プレビュー欄の隣にある「コピーして試す」ボタン（`OnCopyPreview`）は
プレビューの文字列をそのままクリップボードへ入れる。案 b / c を採ると、
**メニューを出さずに先頭の選択肢でコピーされる**ことになる。

これは許容してよい。ボタンの名前が「試す」であり、
`DESIGN_BASE_SYNTAX.md` が同じボタンについて
「プレビューに展開結果がそのまま出ているため、押す前に気付ける」と判断した前例に揃う。

#### 揃えるべき 3 か所

- `SettingsViewModel.Preview`
- `RebuildPlaceholderSamples()`（差し込み一覧の「現在値」）
- `TrayIconManager.BuildToolTip()`

**規則が 3 か所で同じであること**が要件で、a・b・c のどれを選ぶかは二次的。

### 5-2. 設定画面の警告 —— `ChoiceStatus` を足す

前例がある。`ClipboardConditionStatus` / `AppConditionStatus` が
**「条件の説明 + いまの状態 or 検証エラー」を 1 つの文字列で返す**形になっていて、
正規表現が不正なときはその場でエラー文を出している。同じ形の `ChoiceStatus` を足す。

表示の出し分けも前例どおり `IsSequenceVisible => IsItemEditable && SelectedItem!.UsesSequence`
に倣い、`IsChoiceVisible => IsItemEditable && SelectedItem!.UsesChoices` でよい。

| 状況 | 文言の例 |
| --- | --- |
| 正常 | `2 か所で選びます: 挨拶（2 択） → 部署（2 択）` |
| 選択肢が 1 個以下 / 21 個以上 | `「敬称」の選択肢は 2〜20 個で書いてください。` |
| 同じ名前に定義が 2 つ以上 | `「敬称」の定義が 2 つあります。最初の「様\|御中」を使います。` |
| 定義の無い名前を参照 | `「敬称」に選択肢が書かれていません。` |
| 名前に `\|` が入っている | `名前に \| は使えません。{choice:名前:選択肢\|選択肢} の形で書いてください。` |

いずれも**展開されずに書いたまま残る**ので、放っておいてもコピー結果から気付ける。
ただし気付くのが「貼り付けたあと」になる。前例がある以上、先に言うほうが安い。

### 5-3. HTML 形式との組み合わせ

`ClipboardService.GetValueTransform(item.Format)` は
**HTML のときだけ** `CfHtml.Escape` を返す（Markdown は Markdig が処理するので `null`）。
これが深さ 0 の差し込み結果に掛かるので、`{choice}` が返す選択肢のリテラルも
`{input:}` と同じくエスケープされる。

これは正しい――選択肢は利用者が書いた「値」であって、タグではない。

タグを選ばせたい（`<b>|<i>`）場合はエスケープされて意図どおりにならないが、
`{input:}` も同じ制約なので**新しい問題ではない**。README に一行書けば足りる。

`{choices}` の区切り文字（`、`）は**エスケープの対象に含めない**。
連結はエンジンが行うもので、利用者が入れた値ではない。
――ただし `{choices}` を HTML で使ったとき、区切りだけ生で、
選択肢だけエスケープされる形になる。`、` に限れば実害は無いが、
区切りを指定できるようにする（§1-4）ときに再確認すること。

### 5-4. 空の選択肢を許す

`{choice:敬称:|様}` は「付けない / 様」。**許す。**

「付けるか付けないか」は 2 択として自然で、これを禁じると
`{choice:敬称:なし|様}` と書いて `{replace:…|なし|}` で消す、のような迂回が生まれる。

メニューでは `(空)` と表示する。空文字のままだと**押せる行が見えない**。
**先頭が空文字の場合も、番号は普通に振る。**

### 5-5. メニューが連鎖する経路すべて

`{choice}` を含む項目は、次のどの経路からも起動しうる。全部で連鎖が成立する必要がある。

| 経路 | 注意点 |
| --- | --- |
| トレイを右クリック → 項目 | `ShowContextMenuMethod`（内部処理）で 1 枚目が出る。<br>**2 枚目からは `ShowMenuAtCursor()` になる**ので、見た目が揃うか確認 |
| トレイを左クリック → 項目 | 同上 |
| ホットキー → 番号キー | `fromHotKey: true` の経路。連鎖でも `SelectFirstEnabledItem()` を効かせる |
| カテゴリのサブメニュー内の項目 | 親メニューごと閉じたあとに 1 枚目が出るか |
| スマートアクションの項目 | `captures` を持ち回ったまま連鎖できるか |

**1 枚目と 2 枚目で表示の経路が違う**のが唯一の非対称で、
右クリック経路だけ「1 枚目はトレイの横、2 枚目はカーソル位置」になる可能性がある。
§3-5 の「1 枚目の位置を覚える」を採れば、`Show()` に渡す座標を統一できて解消する。

---

## 6. 見積もり

| 場所 | 内容 | 行数の目安 |
| --- | --- | --- |
| `TemplateEngine.cs` | パーサ・展開・`ContainsChoice` | 120 |
| `TemplateEngine.cs` | `ExpandValues` の導入と既存経路の付け替え | 60 |
| `TrayIconManager.cs` | `ShowMenuAtCursor()` に連鎖用の引数（§3-4） | 20 |
| `TrayIconManager.cs` | 選択メニューの生成と連鎖 | 120 |
| `SettingsViewModel.cs` ほか | プレビュー・`ChoiceStatus` | 40 |
| `README.md` | 1 節 | — |

**新規ファイルは無し。** 案 B（窓）なら `ChoiceWindow.xaml(.cs)` で 200 行ほど増えていた。

危険なのは行数ではなく **§3-4 のフォーカス**で、ここだけは実機でしか確かめられない。

---

## 7. 段階

### v1（このメモの範囲）

`{choice:名前:選択肢|選択肢}` と `{choice:名前}`。逐次メニュー。

### v2（実装済み）

`{choices:名前:選択肢|選択肢|選択肢}`。区切りは `、` 固定。
チェック付きメニュー + 「決定」（§3-6）。
**区切り文字の指定は入れていない**。必要になってから決める（§1-4）。

### やらないこと

| | 見送る理由 |
| --- | --- |
| 無名形式 `{choice:様\|御中}` | §1-1。同じ選択を使い回せない |
| ~~選択肢の中に差し込み~~ | **対応済み**（§2-5）。`{input:}` `{choice:}` `{choices:}` `{seq}` だけ弾く |
| 前回の選択を覚える | 永続化すると連番と同じ重さ（`DESIGN_BASE_SYNTAX` #6 の理由）。<br>ただし**アプリ起動中だけメモリで覚える**なら安い。<br>`{choices}` を入れたことで**次の第一候補**になった――<br>毎回同じ 3 個をチェックし直すのは、1 つ選び直すより明らかに苦痛が大きい |
| 設定画面での選択肢の GUI 編集 | テキストで書けば足りる。項目が育ってから考える |
| 組み合わせのサブメニュー展開（案 A） | §3-1。`{choices}` で原理的に破綻する |

---

## 8. 実装順の推奨

```
1. TemplateEngine のパーサと展開      選択が渡らなければ書いたまま残る、まで。
                                     この時点で既存の挙動は 1 つも変わらない
2. ShowMenuAtCursor() の連鎖対応      §3-4。既存の 2 か所は既定値で無変更
3. 選択メニューの生成と連鎖           ここで初めて機能として繋がる
4. プレビュー・ChoiceStatus・ツールチップ   §5-1 の規則を 3 か所へ
5. README
──────────────────────────────────── v1 ここまで
6. {choices}                         2〜3 の上に、チェックと「決定」を足す
```

1 だけ入れた状態は**既存の動作を一切変えない**（`{choice}` は書いたまま残るだけ）ので、
安全に区切れる。2 は 1 と独立していて、順番を入れ替えてもよい。

---

## 9. 実機で確認したいこと

**1〜3 が最優先。** §3-4 の読みが外れていた場合、案 C の前提が崩れる。

1. **2 枚目のメニューでキーボード（矢印・Enter・番号キー）が効くか**（§3-4 の核心）
2. 連鎖の最後まで進んだあと、**元のウィンドウへフォーカスが戻るか**
3. 途中で Esc / 別の場所をクリックしたときも、フォーカスが戻り、コピーされないか
4. ホットキー → 番号キーだけで最後まで進めるか
5. メニューの表示位置。カーソル追従と 1 枚目固定のどちらが使いやすいか（§3-5）
6. 右クリック経路で、1 枚目（内部処理）と 2 枚目（`ShowMenuAtCursor`）の見た目が揃うか（§5-5）
7. `{choice}` と `{input:}` を両方含む項目で、メニュー → キャプチャの順に進むか（§4-3）
8. `{choice:敬称:|様}` の空の選択肢が `(空)` と出て選べるか（§5-4）
9. 選択肢が 10 個以上・1 つが長いときのメニューの見た目
10. HTML 形式の項目で、選択肢に含まれる `<` `&` がエスケープされるか（§5-3）

### `{choices}`（複数選択）で追加した確認項目

11. **`Closing` を `e.Cancel = true` で打ち消したとき、メニューが確実に開いたままになるか。**
    広く使われている書き方だが、閉じかけを止めているので実機で確かめる価値がある
12. 打ち消したあと、**キーボードの選択位置が保たれるか**（矢印キーで続けて操作できるか）
13. 番号キーでチェックを反転できるか。`PerformClick()` 経由なので効くはずだが未確認
14. 「決定」を押したときだけ閉じ、「← 戻る」でも閉じて 1 つ前へ戻るか
15. 0 件のまま「決定」を押すと、その差し込みが空になってコピーされるか

---

## 10. 実装の記録（v1・v2 完了）

### 出た不具合: 連鎖の 2 枚目以降が即閉じる（**診断を誤った**）

> **結論を先に。真因は「キーのオートリピート」だった。**
> 番号キーを押しっぱなしにすると、トレイメニューで項目を選んだあとも
> `KeyDown` が届き続け、開いたばかりの選択メニューを次々と選び抜けてしまう。
> 利用者が気付いて解決した。
>
> 下記はそのときの調査記録として残す。**ここに書いた原因分析は症状の説明としては誤り**だが、
> 見つけた `Closed` の順序問題そのものは実在するため、修正は残してある（後述）。
>
> **見落としの決め手**: 「`{choices}` は無事」という観測。
> 実装の差だと読んだが、正しくは**複数選択では番号キーがチェックの反転なので閉じない**、
> つまり<strong>そもそも壊れていなかった</strong>。
> 4 つの観測（ホットキー限定・最後の 1 枚・トーストなし・choices は無事）は
> すべてキー連打で説明がつき、こちらのほうが単純だった。
> **症状に合う説明を作りにいって、より単純な説明を探さなかった。**

**症状**: ホットキー経路のときだけ、**連鎖の最後の 1 枚**が開いた瞬間に閉じる。
2 か所選ばせる項目なら、1 枚目は選べて 2 枚目が消える。

**原因**: **`Closed` も `Click` より先に走る。** 同じ罠の 3 度目。

`OnClosed()` は「選んだかどうか」を `decided` フラグで見ていたが、
そのフラグを立てるのは `Click` ハンドラなので、`Closed` の時点ではまだ false。
**項目を選んで閉じた場合まで「中止」と誤判定していた。**

```
① Closed が先に走る → decided はまだ false → 中止と誤判定
     _choiceChainActive = false        ← 抑止が外れる
     RestoreForeground(有効なハンドル)  ← 復帰を予約
② Click → AdvanceChoice → 2 枚目を Normal で予約
③ [Normal]     2 枚目を表示
④ [Background] 抑止は外れている → フォーカスを奪う → 2 枚目が即閉じる
```

1 枚目が生き残るのは、`AskChoices()` が直前に `_choiceChainActive = true` にしているから。
死ぬのは**選択のあとに出す 2 枚目以降**だけになる。

#### なぜ今まで出なかったのか

**直前のバグ（フォーカスが戻らない）が、こちらを隠していた。**
`prompt.PreviousForeground` が `IntPtr.Zero` だったので、
`RestoreForeground(0)` は何もせず戻り、④ が起きなかった。
**戻し先を直したことで、隠れていた誤判定が表に出た。**

**対処**: `Closing` のときと同じく、フラグをやめて `e.CloseReason` で見分ける。
`ItemClicked` / `CloseCalled` なら選択、それ以外（`Keyboard` / `AppFocusChange`）なら中止。
`decided` フラグは廃止した。

#### 直し方が半端で、単一選択だけ残った

`CloseReason` で見分ける形にしたところ、**`{choices}` は直り `{choice}` は直らなかった。**
その差が答えだった。

| | 項目クリック時の動き | `Closed` が走る位置と理由 |
| --- | --- | --- |
| 複数選択 | `Closing` で `ItemClicked` を**打ち消している** | `Click` の**あと**・`CloseCalled`（こちらが決めた） |
| 単一選択 | 打ち消していない | `Click` の**前**・WinForms が決めた理由 |

複数選択はもともと「1 つ選ぶたびに閉じては選べない」から打ち消していただけだが、
**副作用として『閉じるタイミングと理由を自分で握る』状態になっていた。**
だから `CloseReason` を信じられた。

単一選択は WinForms 任せなので、`ItemClicked` が来る前提が外れると判定できない。
**そこで単一選択にも同じ打ち消しを入れ、`Decide()` で必ず自分で閉じることにした。**
これで両方とも「`Click` のあと・`CloseCalled`」に揃い、`CloseReason` を推測する必要がなくなる。

**教訓の追加**: イベントの順序が読めない場所では、
**順序を読もうとせず、自分が決められる形に持ち込む。**
「WinForms がどの理由で閉じるか」を当てにいくのではなく、
「閉じるのは自分が閉じたときだけ」にすれば、当てる必要がなくなる。

#### この修正を残す理由（真因は別だったが）

真因はキー連打だったので、この一連の変更は**症状の修正としては不要だった**。
それでも戻していないのは、`Closed` が `Click` より先に走ること自体は本当で、
`decided` フラグによる判定が**誤りだったこと自体は変わらない**から。
症状として表に出ていなかっただけで、
`RestoreForeground` の抑止が意図せず解ける経路は実在していた。

いま `OnClosed` は `CloseCalled` だけを見る。そのためには
単一選択でも `ItemClicked` を打ち消して自分で閉じる必要があり、両者は組で意味を持つ。
片方だけ戻すと `decided` の誤判定が復活するので、**戻すなら両方**。

#### 3 度同じ間違いをした

| 回 | 場所 | 誤り |
| --- | --- | --- |
| 1 | `{choices}` の `Closing` | あとで立てるフラグを、先に走る場所で読んだ |
| 2 | `AskChoices` の `_menuContextWindow` | あとで消える値を、消えたあとに読んだ |
| 3 | `OnClosed` の `decided` | 1 と同じ。**同じファイルの数十行下で繰り返した** |

**教訓**: WinForms のイベントは「閉じる系がすべて Click より先」。
`Closing` → `Closed` → `Click` の順で、
**Click ハンドラで書いた状態は、閉じる系のハンドラからは一切見えない。**
この層でフラグを使うなら、必ず「いつ書いていつ読むか」を先に確かめる。
判断材料が `CloseReason` として渡されているなら、そちらを使う。

### 出た不具合: 選び終えてもフォーカスが戻らない（修正済み）

**症状**: `{choice}` / `{choices}` の項目でコピーしたあと、元のウィンドウへフォーカスが戻らない。
`{choice}` を含まない項目では戻る。

**原因**: **またイベントの発火順だった。** 今度は `Closed` と `Click`。

```
1. HandleItemClick → メニューが閉じる → Closed → ClearMenuContext()
                                                _menuContextWindow = 0   ★
2. OnClick → ActivateClipItem → AskChoices → _menuContextWindow を読む → 0
```

`AskChoices()` は「戻す先」を `_menuContextWindow` から取っていたが、
その 1 つ前の段階で `ClearMenuContext()` が消していた。
`RestoreForeground(IntPtr.Zero)` は何もせず戻るので、**黙って戻らない**。

`{choice}` を含まない項目で戻っていたのは、`ShowMenuAtCursor()` が
**メニューを出す時点**で値を捕まえてクロージャに持っているから。クリック後に読み直していない。

**始末が悪いのは、この危険を自分で書いておきながら踏んだこと。**
`ChoicePrompt.PreviousForeground` の説明にこう書いてあった:

> 元のメニューが閉じた時点で `ClearMenuContext` が走って `_menuContextWindow` も消えるため、
> あとからは取り直せない。

「あとからは取り直せない」と書いた当のフィールドを、あとから読んでいた。
**危険を認識していても、書く場所が離れていれば踏む。**

**対処**: `_focusReturnWindow` を足す。`_menuContextWindow` と同じ値だが、
`ClearMenuContext()` で捨てない。役割が違うので寿命も違う。

| フィールド | 役割 | 寿命 |
| --- | --- | --- |
| `_menuContextWindow` | メニューの絞り込み（アプリ条件） | メニューを閉じるまで |
| `_focusReturnWindow` | フォーカスを戻す先 | 次にメニューを開くまで（クリックの処理より長い） |

### 出た不具合: ダークテーマでチェックの箱だけ白い（修正済み）

**症状**: `{choices}` のメニューで、チェックを付けた行の**箱だけが明るいまま**浮く。
メニューの地色と文字は正しく暗い。

**原因**: `ProfessionalColorTable` は余白とチェックで**別の色**を持っている。
`DarkColorTable` は `ImageMarginGradient*`（余白）だけを塞いでいて、
`CheckBackground` / `CheckSelectedBackground` を素通しにしていた。

**ここで一度間違えかけた**: 色表に `CheckBackground => _hover` を足すだけでは足りない。
既定の描画は「箱を敷く → その上にシステム色のチェックを重ねる」の 2 段構えで、
**箱だけ暗くするとチェック自体が暗い地に暗い線で描かれて見えなくなる**。
地とチェックの両方をこちらで決める必要がある。

**対処**: `DarkRenderer.OnRenderItemCheck()` を `base` を呼ばずに描き切る。

```csharp
// カーソルが乗っている行は行全体が hover 色。地をそれに合わせないと
// チェックの周りだけ四角く色が違って見える
Color back = e.Item is { Selected: true, Enabled: true } ? hover : background;
ControlPaint.DrawMenuGlyph(e.Graphics, rect, MenuGlyph.Checkmark, text, back);
```

色表のほうも `CheckBackground` など 4 つを塞いである。
自前描画で使わなくなったが、別の経路から参照されても浮かないようにするため。

**学べること**: `TrayMenuTheme` は<strong>トレイメニューで使う描画しか塞いでいなかった</strong>。
`{choices}` はこのアプリで初めてチェック付き項目を使ったので、
塞ぎ忘れが表に出た。既存のテーマ適用を「呼べば済む」と考えたが、
**新しい種類の部品を持ち込むときは、テーマ側にその部品の色があるかを確かめる必要がある。**

### 見直しで見つけて直したもの

利用者から 3 つ不具合が上がったあと、同じ形の穴が残っていないかコードを読み直した。

| 見つけたもの | 直し方 |
| --- | --- |
| `ShowChoiceMenu()` の例外がアプリを落とす。<br>ディスパッチャのコールバックなので捕まえる人がいない。<br>さらに `_choiceChainActive` が立ったまま残り、<br>**以降フォーカスの復帰が二度と効かなくなる** | `ShowMenuFromHotKey()` と同じくトーストに変える。<br>あわせて抑止を解いてフォーカスを戻す |
| タブだけの選択肢が見えない。<br>`ToSingleLine()` がタブを空白 4 つに変えるため、<br>空白の手当てをすり抜けていた | 「見える文字が 1 つも無い」で判定する |
| 同じ名前を 2 か所で参照していると、<br>「選択肢が書かれていません」が 2 回出る | 名前ごとに 1 回だけ知らせる |

### 残っている懸念（実機でないと確かめられない）

| # | 内容 | 影響 |
| --- | --- | --- |
| 1 | 選択肢が 10 個以上ある `{choices}` では、`AssignNumberAccessKeys()` が<br>10 個までしか番号を振らないため、**「決定」に番号が届かない** | 矢印キー + Enter かマウスが要る |
| 2 | ~~ダークテーマでのチェック欄の配色~~ → **実機で再現し、修正済み**（下記） | — |
| 3 | `{choice}` と `{clip}` を両方含む項目で**クリップボードが空**だと、<br>全部選び終えたあとに「クリップボードが空です」で中止になる。<br>選ばせる前に確かめていない | 手戻りが大きい |
| 4 | 連鎖の 2 枚目以降で `SetForegroundWindow` が<br>Windows のフォアグラウンドロックに拒否される可能性 | 2 枚目からキーが効かない |

#3 は選択を先に置いた（§4-3）ことの裏返しで、直すなら
`CopyToClipboard()` の前提チェックを選ばせる前にも通す。

### 出た不具合: `{choices}` の「決定」で閉じない（修正済み）

**症状**: 複数選択で「決定」を押してもメニューが閉じない。

**原因**: **`Closing` は `Click` ハンドラより先に走る。**

```
ToolStripItem.HandleClick
  1. ParentInternal.HandleItemClick(this)   → 閉じようとする → Closing 発火
  2. OnClick(e)                             → こちらの Click ハンドラ
  3. ParentInternal.HandleItemClicked(this)
```

当初は `Closing` で「`decided` が立っていなければ打ち消す」としていたが、
`decided` を立てるのは 2 の Click ハンドラなので、
**1 の時点では決定を押した場合でも必ず false**。決定まで打ち消していた。

「あとで立てるフラグ」を、それより先に走る場所で読んでいた――順序の取り違えである。

**対処**: フラグをやめ、**閉じた理由だけで判断する**。
決定と「← 戻る」は自分で `Close(CloseCalled)` を呼び、理由を変えて抜ける。

| 操作 | `CloseReason` | `Closing` | 結果 |
| --- | --- | --- | --- |
| 選択肢のチェック反転 | `ItemClicked` | 打ち消す | 閉じない（意図どおり） |
| 決定 / ← 戻る | `CloseCalled` | 通す | 閉じる |
| Esc | `Keyboard` | 通す | 中止 |
| 別の場所をクリック | `AppFocusChange` | 通す | 中止 |

`decided` は残っているが、役割は「`Closed` で中止かどうかを見分ける」だけになった。
こちらは `Closed` が Click より<strong>あと</strong>に走るので、読む時点では正しく立っている。

**学べること**: このバグと §10 の「メニューが出ない」は同じ形をしている。
**どちらもイベントの発火順を確かめずに書いた。**
WinForms と WPF のディスパッチャが混ざる場所では、
「いつ走るか」を確かめてからでないとフラグは信用できない。

### 出た不具合: 見た目がトレイメニューと違う（修正済み）

**症状**: 選択肢のメニューだけ雰囲気が違う。

**原因**: `TrayMenuTheme.Apply()` の呼び忘れ。
トレイメニューは `PopulateMenu()` の最後でこれを呼んでダークテーマの配色を配っているが、
選択メニューは自前で `ContextMenuStrip` を作っていて、そこを通らなかった。
**ダークテーマのときだけ、選択メニューが Windows 標準の明るい見た目のまま出ていた。**

§3-3 で「既存の解決をそのまま呼ぶ」と書いたのは前面化・フォーカス・番号キーの話で、
**配色を配っているのが `PopulateMenu()` 側だと気付いていなかった**。
`ShowMenuAtCursor()` を再利用しても、メニューを自分で組み立てる以上ここは付いてこない。

**同時に見つかった不具合**: `ShowImageMargin = false` をトレイメニューに合わせて設定していたが、
**`ShowImageMargin` と `ShowCheckMargin` が両方 false だと WinForms はチェックの印を描かない**。
`{choices}` で何を選んだのか見えていなかった。複数選択のときだけ `ShowCheckMargin = true` にする。

**あわせて揃えたもの**: 見出し行の書き方を、セッション中のメニューの
`入力待ち: 名前 (1/2)` に合わせて `選択: 敬称 (1/2)` にした（全角かっこをやめた）。

### 出た不具合: 選択肢のメニューが出ない（修正済み）

**症状**: `{choice:…}` を含む項目を選んでも、選択肢のメニューが出ない（または一瞬で消える）。

**原因**: §3-4 で見つけたフォーカス奪取そのものだが、**危ない 1 枚を数え間違えていた**。
§3-4 は「連鎖の 2 枚目以降」だけを見ていて、
**選択メニューを起動したトレイメニュー自身**が同じ復帰を予約することを見落としていた。

```
1. 項目をクリック → ActivateClipItem → InvokeLater(1 枚目) を Normal で予約
2. トレイメニューが閉じる → RestoreForegroundOnce → 復帰を Background で予約
3. [Normal]     1 枚目を表示。SetForegroundWindow(_menuHost)
4. [Background] 「前面はまだ _menuHost だ」→ 元のウィンドウへ戻す
5. 1 枚目が活性を失って即座に閉じる          ← 選択肢が出ないように見える
```

`ShowMenuAtCursor()` に足した `restoreForegroundOnClose` は
**これから出す 1 枚**にしか効かない。既に予約済みの復帰は止められない。

**対処**: `_choiceChainActive` を立て、`RestoreForeground()` の遅延処理の先頭で見る。
連鎖のあいだ前面は `_menuHost` のままでよく、戻すのは連鎖が終わってからでよい。

```csharp
// AskChoices() の中。呼び出し元のメニューが閉じるより前に立てる必要がある
_choiceChainActive = true;
```

連鎖の出口 3 つすべてで落とす。**完了時は落としてから `RestoreForeground()` を呼ぶ**
（順序が逆だと、自分で仕込んだ抑止に自分の復帰が握りつぶされる）。

| 出口 | 落とす場所 |
| --- | --- |
| 最後まで選んだ | `AdvanceChoice()` の `Index >= Count` |
| 中止（Esc・別の場所をクリック） | `OnClosed()` の `!decided` |
| アプリ終了 | `ShowChoiceMenu()` の `_disposed` |

**この見落としから学べること**: 遅延実行どうしの干渉は、
「これから出すもの」だけを見ていても足りない。
**すでにキューに入っているもの**を数える必要がある。
`restoreForegroundOnClose` 引数は設計として正しいが、それだけでは不十分だった。
`ShowMenuAtCursor()` の引数は 2 枚目以降のために残してある（両方要る）。


### v2（`{choices}`）

§3-6 の読みどおり、逐次メニューの枠にそのまま収まった。新しい UI 部品は要らなかった。

| 決めごと | 実装 |
| --- | --- |
| 接頭辞 | `choices:` を先に見る。`choices:x` は `choice:` で始まらない（`choice` の次が `s`）ので食い違わない |
| 閉じない | `Closing` で `CloseReason == ItemClicked` を `e.Cancel = true`。<br>決定・戻るは自分で `Close(CloseCalled)` を呼んで抜ける（下記の不具合を参照） |
| 反転 | `CheckOnClick = true`。番号キーは `PerformClick()` を通るので同じく反転として効く |
| 連結の順 | 選択肢を**書いた順**。選んだ位置（`HashSet<int>`）で持つので、<br>同じ文字列の選択肢が 2 つあっても取り違えない |
| 0 件 | 許す。空文字になる。「決定」のツールチップで選び忘れと区別できるようにする |

#### 設計から変えた点

- **「決定（N 件）」の件数を見出しに入れなかった。**
  `AssignNumberAccessKeys()` が `Text` の先頭へ番号を差し込むため、
  件数を書き換えるたびに**番号ごと消えてしまう**。
  件数はすぐ上に並ぶチェックの数で分かるので、見出しは `決定` の固定にし、
  件数と結果はツールチップへ回した
- **「決定」に `0` を割り当てなかった。** 番号は既存の `AssignNumberAccessKeys()` に任せ、
  選択肢のあとに続く番号になる。特別扱いを入れるほどの理由が見つからなかった
- **複数選択では空の選択肢を連結に加えない。**
  加えると `{choices:敬称:|様|さん}` で「空」と「さん」を選んだときに
  `、さん` という区切りだけが浮いた文字列になる。
  単一選択の空には「付けない」という意味があるが、複数選択では
  **何も選ばなければ同じ結果になる**ので、落としても表せなくなるものは無い

#### 実装して分かったこと

`{choices}` を足すためにエンジンで変えたのは次の 4 か所だけだった。
§1-4 で決めた「器を `Dictionary<string, string>` にする」が効いている。

```
ChoiceDefinition に AllowMultiple を 1 つ        （v1 で入れなかったものを、必要になった今入れた）
TryParseChoiceToken を接頭辞 2 種に対応          （本体は共通のまま）
ExpandToken に case "choices": を 1 行           （差し込む側から見れば単一選択と違いが無い）
JoinChoices を追加                              （連結はメニュー側で済ませ、器へは文字列で渡す）
```

`ExpandContext` も `ExpandValues` も**変更なし**。

---

## 11. 実装の記録（v1 完了）

§8 の 1〜5 を実装した。設計から変えた点だけ書き残す。

### 変えた点

| 設計 | 実装 | 理由 |
| --- | --- | --- |
| `ChoiceDefinition(Name, Options, AllowMultiple)` | `AllowMultiple` を入れなかった | v1 で常に false のフィールドは、誰も通らない分岐を残すだけ。<br>`{choices}` は `ExpandToken()` の `switch` に `case "choices":` を並べれば分かれる（§1-4）。<br>**器（`Dictionary<string, string>`）の予約は設計どおり入れた**ので、v2 の縛りは生まれていない |
| `ExpandValues` は既存のオーバーロードを残して段階的に移行 | 位置引数のオーバーロード 5 段を**削除した** | 呼び出しが 5 か所しかなく、その場で全部移せた。<br>使われないオーバーロードを残す理由が無い |
| プレビューの代表値を `SettingsViewModel` に置く | `TemplateEngine.GetDefaultChoices()` に置いた | §5-1 が要求したのは「3 か所で規則を揃えること」。<br>トレイのツールチップが設定画面の ViewModel を参照するのは筋が悪く、<br>規則をエンジンに 1 つ置けば揃うことが構造で保証される |
| 空の選択肢は `(空)` と表示 | 空白だけの選択肢も `(空白 N 文字)` と表示 | `{choice:区切り: |,}` のように半角スペースを選ばせると、<br>`(空)` の手当てだけでは<strong>押せる行が見えない</strong>ままだった |

### 追加で必要だった配線

- `_menuOpenedFromHotKey`。連鎖の 2 枚目以降にキーボード操作を引き継ぐために要る。
  右クリックは `ShowTrayMenu()` を通らないので、`OnIconMouseDown` でも落としている
- **1 枚目も `InvokeLater` で出す。** 設計では連鎖の 2 枚目以降だけを遅延させるつもりだったが、
  1 枚目も「項目をクリックしたメニューの `Click` の中」から出すことになるため、同じ問題を踏む
- `PreviousForeground` を**連鎖を始める時点で確定させる**。
  `ShowMenuAtCursor()` の戻り値で受け取る形も試したが、1 枚目のメニューが閉じた時点で
  `ClearMenuContext()` が走って `_menuContextWindow` が消えるため、あとからでは取り直せない

### 確認したこと（サンドボックスに .NET SDK が無いため、ロジックを移植して実行）

- 記法の解析: 参照形式・空の選択肢・`\|`・`{{}}`・`10:00|15:30`（2 つめ以降の `:`）・
  `+1|-1`（オフセットと誤読しない）・全角空白の保持
- 誤りの検出: 重複定義（最初が勝つ）・未定義の参照・選択肢 1 個・名前に `|`
- 連鎖の状態遷移: 順に選ぶ / 「戻る」で選び直す / 中止（`Completed` を呼ばない）
- 波かっこと丸かっこの対応、XAML の妥当性

**§9 の実機確認は未実施。** 特に 1〜3（連鎖中のキーボードとフォーカス）は
コードを読んだだけでは確かめられない。

---

## 11. 判断

- **記法は既存パーサに素直に乗る**（§2-1、§2-2）。正規表現の変更は要らない。
  エンジン側の追加は `{input:}` の写しで、リスクは低い
- **UI は逐次メニュー（案 C）。** 決め手は「新しい部品が要らない」こと。
  前面化・フォーカスの戻し・番号キーは `ShowMenuAtCursor()` に**既に実装されて動いている**。
  案 B（窓）はそれを作り直すことになる
- **唯一の実装上の難所は §3-4 のフォーカス。** `RestoreForeground()` の遅延復帰が
  連鎖の 2 枚目を殺す。`ShowMenuAtCursor()` に引数を 1 つ足して回避するが、
  **実機でしか確かめられない**
- **`{choices}` は v1 の記法と器で受けられる**（§1-4、§3-6）。
  `choices` という名前を予約し、選択結果を `Dictionary<string, string>` で持ち回れば、
  v2 でエンジンの器は変わらない。区切り文字の指定だけは未決のまま残す
- ~~**`}}` の既存バグ**（§2-4）は `{choice}` が踏みやすくするので、回避して別の仕事に切り出す~~
  → **直した。** 「バックトラックが要る」という見立ては悲観しすぎで、
  読む順を決めるだけで済んだ（§2-4）。副作用として選択肢の中の差し込みも扱えるようになり、
  安全な範囲だけ展開する形にした（§2-5）
- **`Expand()` のオーバーロードが限界**（§4-1）。`{choice}` で 6 つめの値が増えるので、
  ここで引数の袋に移す。移行は既存のオーバーロードを残したまま段階的にできる
