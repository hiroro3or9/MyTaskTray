# メニューを出すホットキー 検討メモ

「ホットキーを押すとメニューが出て、そこから選んでコピーする」案の検討。
現在は設定画面で `Ctrl+Alt+V` のような組み合わせを指定でき、カーソル位置へ表示するところまで実装している。
既定値は空（無効）で、設定した場合だけグローバルホットキーを登録する。
ホットキーから開いた場合だけ、誤操作防止のため「終了」と直前の区切り線を隠している。

前回の検討で、項目ごとにホットキーを登録する案と比べてこちらのほうが安く広く効くと判断したもの。
調べたところ、**表示まわりはほぼ書き足しが要らない**ことが分かった。
代わりに、**キーボードだけで完結するか**が成否を分ける。

---

## 1. 表示部分は既にある

`TrayIconManager.ShowTrayMenu()` が既に存在する。左クリックでメニューを出すために書かれたもので、
「アプリが前面にならないとメニューが閉じない」という Win32 の面倒も既に解決済み。

ホットキーの処理は**これを呼ぶだけ**。

```
WM_HOTKEY → ShowTrayMenu()
```

`SetForegroundWindow` の P/Invoke も既にある。追加で要るのは
`RegisterHotKey` / `UnregisterHotKey` と、それを受ける隠しウィンドウだけ。

### 表示位置は変える必要がある

`ShowTrayMenu()` は 2 経路ある。

1. `NotifyIcon` の内部処理をリフレクションで呼ぶ（通常経路）→ **トレイアイコンの近く**に出る
2. `menu.Show(Cursor.Position)` + 前面化（フォールバック経路）→ **カーソル位置**に出る

トレイクリックなら 1 が正しい（押した場所の近くに出る）。
だがホットキーの場合、利用者の視線と手はトレイではなく作業中のウィンドウにある。
マルチモニタだとトレイは別の画面かもしれない。

**ホットキーからは経路 2 を使う**のが素直。既にフォールバックとして書かれているので、
`ShowTrayMenu(atCursor: true)` のような引数を足して呼び分ければよい。

---

## 2. 最大の論点: キーボードだけで完結するか

ここが本質。ホットキーを押す人の手はキーボードにある。
メニューが出てもマウスに持ち替えるなら、トレイをクリックするのと大差ない。

### 2-1. まずキーボードフォーカスが要る

`ContextMenuStrip.Show()` で出したメニューが、そのままキーボード入力を受けるとは限らない。
矢印キーや Enter が効かないと何も始まらないので、
**表示直後に先頭項目を選択状態にする**必要がある。

```
menu.Show(...) の後に、先頭の ToolStripItem を Select() する
```

これが効くかどうかは実機で確認するしかない。**この案全体の成否がここに懸かっている**ので、
何を作るより先に、ここだけを試すのがよい。

#### 追記: 選択状態にするだけでは足りない

先頭項目を `Select()` しても、**アプリが前面になっていなければキー入力自体が届かない**。
WinForms のメニューはスレッドのメッセージキューに来たキーを拾う仕組みなので、
前面が他アプリのままだとキーはそちらへ行ってしまう。同じ理由で、
他所をクリックしてもメニューが閉じない（設計メモ §1 で「解決済み」としていた問題の再発）。

ドロップダウン自身は非アクティブなウィンドウなので、
`SetForegroundWindow(menu.Handle)` を呼んでも前面化できない。
`NotifyIcon` が右クリック時に踏んでいるのと同じ手順を再現する:

```
1. 画面に出ない窓（MenuHostWindow）を前面化する
2. menu.Show(カーソル位置)
3. その窓のオーナーをメニューに設定する（タスクバーにボタンを出さない）
4. その窓へ WM_NULL を送る（前面化を確定させる）
5. 先頭項目を Select()
```

WM_HOTKEY を受け取った直後は Windows のフォアグラウンド制限を通過できるため、
ホットキー経路ではこの前面化が通る。

#### 前面化するとフォーカスが元に戻らない

前面化した以上、メニューを閉じたあとに**元のウィンドウへ戻す**必要がある。
戻さないとコピーした直後の `Ctrl+V` が別の場所へ行き、このアプリの目的が崩れる。

ただし「設定」を選んで設定画面を開いた場合まで戻してしまうと、開いた画面からフォーカスを奪う。
**閉じた時点で前面がまだ自分の見えない窓のままのときだけ戻す**、という条件にしてある。
Windows が自分で元へ戻してくれる場合も、この条件で自動的に何もしなくなる。

### 2-2. 数字で一発選択できると、当初やりたかったことがそのまま実現する

`ToolStripMenuItem` は `&` によるアクセスキーに対応している。
項目名の先頭に `&1 ` `&2 ` … を自動で付けると:

```
ホットキー → 1     で 1 番目の項目がコピーされる
```

**2 打鍵で特定の項目をコピーできる。**
項目ごとにホットキーを登録する案がやりたかったことが、登録 1 つで実現する。
しかも新しい項目を足しても勝手に番号が振られ、設定は要らない。

注意点:

- 既存の項目名に `&` が含まれていると誤作動するので、`&&` にエスケープする必要がある
- 数字は 1〜9（0 を足して 10 個）まで。それ以上は矢印キーで選ぶ
- 番号は表示順に振る。並べ替えると番号が変わる。
  これは「一覧の並び順がそのままメニューの順序」という既存の考え方と一貫している
- カテゴリ（サブメニュー）の中身には振れない。トップレベルのみ

番号を出すかどうかは設定で切り替えられるようにしてもよいが、
まずは常に出す形で試して、邪魔かどうかを見るのが早い。

### 2-3. 頭文字での移動

WinForms のメニューは、項目名の先頭文字を打つとその項目に移動する挙動を持つ。
日本語名だと効きにくいので、2-2 の番号のほうが確実。

---

## 3. 設定画面のコストが激減する

前回「実装量の大半は設定画面」と書いたが、**この案ではそこが崩れる**。

| | 項目ごと | メニュー 1 つ |
| --- | --- | --- |
| 記録欄 | 項目の数だけ | 1 つ |
| アプリ内の重複検証 | 必要 | 不要 |
| 「複製」でホットキーが二重になる問題 | 対処が必要 | 起きない |
| 新しい項目 | 個別に設定が要る | 何もしなくてよい |

さらに、**v1 は記録欄を作らずに済ませられる**。

README に「設定ファイルは手で編集することもできます」と書いてあるとおり、
このアプリは手編集を前提の 1 つにしている。
設定画面には**テキスト欄**（`Ctrl+Alt+V` と打つ）を置き、
解釈できなければ状態表示と保存時の警告で知らせる形にした。
「キーを押して記録する」入力欄は、需要が確認できてから足せばよい。

Alt が `Key.System` になる、IME が絡む、Tab がフォーカス移動に食われる、
といった WPF 側の面倒を丸ごと後回しにできる。

### 設定ファイル

`AppSettings` に 1 つ足すだけ。古いファイルには無いので既定値（空）になる。`Version` の変更も不要。

```json
{ "Version": 1, "MenuHotKey": "Ctrl+Alt+V", "ShowCopyNotification": true, "Items": [ … ] }
```

**既定値は空にする。** 導入しただけで他のアプリの組み合わせを奪うのは避ける。

---

## 4. 項目ごとの案から引き継ぐ注意点

登録が 1 つになっても、以下はそのまま必要。

- **`MOD_NOREPEAT (0x4000)` を付ける。**
  付けないと押しっぱなしでメニューが開閉を繰り返す。
  `{seq}` の暴走ほどの実害はないが、見た目が壊れる
- **修飾キー（Ctrl / Alt / Win）を必須にする。**
  `V` 単独を登録すると全アプリで `V` が打てなくなり、
  設定画面でも打てないので復旧できない。Shift だけでは不十分
- **登録の失敗を知らせる。**
  他のアプリが先に取っていると `RegisterHotKey` は `false` を返す。
  これは設定した瞬間だけでなく**次回起動時にも起きる**（前回終了後に他のアプリが取った場合）。
  黙って失敗すると「押しても何も起きない」だけになる。起動時の失敗もトーストで知らせる

---

## 5. Windows 実機で確認が必要なこと

上から順に、確認できないと先に進めない順。

1. **`ContextMenuStrip` を出した直後に、矢印キーと Enter が効くか**（2-1）。
   効かなければこの案の価値が半減する。**まずこれだけを試す**
2. **項目を選んだあと、元のウィンドウで `Ctrl+V` がそのまま効くか**（2-1 の追記）。
   効かないとコピーツールとして成立しない
3. メニューを開いた状態でタスクバーにボタンが現れないか（オーナー設定が効いているか）
4. `&1` によるアクセスキーが、この出し方のメニューで効くか（2-2）
5. 管理者権限のウィンドウが前面のとき `WM_HOTKEY` が届くか。
   届くはずだが UIPI の絡みは実機で見たい。
   届いても前面化は弾かれる可能性があるので、そのときの挙動も見る
6. 同じ状況でクリップボードへの書き込みが成功するか（`{clip}` を使う項目）

---

## 6. 判断

- **表示部分は既存の `ShowTrayMenu()` でほぼ済む。** 新規は Win32 の登録まわりだけ
- **成否は 2-1（キーボードフォーカス）に懸かっている。** 何を作るより先にここを試す
- 2-2 の数字選択まで通れば、**2 打鍵で特定項目をコピー**でき、
  項目ごとにホットキーを登録する案は不要になる可能性が高い
- v1 は設定欄をテキスト入力にして、記録欄は後回しにできる

### 進め方の案

```
1. ContextMenuStrip を出して矢印キーが効くか、小さく試す   ← ここで可否が決まる
2. RegisterHotKey + 隠しウィンドウ、組み合わせは決め打ち
3. 設定（テキスト欄）と、登録失敗の通知
4. &1 &2 … の番号付け
5. 需要を見てから「キーを押して記録する」入力欄
```
