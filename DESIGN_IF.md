# 条件分岐 `{if:}` 設計メモ

対象: `Services/TemplateEngine.cs`。
問い: **テンプレートの中で条件分岐を書けるようにすべきか。**

`{choice:}`（人が選ぶ）とスマートアクション（クリップボードの内容で項目を出し分ける）が
既にある状態で、さらに `{if:条件|真|偽}` を足す価値があるかを検討する。

> **状態: `{if:}` は見送り、`{default:}` は実装待ち。**
> 文法と実装順は決定済み。具体的な次工程は `ROADMAP.md` を正とする。

---

## 0. 前提: このアプリには既に 3 つの分岐がある

| 分岐 | 何で決まるか | いつ決まるか | 単位 |
| --- | --- | --- | --- |
| スマートアクション（表示条件） | クリップボードの内容 | メニューを開いた時 | **項目** |
| 表示するアプリ | 前面ウィンドウ | メニューを開いた時 | **項目** |
| `{choice:}` | 人が選ぶ | クリックした後 | 項目の中 |

`{if:}` は 4 つめで、**唯一「1 つの項目の内側」を機械的に分岐させる**ものになる。

### 選別の問い

> それは「**項目を 2 つに分ければ書ける**」ものか？

Yes なら `{if:}` は要らない。既にある 3 つのどれかで足りる。
No のものだけが `{if:}` の存在理由になる。

`DESIGN_BASE_SYNTAX.md` §0 で `@` の意味を先に固定したのと同じ考え方で、
ここでも先に線を引いておく。線を引かないと「便利そう」で通ってしまう。

---

## 1. 用途の棚卸し

| やりたいこと | いま書けるか | `{if:}` が要るか |
| --- | --- | --- |
| クリップボードが URL なら A、そうでなければ B | スマートアクションで項目を 2 つに分ける | 不要 |
| クリップボードが日付なら和文、数値なら別文 | 同上（`Date` / `Number` 条件） | 不要 |
| VSCode ならコマンド、ブラウザなら報告文 | 「表示するアプリ」で分ける | 不要 |
| 「様」か「御中」か | `{choice:敬称:様\|御中}` | 不要（後述） |
| 【至急】を付ける／付けない | `{choice:急ぎ:\|【至急】}` | 不要 |
| **入力や抽出が空なら代替値を出す** | **書けない** | **要る** |
| 曜日で末尾の文言を変える | 書けない | 要る（ニッチ） |
| 数量が 1 か複数かで単複を変える | 書けない | 要る（ニッチ） |

### 「様／御中」は `{if:}` にしないほうがよい

一見 `{if:}` の典型例に見える:

```
{if:{clip}|/株式会社/|{clip} 御中|{clip} 様}
```

だが `{choice:敬称:様|御中}` のほうが優れている。
**誤判定が起きないから**である。「山田株式会社商店」のような文字列で御中になっても、
貼り付けるまで気付かない。人が選ぶなら 1 手増える代わりに間違いようがない。

`{if:}` が `{choice:}` に勝つのは「**判定が確実で、かつ選ばせるほどでもない**」場合だけ。
空かどうかの判定はまさにそれに当たる。文字列の内容から意味を推測する判定は当たらない。

### 残るもの

**実質「空のときの代替値」1 種類。** 曜日や単複は書けたら嬉しいが、
これのために 4 引数の文法を導入するほどではない。

---

## 2. 実装上の壁（実コードで確認）

### 2-1. 通らなかった枝の副作用 — 一度解いた問題の再来

`IsUnsupportedInChoiceOption()` に、選択肢の中に書けない差し込みの理由が書いてある:

```csharp
/// <item><c>seq</c> — 値は決まるが、<see cref="ContainsSequence"/> はテンプレート全体を見るため、
/// <strong>その選択肢を選ばなくても連番が進む</strong>。
/// 「選ばなかったのに番号が飛んだ」になるので許さない。</item>
```

**`{if:}` の枝はこれと全く同じ問題を持つ。**
`ContainsSequence()` はテンプレート全体を走査するので、
通らなかった枝に `{seq}` があるだけで永続カウンターが進む。

```
{if:{clip}|/URL/|{seq}|なし}     ← URL でなくても連番が進む
```

`{seq}` は枝の中では**禁止するしかない**。選択肢で既に出した結論と同じ。

### 2-2. 事前収集と噛み合わない（ただし致命傷ではない）

コピーの流れは「選択 → 入力 → 展開」の順:

```
ActivateClipItem()          GetChoiceDefinitions() でテンプレート全体を走査 → 選択メニュー
  → ContinueActivateClipItem()  GetInputDefinitions() でテンプレート全体を走査 → 入力を集める
    → CopyToClipboard()         Expand() で初めて展開＝初めて条件が評価できる
```

**条件は展開時にしか評価できないのに、収集は展開の前に終わっている。**
したがって `{if:}` の枝に `{input:}` や `{choice:}` があると、
**通らないほうの枝の分まで聞かれる。**

当初これを致命的と見たが、実コードを読むと**実害は小さい**:

- `GetInputDefinitions()` は名前で重複排除する（`Dictionary<string, List<string>>`）。
  最も欲しい用途 `{if:{input:備考}||（なし）|{input:備考}}` は同じ名前なので **1 回しか聞かれない**
- 別々の名前を枝に書いた場合だけ「使わない値を聞かれる」。
  余計な入力 1 回・余計なメニュー 1 枚で、状態は壊れない
- `{seq}` と違って**永続化する副作用がない**。ここが 2-1 との決定的な差

したがって禁止一覧は `{seq}` だけでよく、`{input:}` / `{choice:}` は
「**通らない枝の分も聞かれる**」と仕様に明記して受け入れる。
選択肢のとき `input` を禁じたのは「メニューを出す時点で値が無い」からで、
`{if:}` は展開時なので値は既に揃っている。同じ禁止一覧をそのまま持ってくる必要はない。

### 2-3. `|` が 4 つめの意味を持ち、正規表現と衝突する

現在 `|` には 3 つの意味がある:

| 書き方 | `\|` の意味 | 引数の数 |
| --- | --- | --- |
| `{calc:式\|書式}` | 書式の区切り | 2 |
| `{choice:名前:A\|B\|C}` | 選択肢の区切り | 可変 |
| `{replace:元\|検索\|置換}` | 引数の区切り | 3（`TrySplitTextTransformArguments` は 3 個ちょうどを要求） |

`{if:値|条件|真|偽}` は **4 引数**で最多になる。そのうえ条件に正規表現を使うなら
――`/…/` は `{clip:/…/}` `{input:名前:/…/}` `{app:title:/…/}` で既に確立された記法なので、
使わない理由がない――**正規表現の選択（alternation）が必ずエスケープを要求する**:

```
{if:{clip}|/株式会社\|有限会社/|御中|様}
                  ^^ これを忘れると引数が 5 個になって解釈できない
```

`{regexreplace:}` が既に同じ問題を抱えているが、引数が 3 → 4 に増え、
枝の中にさらに `|` を含む差し込み（`{calc:式|書式}`）を書けるため、破綻しやすさが跳ね上がる。
`SplitChoiceOptions()` と同じく `\|` で逃げられるものの、
「正規表現を書くときだけ普段と違うエスケープが要る」のは説明できない仕様になる。

### 2-4. 失敗したときに残る文字列が長い

「解釈できないものは書いたままの文字列を残す」が既存の方針。
`{clip}` が展開されずに残るのは短いので目に付く。
`{if:}` は**両方の枝を含む**ので、失敗すると長い文字列がそのまま貼り付けられる。

プレビューで気付けるという反論はあるが、設定画面の「コピーして試す」にはガードが無い
（`DESIGN_BASE_SYNTAX.md` の既知の穴と同じ）。方針は同じでも、痛みの大きさが違う。

---

## 3. 代替案 `{default:値|代替}`

用途の本命（§1 の「空のときの代替値」）だけを、**2 引数**で切り出す。

```
{default:{input:備考}|（なし）}
{default:{clip:/ID-(\d+)/}|不明}
{default:{app:title}|（アプリ不明）}
```

`{if:}` に対する利点:

- **引数が 2 つ。** `{replace:}` の 3 つより単純で、`TrySplitTextTransformArguments()` を
  「3 個ちょうど」→「2 個」に変えた版を足すだけ
- **条件の言語が要らない。** 空＝偽と決めれば済み、正規表現も等値比較も出てこない。
  §2-3 の `|` 衝突が起きない
- **枝が 1 つしかない。** 失敗時に残る文字列が短い（§2-4）
- `{seq}` の禁止（§2-1）は同じく必要だが、対象が代替値側だけになる

### `{default:}` には `{if:}` に無い独自の価値がある

素直に「空なら代替値」と実装すると**動かない**。ここが重要な点で、
`{clip:/…/}` は一致しなかったとき**空文字ではなく元のトークン文字列を返す**からである:

```csharp
private static string ExtractByRegex(string value, string pattern, string original)
    => TryExtractByRegex(value, pattern, out string extracted) ? extracted : original;
```

つまり `{default:{clip:/ID-(\d+)/}|不明}` の `{default:}` が受け取るのは
空文字ではなく `{clip:/ID-(\d+)/}` という**文字列**になる。

そこで `{default:}` の定義を「**空 または 展開されずに残った**なら代替値」とする。
判定は「元の値が差し込みを含み、かつ展開後も文字列が変わっていなければ未展開」でよい:

```csharp
string sourceTemplate = DecodeTextTransformArgument(sourceExpression.Trim(), decodeBraces: false);
string source = Expand(sourceTemplate, context, depth + 1, false);

bool unexpanded = ContainsAnyToken(sourceTemplate)
    && string.Equals(source, sourceTemplate, StringComparison.Ordinal);

return source.Length == 0 || unexpanded
    ? Expand(替えの側, context, depth + 1, false)
    : source;
```

`{default:abc|x}` のように差し込みを含まない値が誤って代替に落ちないよう、
`ContainsAnyToken()` の条件が要る（`abc` は展開しても `abc` のままだが未展開ではない）。

**この定義は「解釈できないものは書いたまま残す」方針の逃げ道になる。**
いまその方針は「ユーザーが誤りに気付けるように」という理由で正しいが、
`@clip` の日付が読めない・正規表現が一致しない・前面アプリが取れないといった
「**起きうるし、起きたときの代替も決まっている**」場面では、
書いたまま残すのは単に困る。`{default:}` はそこを利用者側で埋められるようにする。

`{if:}` の縮小版ではなく、このアプリ固有の問題に対する独自の解として価値がある。

---

## 4. 一覧と判断

| # | 案 | 内容 | 価値 | コスト | 優先度 |
| --- | --- | --- | --- | --- | --- |
| 1 | `{default:値\|代替}` | 空・未展開のときの代替値 | 中〜高 | 小 | **高** |
| 2 | `{if:値\|/正規表現/\|真\|偽}` | 一般の条件分岐 | 低〜中 | 中〜大 | 保留 |
| 3 | `{if:}` の条件に数値比較 | `>` `<` `=` を書けるように | 低 | 大 | 見送り |
| 4 | `{elif}` / `{else}` | 多分岐 | 低 | 大 | 対象外 |

### 結論

**`{if:}` は単独では見送り、`{default:}` を先に入れる。**

理由:

1. §1 のとおり、`{if:}` で書きたいことの大半は**項目を分ければ既に書ける**。
   スマートアクションと「表示するアプリ」が既に項目レベルの分岐を担当している
2. 残る用途は事実上「空のときの代替値」1 種類で、それは 2 引数の `{default:}` で足りる
3. `{if:}` は `|` の 4 つめの意味になり、正規表現の選択と衝突する（§2-3）。
   文法上の負債が用途に見合わない

`{default:}` を入れてなお「条件で分岐したい」具体例が積み上がったら、そのとき `{if:}` を設計する。
そのときは §2 の制約表がそのまま出発点になる。
`DESIGN_BASE_SYNTAX.md` の `@lastcopy` と同じ扱い ―― **用途が具体化してから再検討**。

### 3・4 を対象外にする理由

数値比較（`{if:{calc:…}|>100|大|小}`）は、`ExpressionEvaluator` に比較演算子を足すか、
`{if:}` 側に小さな条件パーサを持つかの二択になる。前者は「式は値を返す」という
`{calc:}` の性質を崩し、後者はエンジンに 2 つめの構文解析器を作ることになる。

`{elif}` は `{if:}` の入れ子で書けるが、書けたところで読めない:

```
{if:{clip}|/^http/|{if:{clip}|/github/|GitHub|Web}|テキスト}
```

多分岐が欲しくなった時点で、それは `{choice:}` か項目を分ける話になっている。

---

## 5. もし将来 `{if:}` を作るなら

決めておくべきことを、忘れないうちに書き残す。

### 文法

```
{if:値|/正規表現/|真の場合|偽の場合}
{if:値|真の場合|偽の場合}          条件を省いたら「空でない」を条件にする（3 引数）
```

3 引数形を用意すると `{default:}` の上位互換になり、記法が 2 つに割れる。
`{default:}` を先に入れるなら、`{if:}` は**4 引数だけ**にして役割を分けたほうがよい。

### 決めておくこと

- **枝の中で `{seq}` は禁止**（§2-1）。`IsUnsupportedInChoiceOption()` と同じ形で弾き、
  `ChoiceIssue` と同様に設定画面で理由を出す
- **`{input:}` `{choice:}` は許すが、通らない枝の分も聞かれる**ことを README に明記（§2-2）
- **選ばれた枝だけを展開する。** 両方展開すると `{calc:100/{clip}}` のような枝で
  0 除算が起きる。`ExpandTextTransform()` が「元の値だけ展開し、検索・置換文字はリテラル」を
  既にやっているので、部分展開の前例はある
- **条件の照合は 200 ミリ秒で打ち切る**（`RegexTimeout` を流用）
- **入れ子の深さは `MaxDepth`（8）で頭打ち**。既存の保険がそのまま効く
- **`|` は `\|` でエスケープ**（`SplitChoiceOptions()` と同じ）。
  README に「条件の正規表現で選択を使うときは `\|`」と書く

### 実装順の推奨

```
1. {default:値|代替}      2 引数。TrySplitTextTransformArguments の 2 個版を足すだけ
2. （用途が積み上がったら）{if:値|/正規表現/|真|偽}
```

`{default:}` は単独で完結し、`{if:}` を作らなくても価値が残る。
逆に `{if:}` を先に入れると `{default:}` は「`{if:}` の 3 引数形で書ける」ことになり、
記法が 2 つに割れる。順序を逆にしてはいけない。

---

## 6. Windows 実機で確認したいこと

`{default:}` を実装した場合:

- `{default:{clip:/ID-(\d+)/}|不明}` で、一致しないときに `不明` が出るか
  （＝未展開の判定が効いているか）
- `{default:{app:title}|（アプリ不明）}` で、前面アプリを取得できないときに代替が出るか
- `{default:{input:備考}|（なし）}` で、入力を空のまま確定したときに代替が出るか
  （空入力が `ClipboardCaptureSession` でどう扱われるかの確認も兼ねる）
- 設定画面のプレビューでも同じ結果になるか（プレビューはクリップボードを読むが
  `Inputs` は無いため、`{input:}` は未展開 → 常に代替値が出るはず。
  これは「代表値」として妥当か、それとも紛らわしいかの判断が要る）
