# Swap コピー 検討メモ

> **状態: 実装済み・実機確認待ち。**
> クリップボードの全形式退避、`SendInput` による Ctrl+C / Ctrl+V、
> 専用グローバルホットキー、設定 UI は実装済み。
> §5 の待ち時間と §9 の実機確認が残る。

「選択している文字と、クリップボードにある文字を入れ替える」機能の検討。

```
実行前： 選択箇所 = B          クリップボード = A
実行後： 選択箇所 = A          クリップボード = B
```

置換したい文字列 A を 1 度コピーしておけば、置き換えたい箇所 B を選んでキーを押すだけで
入れ替わる。しかも B はクリップボードに残るので、そのまま A のあった場所へ貼れる。
2 か所の値を入れ替える作業が、コピー 1 回とキー 1 回で済む。

---

## 1. 何をするか

4 段に分かれる。

| 段 | やること | 終わった時点の状態 |
| --- | --- | --- |
| 1 | いまのクリップボード A を控える | 変化なし |
| 2 | `Ctrl+C` を送り、選択していた B を控える | クリップボード = B |
| 3 | A を戻して `Ctrl+V` を送る | 選択箇所 = A、クリップボード = A |
| 4 | B をクリップボードへ載せる | クリップボード = B |

段 2 でクリップボードが変わらなければ「選択がない」と判断して、**何も書き換えずに終える**。
段 1 で A が空なら、貼り付けるものがないので開始しない。

---

## 2. トレイメニューからは実行できない

このアプリの他の機能は、トレイメニューを入口にしている。Swap コピーだけは違う。

メニューを開くには `MenuHostWindow` を前面化する必要がある（`DESIGN_HOTKEY.md` §2-1）。
前面が移った時点で、**貼り付け先のキャレットと選択が失われる**。
メニューを閉じたあとに `RestoreForeground` でウィンドウは戻せるが、
戻ってきた時点で選択が残っているかはアプリ次第で、Web ページや一部のエディタでは消える。

Swap コピーは「いま選択しているもの」が前提なので、この不確かさを受け入れられない。
**入口はグローバルホットキーだけ**にした。設定画面の「ホットキーの設定」に、
メニュー用と並べて入力欄を置く。既定は空（無効）で、設定した場合だけ登録する。

### 2-1. 押されたままの修飾キーを離す

ホットキーが `Ctrl+Alt+S` なら、`WM_HOTKEY` を受け取った時点で利用者の指はまだ
Ctrl と Alt を押している。そのまま `Ctrl+C` を送ると、アプリには `Ctrl+Alt+C` に見える。

そこで送信の前に、押されている左右の修飾キー
（`VK_LSHIFT` / `VK_RSHIFT` / `VK_LCONTROL` / `VK_RCONTROL` / `VK_LMENU` / `VK_RMENU` /
`VK_LWIN` / `VK_RWIN`）へ離上だけを送る。押し直しはしない。
利用者が実際に指を離したときに、本来の離上が改めて届く。

まとめ役の `VK_CONTROL` へ離上を送っても、実際に押されている左右のキーの状態は変わらない。
**左右を個別に見る**必要がある。

---

## 3. 段ごとに、失敗したときどうなるか

途中で失敗すると、文書とクリップボードが利用者の想定とずれる。
**どの段で失敗しても、失われるものが最小になる側へ倒す**方針で決めた。

| 失敗した段 | とる行動 | 通知 |
| --- | --- | --- |
| A を控えられない | 何もしない | Swap コピーできません |
| A が空 | 何もしない | クリップボードが空です |
| `Ctrl+C` を送れない | 何もしない | キーを送れませんでした |
| コピーされない（選択なし） | 何もしない | 文字を選んでから実行してください |
| B を控えられない | A を戻す | Swap コピーできません |
| A を戻せない | 貼り付けない | 中断しました（いまあるのは B） |
| `Ctrl+V` を送れない | B を載せずに終える | キーを送れませんでした |
| B を載せられない | そのまま終える | 貼り付けまで完了しました |

`Ctrl+V` を送れなかったときに B を載せない理由は 1 つ。
この時点でクリップボードには A が戻っている。ここで B を載せると、
**貼り付けられないまま A が消える**。利用者が持っていた内容を黙って捨てることになる。
貼り付けが起きていない以上、何も起きなかった状態に留めるほうが正しい。

成功したときの通知だけは「コピーしたときに通知を表示する」の設定に従う。
失敗と中断は、状態がずれている以上、設定にかかわらず必ず伝える。

---

## 4. 書式を保持する

段 1 と段 2 で控えるのは文字列ではなく、`IDataObject` に載っていた**全形式**。
文字列だけを覚えると、Excel の書式付きコピーやブラウザからの HTML コピーが、
入れ替えを通すだけで平文に変わってしまう。入れ替えは値を移す操作であって、
加工する操作ではない。

`ClipboardSnapshot` が形式と値の組を持ち、戻すときは新しい `DataObject` へ載せ直す。

- 1 つの形式を載せられなくても、その形式だけ飛ばして続ける
- 全形式で載せられなかった場合は、**文字列だけでも戻す**。
  1 つの形式のせいで利用者の元の内容が丸ごと消えるほうが困る
- 控えはメモリ上だけに置き、設定ファイルや履歴には書かない

### 4-1. 保存を拒む印も運ぶ

パスワード管理ソフトなどは `ExcludeClipboardContentFromMonitorProcessing` のような形式を
載せて「他のアプリで保存するな」と伝える（`DESIGN_RICH_COPY.md` §6-7）。
全形式を控えるので、この印も自然に運ばれ、戻したあとのクリップボードでも拒否は保たれる。

これらの形式は**載っていること自体に意味がある**ため、値を読めなかった場合でも
空のまま載せ直す。他の形式は、値を読めなければ落とす
（空の `HTML Format` を載せると、貼り付け先がそれを選んで何も貼らない）。

連続コピーと違い、Swap コピーは印の付いた内容を**拒まない**。
連続コピーは複数件をモードが終わるまで抱えるが、Swap コピーが持つのは 1 件・1 秒未満で、
しかもその内容は入れ替えの結果としてクリップボードへ戻る。
利用者がその場で押した 1 回の操作の範囲を超えていない。

---

## 5. 待ち時間

**この機能でいちばん脆いのはここ。** Windows は「貼り付けが終わった」を教えてくれない。

| 待ち | 長さ | 根拠 |
| --- | --- | --- |
| `Ctrl+C` の結果を待つ | 最大 800ms | クリップボードの更新回数を 20ms ごとに見る。変われば即進む |
| 更新に気づいてから読むまで | 60ms | 1 回のコピーで形式を順に載せるアプリがある（`DESIGN_COPY_INTENT.md` §8） |
| `Ctrl+V` を送ってから B を載せるまで | 350ms | **時間で待つしかない**。実測で詰めるならここ |

最後の 350ms が短すぎると、クリップボードを遅れて読むアプリ
（ブラウザや Electron 製のエディタ）が、入れ替えた後の B を貼ってしまう。
長すぎると、続けて操作したときに待たされる。

待っているあいだに他のアプリがコピーした場合は、更新回数が変わったことで気づける。
そのときは B を載せず、利用者が新しくコピーした内容を残す。
いま欲しいのはそちらである可能性が高い。

`Thread.Sleep` では待たない。クリップボードは STA スレッドからしか触れないので
UI スレッドで動かすことになり、そこで止めるとメニューも通知も固まる。
`await Task.Delay` で譲る。

---

## 6. 他の機能との関係

- **作業モードの実行中は受け付けない。**
  連続コピー＆ペーストはクリップボード更新と `Ctrl+V` を自分で使う。
  重ねると収集内容が黙って壊れる。`ActionSessionManager.HasActiveSession` で断る
- **重ねて実行しない。** 1 回の入れ替えのあいだにもう一度押されても無視する。
  `Ctrl+C` と `Ctrl+V` が混ざると、どちらの段の結果か分からなくなる
- **自分の書き込みは自分で分かる。** 段 3・4 の書き込みは `ClipboardService` が
  自己書き込みとして記録するので、連続コピーの収集には混ざらない
- **送ったキーには印が付く。** `SendInput` の `dwExtraInfo` に `MTSW` を入れる。
  連続コピー側は `LLKHF_INJECTED` で自動化された入力を既に除外している

---

## 7. 設定

```jsonc
{
  // 空欄なら無効。書き方はメニュー用のホットキーと同じ
  "SwapCopyHotKey": "Ctrl+Alt+S"
}
```

- 解釈は `HotKeyGesture.TryParse` を共用する。Ctrl / Alt / Win のいずれかが必要で、
  文字を入力しないキー（無変換・変換・アプリケーション・Pause・`F13`〜`F24`）は単体で指定できる
  （線引きは `DESIGN_HOTKEY.md` §4-1）
- **メニュー用と同じキーは保存時に断る。** 同じキーを 2 つ登録すると
  後から登録するほうが必ず失敗し、どちらが効いているのか利用者には見えない
- `RegisterHotKey` の識別子は用途ごとに分ける（`GlobalHotKeyIds`）

---

## 8. 置き場所

| ファイル | 役割 |
| --- | --- |
| `Services/ClipboardSnapshot.cs` | 形式と値の組を持つだけの控え |
| `Services/ClipboardService.cs` | 控える・戻す（既存の再試行と自己書き込み記録を共用） |
| `Services/InputInjector.cs` | `SendInput` で Ctrl+C / Ctrl+V、修飾キーの解放 |
| `Services/SwapCopy.cs` | 4 段の手順と待ち時間 |
| `TrayIconManager.SwapCopy.cs` | ホットキー登録、多重実行の抑止、通知 |

`SwapCopy` は状態を持たないので `ActionSessionManager` には載せない。
1 秒未満で終わる操作をメニュー先頭へ昇格させても、表示が間に合わない。

---

## 9. Windows 実機で確認したいこと

自動テストで裏取りできるのは、ホットキーの解釈と設定の往復だけ。
残りはすべて実機で確かめる。

| # | 内容 | 外れたときに起きること |
| --- | --- | --- |
| 9-1 | メモ帳・VS Code・Word で入れ替わるか | 機能そのものが成立しない |
| 9-2 | ブラウザの入力欄で、350ms が足りるか | 入れ替えた後の内容が貼られる（§5） |
| 9-3 | Excel のセルで書式が保たれるか | §4 の全形式退避が効いていない |
| 9-4 | ホットキーの修飾キーを押したままでも動くか | §2-1 の解放が効いていない |
| 9-5 | 選択が無い状態で押しても、クリップボードが壊れないか | 元の内容を失う |
| 9-6 | 管理者権限のアプリが前面のとき、通知が出るか | 黙って何も起きない |
| 9-7 | 連続コピー中に押して、収集が壊れないか | 収集データが黙って壊れる |

9-2 と 9-5 は、外れたときに利用者のデータが消える。**最初に確かめる。**

---

## 10. いまできないこと

- **貼り付けの完了を待てない。** 時間で待つしかない（§5）
- **選択の有無を先に知る手立てがない。** `Ctrl+C` を送って、
  クリップボードが変わったかどうかで後から判断している。
  選択が無い状態で「行全体をコピーする」アプリでは、その行が入れ替え対象になる
- **元に戻す操作は用意していない。** 貼り付け先の `Ctrl+Z` に任せる。
  クリップボードには入れ替え前の選択内容が残っているので、
  戻したあとにもう一度貼れば元の並びへ復せる
- **トレイメニューには出さない**（§2）。「アクションの表示」の一覧にも並ばない
