<Window x:Class="MyTaskTray.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:MyTaskTray"
        Title="MyTaskTray の設定"
        Height="800" Width="980"
        MinHeight="560" MinWidth="840"
        WindowStartupLocation="CenterScreen"
        FontFamily="Yu Gothic UI" FontSize="13"
        Background="{DynamicResource Brush.Window.Bg}"
        Foreground="{DynamicResource Brush.Text}"
        PreviewKeyDown="OnWindowPreviewKeyDown"
        Closing="OnWindowClosing">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <local:NotBoolToVisibilityConverter x:Key="NotBoolToVis" />

        <Style x:Key="CategoryPalette" TargetType="ListBox">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource Brush.Text}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CategoryPaletteItem" TargetType="ListBoxItem">
            <Setter Property="Margin" Value="0,0,5,5" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Tile" CornerRadius="5" BorderThickness="1"
                                BorderBrush="{DynamicResource Brush.Border}"
                                Background="Transparent">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Tile" Property="Background"
                                        Value="{DynamicResource Brush.Hover}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Tile" Property="BorderBrush"
                                        Value="{DynamicResource Brush.Accent}" />
                                <Setter TargetName="Tile" Property="BorderThickness" Value="2" />
                                <Setter TargetName="Tile" Property="Background"
                                        Value="{DynamicResource Brush.Selected}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 一覧の 1 行。通常項目は 2 行表示、区切り線は線として描く -->
        <DataTemplate x:Key="ClipItemTemplate">
            <Grid>
                <Grid Visibility="{Binding IsNotSeparator, Converter={StaticResource BoolToVis}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="{Binding DisplayLabel}" FontWeight="SemiBold"
                                   TextTrimming="CharacterEllipsis" />
                        <TextBlock Text="{Binding TextPreview}" Margin="0,2,0,0" FontSize="11.5"
                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                   TextTrimming="CharacterEllipsis" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top" Margin="4,1,0,0">
                        <Border Style="{StaticResource Badge}"
                                ToolTip="書式付きでコピーします"
                                Visibility="{Binding HasFormat, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="{Binding FormatLabel}" Style="{StaticResource BadgeText}" />
                        </Border>
                        <Border Style="{StaticResource SeqBadge}"
                                Visibility="{Binding UsesSequence, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="連番" Style="{StaticResource SeqBadgeText}" />
                        </Border>
                        <Border Style="{StaticResource Badge}"
                                Visibility="{Binding UsesInputs, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="入力" Style="{StaticResource BadgeText}" />
                        </Border>
                        <Border Style="{StaticResource Badge}"
                                Visibility="{Binding HasSmartCondition, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="スマート" Style="{StaticResource BadgeText}" />
                        </Border>
                        <Border Style="{StaticResource Badge}"
                                ToolTip="クリップボードの各行に適用します"
                                Visibility="{Binding UsesEachLine, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="複数行" Style="{StaticResource BadgeText}" />
                        </Border>
                        <Border Style="{StaticResource Badge}"
                                ToolTip="前面のアプリによって表示が変わります"
                                Visibility="{Binding HasAppCondition, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="アプリ" Style="{StaticResource BadgeText}" />
                        </Border>
                        <Border Style="{StaticResource Badge}"
                                Visibility="{Binding HasCategory, Converter={StaticResource BoolToVis}}">
                            <TextBlock Text="{Binding Category}" Style="{StaticResource BadgeText}"
                                       MaxWidth="90" TextTrimming="CharacterEllipsis" />
                        </Border>
                    </StackPanel>
                </Grid>

                <Grid Height="20" Visibility="{Binding IsSeparator, Converter={StaticResource BoolToVis}}">
                    <Border Height="1" VerticalAlignment="Center"
                            Background="{DynamicResource Brush.Separator}" />
                    <TextBlock Text="区切り線" FontSize="10.5" Padding="8,0" HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Background="{DynamicResource Brush.Surface}"
                               Foreground="{DynamicResource Brush.Text.Secondary}" />
                </Grid>
            </Grid>
        </DataTemplate>

        <!-- 実際のメニュー構造に寄せたアウトラインの 1 行 -->
        <DataTemplate x:Key="MenuOutlineRowTemplate">
            <Grid>
                <!-- 通常メニュー / この内容でできること -->
                <Border Padding="4,7,4,5" Margin="0,2,0,1"
                        BorderBrush="{DynamicResource Brush.Border}" BorderThickness="0,0,0,1"
                        Visibility="{Binding IsSection, Converter={StaticResource BoolToVis}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding SectionTitle}" FontWeight="SemiBold" />
                        <TextBlock Grid.Column="1" Text="{Binding SectionHint}" FontSize="10.5"
                                   Foreground="{DynamicResource Brush.Text.Secondary}" />
                    </Grid>
                </Border>

                <!-- カテゴリ見出し -->
                <Grid Margin="2,2,0,2"
                      Visibility="{Binding IsCategory, Converter={StaticResource BoolToVis}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="4" />
                        <ColumnDefinition Width="24" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Width="3" Height="22" CornerRadius="2"
                            HorizontalAlignment="Left"
                            Background="{Binding CategoryColorBrush}" />
                    <Button Grid.Column="1" Content="{Binding ExpandGlyph}" Width="22" Height="22"
                            MinHeight="0" Padding="0" Style="{StaticResource SubtleButton}"
                            ToolTip="カテゴリを折りたたむ / 展開する"
                            Click="OnToggleCategory" />
                    <Grid Grid.Column="2" Width="22" Height="22" Margin="3,0,1,0"
                          Visibility="{Binding HasCategoryIcon, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="{Binding CategoryIconGlyph}"
                                   FontFamily="{Binding CategoryIconFontFamily}" FontSize="14"
                                   Foreground="{Binding CategoryIconBrush}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Grid>
                    <TextBlock Grid.Column="3" Text="{Binding Category}" Margin="5,0,8,0"
                               VerticalAlignment="Center" FontWeight="SemiBold"
                               TextTrimming="CharacterEllipsis" />
                    <Border Grid.Column="4" Style="{StaticResource Badge}" VerticalAlignment="Center">
                        <TextBlock Text="{Binding ItemCountText}" Style="{StaticResource BadgeText}" />
                    </Border>
                </Grid>

                <!-- 項目。カテゴリ配下だけ字下げする -->
                <Border Visibility="{Binding IsItem, Converter={StaticResource BoolToVis}}">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Margin" Value="0" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsCategoryItem}" Value="True">
                                    <Setter Property="Margin" Value="27,0,0,0" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <ContentPresenter Content="{Binding Item}"
                                      ContentTemplate="{StaticResource ClipItemTemplate}" />
                </Border>
            </Grid>
        </DataTemplate>

        <!-- 組み込みアクションの表示設定 1 件 -->
        <DataTemplate x:Key="ActionSettingTemplate">
            <CheckBox IsChecked="{Binding IsVisible}" Margin="2,5"
                      HorizontalContentAlignment="Stretch">
                <Grid Width="400">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,12,0">
                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding Description}" Margin="0,2,0,0"
                                   Style="{StaticResource Hint}" TextWrapping="Wrap" />
                    </StackPanel>
                    <TextBlock Grid.Column="1" Text="{Binding Group}"
                               VerticalAlignment="Top" Margin="6,1,0,0"
                               Foreground="{DynamicResource Brush.Text.Secondary}" />
                </Grid>
            </CheckBox>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- ヘッダー -->
        <StackPanel Grid.Row="0" Margin="18,16,18,0">
            <TextBlock Text="メニュー構成" FontSize="18" FontWeight="SemiBold" />
            <TextBlock Margin="0,3,0,0" Style="{StaticResource Hint}"
                       Text="カテゴリと項目を、トレイメニューに表示される構造で編集します。ドラッグして移動・並べ替えできます。" />
        </StackPanel>

        <!-- 本体 -->
        <Grid Grid.Row="1" Margin="18,14,18,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="ListColumn" Width="330" MinWidth="250" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" MinWidth="400" />
            </Grid.ColumnDefinitions>

            <!-- 左: 項目一覧 -->
            <Border Grid.Column="0" Style="{StaticResource Card}">
                <DockPanel Margin="10">
                    <!-- 検索 -->
                    <Grid DockPanel.Dock="Top" Margin="0,0,0,8">
                        <TextBox x:Name="FilterBox" Padding="8,5,28,5"
                                 Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Text="項目を検索（Ctrl+F）" Margin="10,0,0,0" VerticalAlignment="Center"
                                   IsHitTestVisible="False"
                                   Foreground="{DynamicResource Brush.Text.Disabled}"
                                   Visibility="{Binding HasFilter, Converter={StaticResource NotBoolToVis}}" />
                        <Button Content="✕" Width="22" Height="22" MinHeight="0" Padding="0" FontSize="10"
                                HorizontalAlignment="Right" Margin="0,0,4,0"
                                Style="{StaticResource SubtleButton}"
                                ToolTip="検索をクリア" Click="OnClearFilter"
                                Visibility="{Binding HasFilter, Converter={StaticResource BoolToVis}}" />
                    </Grid>

                    <!-- 操作ボタン -->
                    <Grid DockPanel.Dock="Bottom" Margin="0,8,0,0">
                        <StackPanel Orientation="Horizontal">
                            <Button Content="追加" Padding="10,4" Click="OnAddItem"
                                    ToolTip="新しい項目を追加（Ctrl+N）" />
                            <Button Content="区切り線" Padding="10,4" Margin="5,0,0,0" Click="OnAddSeparator"
                                    ToolTip="メニューに水平線を入れる" />
                            <Button Content="複製" Padding="10,4" Margin="5,0,0,0" Click="OnDuplicateItem"
                                    IsEnabled="{Binding CanDuplicate}" ToolTip="選択項目を複製（Ctrl+D）" />
                            <Button Content="削除" Padding="10,4" Margin="5,0,0,0" Click="OnDeleteItem"
                                    IsEnabled="{Binding CanDelete}" ToolTip="選択した項目またはカテゴリを削除（Delete）" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="▲" Style="{StaticResource IconButton}" Click="OnMoveUp"
                                    IsEnabled="{Binding CanReorder}" ToolTip="上へ移動（Alt+↑）" />
                            <Button Content="▼" Style="{StaticResource IconButton}" Margin="4,0,0,0" Click="OnMoveDown"
                                    IsEnabled="{Binding CanReorder}" ToolTip="下へ移動（Alt+↓）" />
                        </StackPanel>
                    </Grid>

                    <TextBlock DockPanel.Dock="Bottom" Margin="2,8,0,0" Style="{StaticResource Hint}"
                               Text="{Binding StatusText}" />

                    <ListBox x:Name="ItemsList"
                             ItemsSource="{Binding MenuOutline}"
                             SelectedItem="{Binding SelectedOutlineRow, Mode=TwoWay}"
                             ItemTemplate="{StaticResource MenuOutlineRowTemplate}"
                             AllowDrop="True"
                             SelectionChanged="OnSelectionChanged"
                             PreviewMouseLeftButtonDown="OnListPreviewMouseLeftButtonDown"
                             PreviewMouseMove="OnListPreviewMouseMove"
                             DragOver="OnListDragOver"
                             DragLeave="OnListDragLeave"
                             Drop="OnListDrop"
                             PreviewKeyDown="OnListPreviewKeyDown" />
                </DockPanel>
            </Border>

            <GridSplitter Grid.Column="1" Width="10" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />

            <!-- 右: 選択項目の編集 -->
            <Border Grid.Column="2" Style="{StaticResource Card}">
                <Grid>
                    <TextBlock Text="{Binding EditorHint}" MaxWidth="320" Margin="24"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               TextAlignment="Center" TextWrapping="Wrap"
                               Foreground="{DynamicResource Brush.Text.Secondary}"
                               Visibility="{Binding ShowEditorHint, Converter={StaticResource BoolToVis}}" />

                    <!-- カテゴリ見出しを選んだときの一括編集 -->
                    <Grid Margin="16"
                          Visibility="{Binding IsCategoryEditable, Converter={StaticResource BoolToVis}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="カテゴリの設定" Style="{StaticResource SectionTitle}"
                                   Margin="0,0,0,12" />

                        <StackPanel Grid.Row="1">
                            <TextBlock Text="カテゴリ名" Style="{StaticResource FieldLabel}" />
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="CategoryNameBox" Grid.Column="0"
                                         Text="{Binding CategoryNameDraft, UpdateSourceTrigger=PropertyChanged}"
                                         KeyDown="OnCategoryNameKeyDown" />
                                <Button Grid.Column="1" Content="名前を変更" Margin="8,0,0,0"
                                        Padding="12,4" Click="OnRenameCategory" />
                            </Grid>
                        </StackPanel>

                        <StackPanel Grid.Row="2" Margin="0,14,0,0">
                            <Grid>
                                <TextBlock Text="色" Style="{StaticResource FieldLabel}" />
                                <Button HorizontalAlignment="Right" Content="装飾をリセット"
                                        Padding="8,2" MinHeight="24" Style="{StaticResource SubtleButton}"
                                        IsEnabled="{Binding HasSelectedCategoryAppearance}"
                                        Click="OnResetCategoryAppearance" />
                            </Grid>
                            <ListBox Style="{StaticResource CategoryPalette}"
                                     ItemContainerStyle="{StaticResource CategoryPaletteItem}"
                                     ItemsSource="{Binding CategoryColorOptions}"
                                     SelectedValuePath="Value"
                                     SelectedValue="{Binding SelectedCategoryColor, Mode=TwoWay}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Width="30" Height="30" ToolTip="{Binding Name}">
                                            <Ellipse Width="16" Height="16" Fill="{Binding Brush}"
                                                     Stroke="{DynamicResource Brush.Border.Strong}"
                                                     StrokeThickness="1" />
                                            <TextBlock Text="—" Foreground="{DynamicResource Brush.Text}"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center" FontSize="13">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding HasValue}" Value="False">
                                                                <Setter Property="Visibility" Value="Visible" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <TextBlock Text="アイコン" Margin="0,4,0,0"
                                       Style="{StaticResource FieldLabel}" />
                            <ListBox Style="{StaticResource CategoryPalette}"
                                     ItemContainerStyle="{StaticResource CategoryPaletteItem}"
                                     ItemsSource="{Binding CategoryIconOptions}"
                                     SelectedValuePath="Value"
                                     SelectedValue="{Binding SelectedCategoryIcon, Mode=TwoWay}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Width="30" Height="30" ToolTip="{Binding Name}">
                                            <TextBlock Text="{Binding Glyph}" FontFamily="{Binding FontFamily}"
                                                       Foreground="{DynamicResource Brush.Text}"
                                                       FontSize="15" HorizontalAlignment="Center"
                                                       VerticalAlignment="Center" />
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </StackPanel>

                        <Border Grid.Row="3" Margin="0,9,0,0" Padding="12,10" CornerRadius="6"
                                Background="{DynamicResource Brush.Surface.Alt}"
                                BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1">
                            <StackPanel>
                                <TextBlock>
                                    <Run Text="{Binding SelectedCategoryItemCount, Mode=OneWay}" />
                                    <Run Text=" 件の項目がこのカテゴリに属しています。" />
                                </TextBlock>
                                <TextBlock Text="{Binding SelectedCategoryLocation}" Margin="0,4,0,0"
                                           Style="{StaticResource Hint}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <Button Grid.Row="4" Content="カテゴリを削除…" HorizontalAlignment="Left"
                                Margin="0,14,0,0" Padding="12,4" Click="OnRemoveCategory"
                                ToolTip="中の項目は削除せず、トップレベルへ移します" />
                    </Grid>

                    <Grid Margin="16" Visibility="{Binding IsItemEditable, Converter={StaticResource BoolToVis}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" MinHeight="100" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="項目の内容" Style="{StaticResource SectionTitle}"
                                   Margin="0,0,0,12" />

                        <StackPanel Grid.Row="1" Margin="0,0,0,12">
                            <TextBlock Text="メニューに表示する名前" Style="{StaticResource FieldLabel}" />
                            <TextBox x:Name="NameBox"
                                     Text="{Binding SelectedItem.Name, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>

                        <StackPanel Grid.Row="2" Margin="0,0,0,12">
                            <TextBlock Style="{StaticResource FieldLabel}"
                                       Text="配置先（空欄ならトップレベル。入力した名前のサブメニューに入ります）" />
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="CategoryBox" Grid.Column="0"
                                         Text="{Binding SelectedItem.Category, UpdateSourceTrigger=PropertyChanged}" />
                                <Button x:Name="CategoryPickButton" Grid.Column="1" Content="▾" Margin="5,0,0,0"
                                        Style="{StaticResource IconButton}"
                                        ToolTip="配置先を選ぶ" Click="OnOpenCategoryPopup" />
                            </Grid>
                        </StackPanel>

                        <StackPanel Grid.Row="3">

                            <!-- スマートアクションの条件 -->
                            <Expander x:Name="SmartActionExpander" Margin="0,0,0,10"
                                      Foreground="{DynamicResource Brush.Text}"
                                      Header="スマートアクション（クリップボードに応じて表示）">
                                <Border Margin="0,6,0,0" Padding="10,9" CornerRadius="6"
                                        Background="{DynamicResource Brush.Surface.Alt}"
                                        BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1"
                                        TextElement.Foreground="{DynamicResource Brush.Text}">
                                    <StackPanel>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="180" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="表示条件" Margin="0,0,10,0"
                                                       VerticalAlignment="Center" />
                                            <ComboBox Grid.Column="1"
                                                      ItemsSource="{Binding ClipboardMatchOptions}"
                                                      DisplayMemberPath="Name" SelectedValuePath="Kind"
                                                      SelectedValue="{Binding SelectedItem.ClipboardCondition, Mode=TwoWay}" />
                                        </Grid>

                                        <StackPanel Margin="0,8,0,0"
                                                    Visibility="{Binding SelectedItem.IsRegexCondition, Converter={StaticResource BoolToVis}}">
                                            <TextBlock Text="正規表現" Style="{StaticResource FieldLabel}" />
                                            <TextBox x:Name="ClipboardPatternBox" FontFamily="Consolas, Yu Gothic UI"
                                                     Text="{Binding SelectedItem.ClipboardPattern, UpdateSourceTrigger=PropertyChanged}"
                                                     ToolTip="名前付きキャプチャは {match:名前} でコピー文字列に差し込めます" />
                                        </StackPanel>

                                        <CheckBox Margin="0,10,0,0"
                                                  Content="複数行にも適用する（各行を 1 件として、行の数だけコピーする）"
                                                  IsChecked="{Binding SelectedItem.ApplyToEachLine, Mode=TwoWay}"
                                                  Visibility="{Binding SelectedItem.CanApplyToEachLine, Converter={StaticResource BoolToVis}}"
                                                  ToolTip="20 行をコピーしてこの項目を選ぶと、20 行ぶんの結果が改行でつながってコピーされます。条件に合わない行（見出し行や空行）は飛ばします" />

                                        <TextBlock Margin="0,7,0,0" TextWrapping="Wrap" FontSize="11"
                                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                                   Text="{Binding ClipboardConditionStatus}" />
                                    </StackPanel>
                                </Border>
                            </Expander>

                            <!-- 前面アプリによる絞り込み -->
                            <Expander x:Name="AppContextExpander" Margin="0,0,0,10"
                                      Foreground="{DynamicResource Brush.Text}"
                                      Header="表示するアプリ（前面のウィンドウに応じて表示）">
                                <Border Margin="0,6,0,0" Padding="10,9" CornerRadius="6"
                                        Background="{DynamicResource Brush.Surface.Alt}"
                                        BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1"
                                        TextElement.Foreground="{DynamicResource Brush.Text}">
                                    <StackPanel>
                                        <TextBlock Style="{StaticResource FieldLabel}"
                                                   Text="アプリ（実行ファイル名。カンマ区切りで複数指定できます）" />
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBox x:Name="AppProcessBox" Grid.Column="0"
                                                     FontFamily="Consolas, Yu Gothic UI"
                                                     Text="{Binding SelectedItem.AppProcess, UpdateSourceTrigger=PropertyChanged}"
                                                     ToolTip="例: chrome.exe, msedge.exe（.exe は省略できます）" />
                                            <Button x:Name="AppPickButton" Grid.Column="1" Content="▾" Margin="5,0,0,0"
                                                    Style="{StaticResource IconButton}"
                                                    IsEnabled="{Binding HasKnownApps}"
                                                    ToolTip="直近に前面だったアプリから選ぶ" Click="OnOpenAppPopup" />
                                        </Grid>

                                        <TextBlock Margin="0,8,0,0" Style="{StaticResource FieldLabel}"
                                                   Text="ウィンドウタイトル（正規表現。空欄ならタイトルを見ません）" />
                                        <TextBox x:Name="AppTitleBox" FontFamily="Consolas, Yu Gothic UI"
                                                 Text="{Binding SelectedItem.AppTitlePattern, UpdateSourceTrigger=PropertyChanged}"
                                                 ToolTip="例: /issues/ （タイトルは言語やバージョンで変わることがあります）" />

                                        <TextBlock Margin="0,7,0,0" TextWrapping="Wrap" FontSize="11"
                                                   Foreground="{DynamicResource Brush.Text.Secondary}"
                                                   Text="{Binding AppConditionStatus}" />
                                    </StackPanel>
                                </Border>
                            </Expander>

                        </StackPanel>

                        <Grid Grid.Row="4" Margin="0,0,0,4">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="クリップボードにコピーする文字列"
                                       VerticalAlignment="Center"
                                       Style="{StaticResource FieldLabel}" Margin="0" />

                            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right">
                                <TextBlock Text="形式" VerticalAlignment="Center" Margin="0,0,6,0"
                                           Foreground="{DynamicResource Brush.Text.Secondary}" />
                                <ComboBox x:Name="FormatBox" Width="120" VerticalAlignment="Center"
                                          ItemsSource="{Binding ClipFormatOptions}"
                                          DisplayMemberPath="Name" SelectedValuePath="Format"
                                          SelectedValue="{Binding SelectedItem.Format, Mode=TwoWay}"
                                          ToolTip="貼り付け先によって、この形式に応じた見え方になります" />
                                <Button x:Name="InsertButton" Content="差し込みを挿入 ▾" Margin="8,0,0,0"
                                        Padding="10,3" MinHeight="26" Click="OnOpenInsertPopup"
                                        ToolTip="{}{date} などの差し込みをカーソル位置に挿入します" />
                            </StackPanel>

                            <TextBlock Grid.Row="1" Margin="0,5,0,0" TextWrapping="Wrap" FontSize="11"
                                       Foreground="{DynamicResource Brush.Text.Secondary}"
                                       Text="{Binding ClipFormatStatus}" />
                        </Grid>

                        <TextBox Grid.Row="5" x:Name="TextBox_Content"
                                 AcceptsReturn="True" AcceptsTab="False" TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 VerticalContentAlignment="Top"
                                 FontFamily="Consolas, Yu Gothic UI"
                                 Text="{Binding SelectedItem.Text, UpdateSourceTrigger=PropertyChanged}" />

                        <!-- 連番の設定（{seq} を使っているときだけ表示） -->
                        <Border Grid.Row="6" Margin="0,12,0,0" Padding="12,10" CornerRadius="6"
                                Background="{DynamicResource Brush.Seq.Bg}"
                                BorderBrush="{DynamicResource Brush.Seq.Border}"
                                BorderThickness="1"
                                Visibility="{Binding IsSequenceVisible, Converter={StaticResource BoolToVis}}">
                            <StackPanel>
                                <TextBlock Text="連番の設定" FontWeight="SemiBold" Margin="0,0,0,8"
                                           Foreground="{DynamicResource Brush.Seq.Text}" />
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="次の番号" VerticalAlignment="Center"
                                               Foreground="{DynamicResource Brush.Seq.Text}" />
                                    <TextBox Width="80" Margin="8,0,18,0"
                                             PreviewTextInput="OnIntegerTextInput"
                                             DataObject.Pasting="OnIntegerPasting"
                                             LostFocus="OnIntegerLostFocus"
                                             Text="{Binding SelectedItem.SequenceValue, UpdateSourceTrigger=PropertyChanged}" />
                                    <TextBlock Text="増分" VerticalAlignment="Center"
                                               Foreground="{DynamicResource Brush.Seq.Text}" />
                                    <TextBox Width="64" Margin="8,0,18,0"
                                             ToolTip="負の値を入れるとカウントダウンになります"
                                             PreviewTextInput="OnIntegerTextInput"
                                             DataObject.Pasting="OnIntegerPasting"
                                             LostFocus="OnIntegerLostFocus"
                                             Text="{Binding SelectedItem.SequenceStep, UpdateSourceTrigger=PropertyChanged}" />
                                    <Button Content="1 に戻す" Padding="10,3" MinHeight="26" Click="OnResetSequence" />
                                </StackPanel>
                                <TextBlock Margin="0,8,0,0" TextWrapping="Wrap" FontSize="11"
                                           Foreground="{DynamicResource Brush.Seq.Text}"
                                           Text="コピーするたびに番号が増分だけ進み、設定ファイルに保存されます。" />
                            </StackPanel>
                        </Border>

                        <!-- プレビュー -->
                        <StackPanel Grid.Row="7" Margin="0,12,0,0">
                            <!-- 選択肢の内訳と書き方の誤り（{choice:…} を使っているときだけ表示） -->
                            <TextBlock Margin="0,0,0,8" TextWrapping="Wrap" FontSize="11"
                                       Foreground="{DynamicResource Brush.Text.Secondary}"
                                       Visibility="{Binding IsChoiceVisible, Converter={StaticResource BoolToVis}}"
                                       Text="{Binding ChoiceStatus}" />

                            <Grid Margin="0,0,0,4">
                                <TextBlock Text="プレビュー（実際にコピーされる文字列）" VerticalAlignment="Center"
                                           Style="{StaticResource FieldLabel}" Margin="0" />
                                <Button Content="コピーして試す" HorizontalAlignment="Right" Padding="10,3" MinHeight="26"
                                        Style="{StaticResource SubtleButton}" Click="OnCopyPreview"
                                        ToolTip="プレビューの内容をクリップボードにコピーします（連番は進みません）" />
                            </Grid>
                            <TextBox x:Name="PreviewBox" Height="68" IsReadOnly="True"
                                     TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                                     Foreground="{DynamicResource Brush.Preview.Text}"
                                     Text="{Binding Preview, Mode=OneWay}" />
                        </StackPanel>
                    </Grid>
                </Grid>
            </Border>
        </Grid>

        <!-- フッター -->
        <Grid Grid.Row="2" Margin="18,14,18,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <WrapPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,12,0">
                <CheckBox Content="コピーしたときに通知を表示する" VerticalAlignment="Center"
                          IsChecked="{Binding ShowCopyNotification}" />
                <Button x:Name="HotKeyButton" Content="ホットキーの設定" Margin="16,0,0,0"
                        Style="{StaticResource SubtleButton}" Click="OnOpenHotKeySettings"
                        ToolTip="トレイメニューの表示と Swap コピーのグローバルホットキーを設定します" />
                <Button x:Name="ActionSettingsButton" Content="アクションの表示" Margin="8,0,0,0"
                        Style="{StaticResource SubtleButton}" Click="OnOpenActionSettings"
                        IsEnabled="{Binding HasActionSettings}"
                        ToolTip="組み込みアクションの表示と、表示されるメニューを確認します" />
                <Button x:Name="SequentialCaptureButton" Content="連続コピーの設定" Margin="8,0,0,0"
                        Style="{StaticResource SubtleButton}" Click="OnOpenSequentialCaptureSettings"
                        ToolTip="連続コピーが 1 件として集める操作の範囲を決めます" />
                <Button x:Name="SprintButton" Content="スプリントの設定" Margin="8,0,0,0"
                        Style="{StaticResource SubtleButton}" Click="OnOpenSprintSettings"
                        ToolTip="{}{date@sprint} などが参照する基準日と長さを決めます" />
                <Button x:Name="FolderButton" Content="設定フォルダーを開く" Margin="8,0,0,0"
                        Style="{StaticResource SubtleButton}" Click="OnOpenFolder" />
            </WrapPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="キャンセル" Width="104" Click="OnCancel" IsCancel="True" />
                <Button Content="保存して閉じる" Width="132" Margin="8,0,0,0"
                        Style="{StaticResource AccentButton}" Click="OnSave" IsDefault="True" />
            </StackPanel>
        </Grid>

        <!-- グローバルホットキー設定パネル -->
        <Popup x:Name="HotKeyPopup" StaysOpen="False" AllowsTransparency="True" Focusable="True"
               Placement="Top" PlacementTarget="{Binding ElementName=HotKeyButton}"
               VerticalOffset="-4" HorizontalOffset="-10"
               PreviewKeyDown="OnPopupPreviewKeyDown">
            <Border Style="{StaticResource PopupCard}" Width="420" Margin="10">
                <StackPanel Margin="14">
                    <TextBlock Text="メニューを表示するホットキー" FontWeight="SemiBold" />
                    <TextBlock Margin="0,4,0,10" Style="{StaticResource Hint}" TextWrapping="Wrap"
                               Text="Ctrl+Alt+V のように入力します。空欄なら無効です。通常の文字入力を妨げないよう、Ctrl / Alt / Win のいずれかを必ず含めてください。「無変換」だけは単体で指定できます。この欄で無変換キーを押すか muhenkan と打つと入ります（登録しているあいだ、無変換キー本来の働きは使えなくなります）。" />
                    <!--
                        日本語入力が有効なままだと「Ｃｔｒｌ＋Ｖ」のような全角になり、
                        見た目はほとんど同じなのに解釈だけ失敗する。この欄では IME を切る。
                        （それでも全角が入りうるため、解釈側でも半角へ揃えている）
                    -->
                    <TextBox x:Name="HotKeyBox"
                             InputMethod.IsInputMethodEnabled="False"
                             PreviewKeyDown="OnHotKeyBoxPreviewKeyDown"
                             Text="{Binding MenuHotKey, UpdateSourceTrigger=PropertyChanged}"
                             ToolTip="キーは A〜Z、0〜9、F1〜F24、無変換 を指定できます" />
                    <TextBlock Margin="0,8,0,0" Style="{StaticResource Hint}" TextWrapping="Wrap"
                               Text="{Binding MenuHotKeyStatus}" />

                    <Border Height="1" Margin="0,14,0,12"
                            Background="{DynamicResource Brush.Separator}" />

                    <TextBlock Text="Swap コピーのホットキー" FontWeight="SemiBold" />
                    <TextBlock Margin="0,4,0,10" Style="{StaticResource Hint}" TextWrapping="Wrap"
                               Text="選択している文字をコピーし、その場へ元のクリップボードの内容を貼り付けます。次に貼り付けられるのは、選択していた文字です。空欄なら無効です。書き方はメニュー用と同じで、メニュー用と同じキーは指定できません。" />
                    <TextBox x:Name="SwapCopyHotKeyBox"
                             InputMethod.IsInputMethodEnabled="False"
                             PreviewKeyDown="OnSwapCopyHotKeyBoxPreviewKeyDown"
                             Text="{Binding SwapCopyHotKey, UpdateSourceTrigger=PropertyChanged}"
                             ToolTip="キーは A〜Z、0〜9、F1〜F24、無変換 を指定できます" />
                    <TextBlock Margin="0,8,0,0" Style="{StaticResource Hint}" TextWrapping="Wrap"
                               Text="{Binding SwapCopyHotKeyStatus}" />
                </StackPanel>
            </Border>
        </Popup>

        <!-- 組み込みアクションの表示設定 -->
        <Popup x:Name="ActionSettingsPopup" StaysOpen="False" AllowsTransparency="True" Focusable="True"
               Placement="Top" PlacementTarget="{Binding ElementName=ActionSettingsButton}"
               VerticalOffset="-4" HorizontalOffset="-10"
               PreviewKeyDown="OnPopupPreviewKeyDown">
            <Border Style="{StaticResource PopupCard}" Width="470" Margin="10">
                <StackPanel Margin="14">
                    <TextBlock Text="アクションの表示" FontWeight="SemiBold" />
                    <TextBlock Margin="0,4,0,10" Style="{StaticResource Hint}" TextWrapping="Wrap"
                               Text="チェックした機能を、下に示すメニューへ表示します。実行中の作業は、完了またはキャンセルまで先頭に表示されます。" />

                    <ScrollViewer MaxHeight="320" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <StackPanel Visibility="{Binding HasWorkToolActionSettings, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="作業ツール" FontSize="14" FontWeight="SemiBold" />
                                <TextBlock Margin="0,2,0,3" Style="{StaticResource Hint}" TextWrapping="Wrap"
                                           Text="通常時に、メニュー下部の「作業ツール」へ表示されます。" />
                                <ItemsControl ItemsSource="{Binding WorkToolActionSettings}"
                                              ItemTemplate="{StaticResource ActionSettingTemplate}" />
                            </StackPanel>

                            <Border Height="1" Margin="0,10,0,10"
                                    Background="{DynamicResource Brush.Separator}" />

                            <StackPanel Visibility="{Binding HasContextualActionSettings, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="この内容でできること" FontSize="14" FontWeight="SemiBold" />
                                <TextBlock Margin="0,2,0,3" Style="{StaticResource Hint}" TextWrapping="Wrap"
                                           Text="クリップボードが条件に合うときだけ、メニュー上部へ表示されます。" />
                                <ItemsControl ItemsSource="{Binding ContextualActionSettings}"
                                              ItemTemplate="{StaticResource ActionSettingTemplate}" />
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </StackPanel>
            </Border>
        </Popup>

        <!-- 連続コピーの収集条件 -->
        <Popup x:Name="SequentialCapturePopup" StaysOpen="False" AllowsTransparency="True" Focusable="True"
               Placement="Top" PlacementTarget="{Binding ElementName=SequentialCaptureButton}"
               VerticalOffset="-4" HorizontalOffset="-10"
               PreviewKeyDown="OnPopupPreviewKeyDown">
            <Border Style="{StaticResource PopupCard}" Width="470" Margin="10">
                <StackPanel Margin="14">
                    <TextBlock Text="連続コピーで集める操作" FontWeight="SemiBold" />
                    <!--
                        Windows はクリップボードが変わったことしか伝えず、誰がコピーしたかは分からない。
                        どこまでを利用者の意図と見なすかの線引きなので、選択肢の違いを必ず添える。
                        判定の考え方は DESIGN_COPY_INTENT.md を参照。
                    -->
                    <TextBlock Margin="0,4,0,10" Style="{StaticResource Hint}" TextWrapping="Wrap"
                               Text="Windows はクリップボードが変わったことしか伝えないため、どの操作の結果かはこのアプリが推測します。どこまでを 1 件として集めるかを選べます。" />

                    <ComboBox x:Name="SequentialCaptureBox"
                              ItemsSource="{Binding SequentialCaptureOptions}"
                              DisplayMemberPath="Name" SelectedValuePath="Trigger"
                              SelectedValue="{Binding SequentialCaptureTrigger, Mode=TwoWay}" />

                    <TextBlock Margin="0,8,0,0" Style="{StaticResource Hint}" TextWrapping="Wrap"
                               Text="{Binding SequentialCaptureStatus}" />

                    <TextBlock Margin="0,10,0,0" Style="{StaticResource Hint}" TextWrapping="Wrap"
                               Text="パスワード管理ソフトなど、コピー元が他のアプリでの保存を断っている内容は、どの設定でも集めません。" />
                </StackPanel>
            </Border>
        </Popup>

        <!-- スプリント設定パネル -->
        <Popup x:Name="SprintPopup" StaysOpen="False" AllowsTransparency="True" Focusable="True"
               Placement="Top" PlacementTarget="{Binding ElementName=SprintButton}"
               VerticalOffset="-4" HorizontalOffset="-10">
            <Border Style="{StaticResource PopupCard}" Width="420" Margin="10">
                <StackPanel Margin="14">
                    <TextBlock Text="スプリントの区切り" FontWeight="SemiBold" />
                    <TextBlock Margin="0,4,0,10" Style="{StaticResource Hint}" TextWrapping="Wrap"
                               Text="どれか 1 つのスプリントの開始日と長さを決めると、{date@sprint} などが「今日を含むスプリントの開始日」を指すようになります。" />

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="基準日" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <TextBox Grid.Column="1" x:Name="SprintAnchorBox" MinWidth="110"
                                 Text="{Binding SprintAnchorText, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="yyyy-MM-dd 形式（例 2026-04-06）。空にするとスプリントの差し込みを使いません" />
                        <TextBlock Grid.Column="2" Text="長さ" VerticalAlignment="Center" Margin="14,0,8,0" />
                        <TextBox Grid.Column="3" Width="52" TextAlignment="Right"
                                 Text="{Binding SprintLengthText, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="スプリント 1 つの日数（2 週間なら 14）" />
                        <TextBlock Grid.Column="4" Text="日" VerticalAlignment="Center" Margin="6,0,0,0" />
                    </Grid>

                    <TextBlock Margin="0,10,0,0" Style="{StaticResource Hint}" TextWrapping="Wrap"
                               Text="{Binding SprintStatus}" />
                </StackPanel>
            </Border>
        </Popup>

        <!-- 差し込み挿入パネル -->
        <Popup x:Name="InsertPopup" StaysOpen="False" AllowsTransparency="True" Focusable="True"
               Placement="Bottom" PlacementTarget="{Binding ElementName=InsertButton}"
               VerticalOffset="4" HorizontalOffset="-260"
               PreviewKeyDown="OnPopupPreviewKeyDown">
            <Border Style="{StaticResource PopupCard}" Width="470" Margin="10">
                <DockPanel Margin="10">
                    <Grid DockPanel.Dock="Top" Margin="0,0,0,8">
                        <TextBox x:Name="PlaceholderFilterBox" TextChanged="OnPlaceholderFilterChanged" />
                        <TextBlock x:Name="PlaceholderFilterHint" Text="差し込みを検索（日付、計算、連番 など）"
                                   Margin="10,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False"
                                   Foreground="{DynamicResource Brush.Text.Disabled}" />
                    </Grid>
                    <TextBlock DockPanel.Dock="Bottom" Margin="2,8,0,0" Style="{StaticResource Hint}"
                               Text="クリックするとカーソル位置に挿入されます。右側は現在の値です。" />
                    <ScrollViewer MaxHeight="330" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="PlaceholderList">
                            <ItemsControl.GroupStyle>
                                <GroupStyle>
                                    <GroupStyle.HeaderTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Name}" FontSize="11" FontWeight="SemiBold"
                                                       Margin="4,8,0,3"
                                                       Foreground="{DynamicResource Brush.Text.Secondary}" />
                                        </DataTemplate>
                                    </GroupStyle.HeaderTemplate>
                                </GroupStyle>
                            </ItemsControl.GroupStyle>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource RowButton}" Click="OnInsertPlaceholder"
                                            ToolTip="{Binding Description}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="185" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding Token}"
                                                       FontFamily="Consolas, Yu Gothic UI"
                                                       Foreground="{DynamicResource Brush.Accent}"
                                                       TextTrimming="CharacterEllipsis" />
                                            <TextBlock Grid.Column="1" Text="{Binding Description}" Margin="8,0,8,0"
                                                       FontSize="11.5"
                                                       Foreground="{DynamicResource Brush.Text.Secondary}"
                                                       TextTrimming="CharacterEllipsis" />
                                            <TextBlock Grid.Column="2" Text="{Binding Sample}" MaxWidth="120"
                                                       FontSize="11.5" TextAlignment="Right"
                                                       TextTrimming="CharacterEllipsis" />
                                        </Grid>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
            </Border>
        </Popup>

        <!-- カテゴリ候補 -->
        <Popup x:Name="CategoryPopup" StaysOpen="False" AllowsTransparency="True" Focusable="True"
               Placement="Bottom" PlacementTarget="{Binding ElementName=CategoryBox}"
               VerticalOffset="4"
               PreviewKeyDown="OnPopupPreviewKeyDown">
            <Border Style="{StaticResource PopupCard}" MinWidth="180" Margin="10">
                <DockPanel Margin="8">
                    <Button DockPanel.Dock="Top" Style="{StaticResource RowButton}"
                            Content="トップレベル" FontWeight="SemiBold"
                            HorizontalContentAlignment="Left" Click="OnClearCategory" />
                    <Border DockPanel.Dock="Top" Height="1" Margin="4,5"
                            Background="{DynamicResource Brush.Separator}" />
                    <ScrollViewer MaxHeight="260" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding KnownCategories}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource RowButton}" Click="OnPickCategory"
                                            HorizontalContentAlignment="Left">
                                        <Grid Width="220">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="18" />
                                                <ColumnDefinition Width="22" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Ellipse Grid.Column="0" Width="9" Height="9"
                                                     Fill="{Binding ColorBrush}"
                                                     Stroke="{DynamicResource Brush.Border.Strong}"
                                                     StrokeThickness="0.7"
                                                     Visibility="{Binding HasColor, Converter={StaticResource BoolToVis}}" />
                                            <TextBlock Grid.Column="1" Text="{Binding IconGlyph}"
                                                       FontFamily="{Binding IconFontFamily}" FontSize="13"
                                                       Foreground="{Binding IconBrush}"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                                       Visibility="{Binding HasIcon, Converter={StaticResource BoolToVis}}" />
                                            <TextBlock Grid.Column="2" Text="{Binding Name}" Margin="4,0,0,0"
                                                       VerticalAlignment="Center" />
                                        </Grid>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
            </Border>
        </Popup>

        <!-- 直近に前面だったアプリ -->
        <Popup x:Name="AppPopup" StaysOpen="False" AllowsTransparency="True" Focusable="True"
               Placement="Bottom" PlacementTarget="{Binding ElementName=AppProcessBox}"
               VerticalOffset="4"
               PreviewKeyDown="OnPopupPreviewKeyDown">
            <Border Style="{StaticResource PopupCard}" MinWidth="220" Margin="10">
                <StackPanel Margin="8">
                    <TextBlock Margin="4,2,4,6" FontSize="11" TextWrapping="Wrap"
                               Foreground="{DynamicResource Brush.Text.Secondary}"
                               Text="トレイメニューを開いたときに前面だったアプリです。" />
                    <ItemsControl ItemsSource="{Binding KnownApps}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource RowButton}" Click="OnPickApp"
                                        Content="{Binding}" HorizontalContentAlignment="Left" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </Popup>
    </Grid>
</Window>
