# 前面アプリで項目を絞り込む 検討メモ

> **実装済み。** 本メモの方針どおり `AppProcess` / `AppTitlePattern` として実装した。
> `Services/ForegroundApp.cs`（取得）と `Services/AppContextMatcher.cs`（判定）が本体で、
> §6 の差し込み（`{app:name}` / `{app:title}` と正規表現抽出）も実装済み。
> §7 の実機確認、特に **7-1（右クリック経路で前面ウィンドウを捕まえられるか）は未確認**。

「ターミナルが前面ならコマンド、チケット画面なら報告テンプレート」のように、
**そのとき前面にあるウィンドウ**に応じてメニューの中身を変える案の検討。

スマートアクションが「クリップボードの中身」を見ているのに対し、これは「作業している場所」を見る。
両方を **AND** で効かせ、判定にはプロセス名とウィンドウタイトルの両方を使う方針で検討する。

---

## 1. 最大の落とし穴: メニューを開く時点では自分が前面

`PopulateMenu()` は `menu.Opening` から呼ばれる。だがそこで `GetForegroundWindow()` を呼んでも、
返るのは**利用者のアプリではなく `MenuHostWindow`**（ホットキー経路では表示より先に前面化しているため）。
クリック経路でも `NotifyIcon` の内部処理が同じことをしている可能性がある。

つまり「メニューを組み立てるときに前面を調べる」は成立しない。
**開く操作を受け取った瞬間に取って覚えておく**必要がある。

幸い、`ShowMenuAtCursor()` は既に同じことをしている。

```csharp
IntPtr previousForeground = GetForegroundWindow();   // 前面化する前に取っている
```

ただしこれは**ホットキー経路とフォールバック経路にしかない**。
トレイ左クリック（リフレクション経路）と右クリックは通らない。

### 取る場所

`ShowTrayMenu()` の入口――経路が分かれる前――で 1 回だけ取り、フィールドに置く。

```
ShowTrayMenu()
  ├ ここで捕まえる（_menuContext = 現在の前面ウィンドウの情報）
  ├ リフレクション経路（トレイクリック）
  └ ShowMenuAtCursor（ホットキー / フォールバック）
       └ menu.Opening → PopulateMenu() が _menuContext を読む
```

`ShowMenuAtCursor()` の `previousForeground` も同じ値を使えるので、
取得は 1 か所に寄せられる（フォーカス復元の対象と、絞り込みの判定対象は同じウィンドウであるべき）。

自分のウィンドウ（`MenuHostWindow` / 設定画面）が前面だった場合は「対象なし」として扱う。
これは既存コードの `previousForeground == _menuHost.Handle` の判定と同じ考え方。

### RebuildMenu からの呼び出し

`RebuildMenu()` は表示前にも `PopulateMenu()` を 1 回呼ぶ。
このときの結果は開いた瞬間に作り直されるので中身は問わないが、
**フィールドが前回の値のまま残っていると誤解を招く**。開き終わったら破棄する（`menu.Closed` で null に戻す）。

---

## 2. 前面アプリを何で識別するか

### 2-1. プロセス名

```
GetForegroundWindow() → GetWindowThreadProcessId() → プロセス名
```

プロセス名は `Process.GetProcessById().ProcessName` で取れるが、
**保護されたプロセスや権限の違うプロセスでは例外になる**。
`QueryFullProcessImageName`（`PROCESS_QUERY_LIMITED_INFORMATION` で開く）のほうが素直に取れる。
どちらにせよ失敗しうるので、**失敗を通常の分岐として扱う**（→ §3）。

#### UWP / ストアアプリの罠

電卓・設定・一部のストアアプリは、前面ウィンドウが `ApplicationFrameHost.exe` になり、
中身のアプリ名が取れない。子ウィンドウをたどれば本体に届くが、対象は限られていて確実でもない。

**v1 では追わない。** このアプリの利用者が絞り込みたいのは
エディタ・ターミナル・ブラウザ・チケット管理の類で、いずれも通常の Win32 プロセスとして見える。
README にも「ストアアプリは `ApplicationFrameHost` として見えることがある」と正直に書いておく。

#### 書き方

正規表現にはしない。プロセス名は `chrome.exe` のような短い固定文字列で、
利用者が書きたいのは「これとこれ」という**列挙**だから。

```
chrome.exe, msedge.exe
```

- カンマ区切り、前後の空白は無視
- 大文字小文字を区別しない
- `.exe` は省略可（`chrome` でも一致させる）

パス全体は見ない。同名の別アプリを区別したいという需要が出てから考える。

### 2-2. ウィンドウタイトル

こちらは**正規表現**にする。既存の `ClipboardPattern` と同じ扱いにできる。

```
表示条件のアプリ: chrome.exe
タイトル:        /issues/|Redmine
```

- `GetWindowText` で取る。長さは上限を決めて切る（数 KB のタイトルを付けるアプリがある）
- タイムアウトは `ClipboardMatcher` と同じ **200 ミリ秒**。メニューを開く手を止めない
- 保存前の検証も `ClipboardMatcher.TryValidateRegex()` をそのまま使える

タイトルは**言語・バージョン・開いているファイルで変わる**。
安定した識別子ではないことは README に書いておく（プロセス名は変わらないがタイトルは変わる）。

### 2-3. 両方書いた場合

AND。プロセス名が一致し、かつタイトルが一致したときだけ表示する。
片方だけ書いた場合はそちらだけを見る。両方空なら条件なし＝常に表示（今までどおり）。

---

## 3. 「項目が見当たらない」事故にどう備えるか

独立した絞り込み（AND）を選んだ以上、**通常の項目もメニューから消える**。
このアプリで一番避けたい失敗は「あるはずの項目が出てこない」なので、ここを設計の中心に置く。

### 3-1. 判定できなかったときは表示する（fail-open）

プロセス名が取れなかった、前面ウィンドウが無い、タイトルが読めない――
こうした場合は**条件に一致したものとして扱い、項目を出す**。

正規表現のタイムアウトを「一致しなかった」として扱っている既存の方針とは逆になるが、これは意図的。

| | 隠れたときの実害 |
| --- | --- |
| スマートアクション | 出なくても元々「おまけ」。気付かない |
| 通常の項目（今回） | 使おうとした項目が無い。原因も分からない |

**タイトルの正規表現がタイムアウトした場合だけは fail-closed** にする。
こちらは「利用者が書いた式が重い」ことが原因で、判定不能とは意味が違う。
……とも考えたが、結果は同じ「項目が消える」なので**ここも表示する**方向で揃える。
迷ったら出す、を通す。

#### 実装時の判断: タイトルが空の場合

「タイトルが読めない」と「タイトルが無い」は区別できない（`GetWindowText` はどちらでも空を返す）。
そこで fail-open にするのは**プロセス名を取れなかった場合まで**とし、
プロセス名が取れているなら、空のタイトルはそのまま空文字列として照合する。

- プロセス名が取れない → 判定材料が無いので表示する
- プロセス名は取れていて、タイトルが空 → 「タイトルが空」という判定結果として扱う（式が一致しなければ隠す）
- 正規表現がタイムアウト・不正 → 表示する

前者は情報の欠落、後者は正当な判定結果、という切り分け。

### 3-2. 設定画面で見つけられるようにする

- 一覧の右端バッジに、カテゴリ・連番と並べて**アプリ条件の印**を出す
- 検索（`FilterText`）の対象にプロセス名とタイトルの式を含める。
  現在は `Name` / `Text` / `Category` / `ClipboardPattern` を見ているので、そこに 2 つ足すだけ

### 3-3. メニュー側で気付けるようにするか

「アプリ条件で N 件隠しています」をメニューに出す案がある。
正直で親切だが、**常に出る行が 1 つ増える**のは 2 打鍵の速さを損なう。

v1 では出さない。代わりに**トレイアイコンのツールチップ**（`BuildIconToolTip()`）に
現在の絞り込み状況を出すことは検討できる。ここなら邪魔にならない。

### 3-4. 区切り線

条件で項目が消えると、区切り線だけが残ったり連続したりする。
`TrimEdgeSeparators()` が既にこれを処理しているので、**追加の対処は要らない**。
カテゴリが空になった場合も既存の処理（`Enabled = false`）に乗る。

ただし「中身が空のカテゴリを無効表示で残す」のは、
スマートアクションでは妥当でも**アプリ条件では邪魔**かもしれない。
空になったカテゴリはいっそ出さない、という判断もありうる。実際に使ってみて決める。

---

## 4. 設定ファイル

`ClipItem` に 2 つ足すだけ。古いファイルには無いので既定値（空）＝条件なしになる。`Version` は据え置き。

```json
{
  "Name": "ブランチ名",
  "Text": "feature/{clip:digits}",
  "AppProcess": "chrome.exe, msedge.exe",
  "AppTitlePattern": "/issues/"
}
```

| フィールド | 意味 |
| --- | --- |
| `AppProcess` | 前面アプリの実行ファイル名（カンマ区切り）。空なら見ない |
| `AppTitlePattern` | 前面ウィンドウのタイトルに対する正規表現。空なら見ない |

`Clone()` と `CreateDefault()` の更新を忘れないこと。
**既定のサンプルには入れない。** 導入しただけで項目が消える体験を作らない。

---

## 5. 設定画面

スマートアクションの `Expander` と同じ形で、もう 1 つ `Expander` を置く。
中身は 2 つの入力欄と状態表示だけなので、既存の
`ClipboardConditionStatus` と同じ作りをもう 1 本引けばよい。

```
▸ 表示するアプリ（前面のウィンドウに応じて表示）
    アプリ      [ chrome.exe, msedge.exe        ] [ 現在のアプリ ▾ ]
    タイトル    [ /issues/                       ]
    状態: chrome.exe が前面で、タイトルに /issues/ を含むときだけ表示します
```

### 「現在のアプリ ▾」が要る

**利用者はプロセス名を知らない。** 「Chrome の実行ファイル名は？」から始まるようでは使われない。

`TrayIconManager` が、メニューを開くたびに拾った前面プロセス名を数件だけ覚えておき
（**メモリ上だけ。設定ファイルにも履歴にも残さない**）、設定画面のドロップダウンに出す。
カテゴリ候補の「▾」と同じ操作感になる。

覚えておく件数は 5 件程度。自分自身（`MyTaskTray`）は除く。

---

## 6. 差し込みへの波及

**実装済み。** メニューを開く直前に取得した同じ `ForegroundApp` を、項目の表示条件だけでなく
コピー完了まで持ち回る。選択肢や複数入力の途中で前面が MyTaskTray に変わっても取り直さない。

| 差し込み | 内容 |
| --- | --- |
| `{app:name}` | 実行ファイル名から `.exe` を除いたアプリ名 |
| `{app:title}` | ウィンドウタイトル |
| `{app:name:/正規表現/}` | アプリ名から一致部分を抽出 |
| `{app:title:/正規表現/}` | タイトルから一致部分を抽出 |

正規表現は `{clip:/…/}` と同じく、最初のキャプチャがあればその値、なければ一致全体を返す。
不正・タイムアウト・不一致なら差し込みを展開せず、書いたまま残す。キャプチャを `{match:…}` に
載せないため、クリップボード側との名前衝突も起こらない。

設定画面では最後に取得できた外部アプリをプレビューに使う。設定画面自身を読み直すことはせず、
まだ外部アプリを取得できていなければ app 系の差し込みを未展開のまま表示する。

---

## 7. 実機で確認が必要なこと

上から順に、確認できないと先に進めない順。

1. **`ShowTrayMenu()` の入口で取った前面ウィンドウが、本当に利用者のアプリか。**
   トレイ**右クリック**の場合、`ContextMenuStrip` は WinForms が勝手に出すため
   `ShowTrayMenu()` を通らない。この経路では `menu.Opening` が最初の入り口になり、
   そこでは既に前面が変わっている可能性がある。**この案の成否はここに懸かっている**
2. 1 が駄目な場合、`NotifyIcon.MouseDown` で先に捕まえられるか。
   （`OnIconMouseUp` は左クリックしか見ていないので、右も拾う必要がある）
3. 管理者権限で動いているウィンドウが前面のとき、プロセス名が取れるか。
   取れなければ fail-open で表示されるはずだが、その挙動を確認する
4. ストアアプリ（電卓・設定）が前面のとき `ApplicationFrameHost.exe` になるか
5. タイトルの取得が、応答していないウィンドウで固まらないか
   （`GetWindowText` は送信先が固まっていても返るはずだが、実機で見たい）

---

## 8. 判断

- **判定そのものは小さい。** Win32 は 3 つ（`GetForegroundWindow` は既にある）、
  判定ロジックは `ClipboardMatcher` の隣にもう 1 本
- **難所は「いつ前面を取るか」** の 1 点に集約される。§7-1 を先に試す
- **設計の中心は「項目が消える」ことへの備え。** 迷ったら表示する側に倒す
- 設定画面は「現在のアプリ ▾」が無いと使われない。ここは v1 に含める

### 進め方の案

```
1. 右クリック経路で前面ウィンドウを捕まえられるか、小さく試す   ← ここで可否が決まる
2. AppProcess / AppTitlePattern を ClipItem に足し、判定を PopulateMenu に組み込む
3. 設定画面の Expander と状態表示、検索対象への追加
4. 「現在のアプリ ▾」（メモリ上の前面アプリ履歴）
5. {app:name} などの差し込み（実装済み）
```
