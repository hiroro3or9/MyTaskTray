# 差し込みの「基準（`@`）」拡張 設計メモ

対象: `Services/TemplateEngine.cs` の `{名前[@基準][±数値[単位]][:書式]}`。
現状 `@sprint`（今日を含むスプリントの開始日）だけが実装されている。
ここでは「ほかにどんな基準がありうるか」を洗い出し、優先度を付ける。

サンドボックスに .NET SDK が無いため、実装コストはコード読解による見積もり。
日付の抽出パターンだけは実際に動かして誤爆を確認した（後述）。

---

## 0. 前提: `@` の意味を何に固定するか

**`@基準` は「展開の起点となる日付（`now`）を差し替える」ものと定義する。**
差し替えたあとは、既存のオフセット・単位・書式がそのまま乗る。

この線引きが、候補を選別する基準になる。

### なぜ「年度」を基準にしてはいけないか

`@fy`（年度）は一見よさそうに見えるが、採用してはいけない。
年度は**起点ではなく派生**だからである。

基準は 1 つしか書けない。もし `@fy` を作ると、
「スプリント開始日の年度」を出したいときに `@sprint` と `@fy` の両方が必要になり、書けなくなる。
実際にこれが最初の要件だった。

いま採用している書き方は、基準（スプリント開始日）と派生（年度・四半期）を分離している:

```
{calc:{year@sprint-3mo}-1996}-{calc:ceil({month@sprint-3mo}/3)}
        ^^^^^^^ 基準      ^^^^ 派生（-3mo で年度に寄せる）
```

**派生は名前側の仕事**（`{fy}` `{fq}` のようなトークン、または今のように `-3mo` の技）であって、
基準側に持ち込むと組み合わせが破綻する。

### 選別の問い

> それは「起点の日付を差し替えるもの」か？

Yes なら `@` の候補。No なら別の文法（トークン名、書式、あるいは別機能）で扱う。

---

## 一覧

| # | 基準 | 内容 | 価値 | コスト | 優先度 | 状況 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `@clip` | クリップボードの文字列を日付として読む | 高 | 中 | **高** | **対応済み** |
| 2 | （前提）`ContainsClipboard()` の修正 | `{date@clip}` を見逃す穴を塞ぐ | — | 小 | **高（1 の必須条件）** | **対応済み** |
| 3 | （隣接）オフセットの複数指定 | `{date@sprint+1sp-1}` を書けるようにする | 中 | 小 | **高** | **対応済み** |
| 4 | 既存の日付トークンを基準に | `{week@monthstart}` など。文法の一般化 | 低〜中 | 中 | 中 | **対応済み** |
| 5 | 日付リテラル `@2026-04-06` | 起点を直書き | 低 | 小 | 低 | 保留 |
| 6 | `@lastcopy` | 前回この項目をコピーした日時 | 低 | 高 | 見送り | 見送り |
| 7 | `@ask` / 入力ダイアログ | コピー時に日付を聞く | 低 | 高 | 対象外 | 対象外 |

#1〜#4 は実装済み。実装して分かった点は各項の末尾に「**対応**」として追記した。

---

## 優先度: 高

### 1. `@clip` — クリップボードの文字列を日付として読む

```
{date@clip:yyyyMMdd}      2026/08/15 をコピー → 20260815
{date@clip:yyyy年M月d日}    → 2026年8月15日
{date@clip+1w}            → その 1 週間後
{week@clip} {dow@clip}    → その日の ISO 週番号 / 曜日番号
{calc:{year@clip-3mo}-1996}-{calc:ceil({month@clip-3mo}/3)}
                          → その日が属するスプリント名
```

#### なぜ最優先か

これは**クリップボードツールとしての本業**だからである。
ほかの候補が「日付計算の表現力を増やす」話なのに対し、これだけは
「いま手元にある値を別の形にして返す」という、このアプリが元々やっていることの延長にある。

- 日付書式の変換は日常的に発生する（`2026/08/15` → `20260815` → `2026年8月15日`）。
  いまはこれを手で打ち直している
- `{clip}` と同じ発想なので、使い方の説明がほとんどいらない
- 既存のすべての日付トークンに一度に効く。`@clip` を 1 つ足すだけで
  「クリップボードの日付の週番号」「その日の月末」「その日のスプリント名」が全部使えるようになる

#### 実装

`ExpandToken()` の基準解決に分岐を足す:

```csharp
now = baseName switch
{
    "sprint" when context.Sprint is not null => context.Sprint.StartOf(context.Now),
    "clip"   when context.HasClipboard       => ParseClipboardDate(context.Clipboard),
    _ => throw ...  // → original を返す
};
```

`ParseClipboardDate()` は次の順で試す:

1. 前後の空白を取り除いた**全体**を `TryParseExact` にかける
   （`yyyy-MM-dd` `yyyy/MM/dd` `yyyy.MM.dd` `yyyyMMdd` `yyyy年M月d日`）
2. 全体が日付でなければ、**文中から最初の日付らしい並びを取り出す**
3. どちらも駄目なら失敗（＝書いたままを残す）

2 を入れるかどうかは判断が要る。「リリース日: 2026/08/15 まで」のような文字列を
そのまま使えるようになるので便利だが、誤爆が怖い。実際に試したところ、
年が 4 桁であることを要求すれば電話番号は拾わない:

| クリップボードの内容 | 取り出せる日付 |
| --- | --- |
| `2026-08-15` / `2026/8/15` / `2026.08.15` | 2026-08-15 |
| `2026年8月15日` / `20260815` | 2026-08-15 |
| `リリース日: 2026/08/15 まで` | 2026-08-15 |
| `スプリント 30-2 は 2026/07/27 開始` | 2026-07-27 |
| `03-0000-0000` / `090-1234-5678` | （なし） |
| `4329` / `123456789` / `v1.2.3` | （なし） |
| `2026-13-45`（存在しない日） | （なし） |
| `8/15` / `15/08/2026` | （なし） |

**採用可**と判断する。ただし `IDは 20260815 です` は日付として拾ってしまう。
8 桁の数字が伝票番号のこともあるので、ここは避けられない曖昧さとして受け入れる。
（誤って展開されてもプレビューとトースト通知で気付ける）

#### 決めておくこと

- **`CurrentCulture` の `TryParse` は使わない。** `08/15/2026` のような
  米国式の並びを環境によって拾ったり拾わなかったりするより、
  受け付ける表記を明示列挙したほうが挙動が読める
- **時刻は無視して日付だけ扱う。** `{time@clip}` は `00:00` になる。
  クリップボードの時刻まで扱いたい要求が出てから考える
- **`{clip:/正規表現/}` との合成はできない。** 書式（`:` の右）は結果の整形に使われるので、
  「クリップボードから正規表現で抜いた文字列を日付として読む」は書けない。
  上記 2（文中からの抽出）は、この制約を実用上埋めるためのもの

#### 見積もり

パーサ 1 つ（40 行程度）＋ 基準解決の分岐 ＋ #2 の修正。
既存のプレビュー（`SettingsViewModel._clipboard`）と
空クリップボードの警告（`CopyToClipboard()`）はそのまま効くので、追加の配線は不要。

#### 対応

`TryParseClipboardDate()` として実装。見積もりどおり追加の配線は不要だった。

実装中に 1 つ設計を変えた。当初は基準の解決に失敗したとき例外を投げるつもりだったが、
基準の解決は `try` ブロックより前で行う必要があり（`At()` の定義より前に起点が要る）、
制御フローに例外を使うことになる。`ResolveBase()` が `DateTime?` を返し、
null なら `original` を返す形にした。「解釈できないものは書いたまま残す」という
既存の方針とも素直に噛み合う。

全体一致に `yyyy-M-d` のように 1 文字の指定子を使うと 1 桁・2 桁の両方を受けられるため、
`2026-8-15` と `2026-08-15` を 1 つの書式でまかなえた。

---

### 2.（前提）`ContainsClipboard()` が `{date@clip}` を見逃す

**これは #1 の必須条件。直さないと `@clip` は黙って壊れる。**

`TrayIconManager.CopyToClipboard()` は、クリップボードを読むかどうかを
`TemplateEngine.ContainsClipboard()` で決めている。読まない項目では空文字が渡る:

```csharp
bool usesClipboard = TemplateEngine.ContainsClipboard(item.Text);
string clipboard = usesClipboard ? ClipboardService.GetText() : string.Empty;
```

いまの判定は差し込みの**名前**しか見ていない:

```csharp
private static bool IsClipboardName(string name)
    => name.Equals("clip", StringComparison.OrdinalIgnoreCase);

private static bool IsToken(string inner, Func<string, bool> matchesName)
{
    Match m = InnerRegex().Match(inner.Trim());
    return m.Success && matchesName(m.Groups["name"].Value);   // ← base を見ていない
}
```

`{date@clip}` は名前が `date` なので `false` になり、クリップボードを読まないまま展開される。
結果、**日付が読めず書いたままの文字列がコピーされる**。
しかも「クリップボードが空です」の警告も出ないので、原因が分かりにくい。

`IsToken()` に基準名も渡し、`name` か `base` のどちらかが一致したら真、とする。
`ContainsSequence()`（`{seq}`）は基準を持てないので影響しない。

#### 対応

`IsToken()` を修正。副次的に `ContainsTimeSensitive()` にも効くようになった
（`{week@now}` のように時間で変わる基準を使った項目でも、プレビューが追従する）。

---

### 3.（隣接）オフセットを 2 つ以上書けるようにする

基準そのものの話ではないが、**`@sprint` を入れた時点で穴が見えている**ので併記する。

いまの正規表現はオフセットを 1 つしか許さない:

```
(?:(?<sign>[+\-])(?<num>\d+)(?<unit>[A-Za-z]*))?
```

このため「**今のスプリントの最終日**」が書けない:

```
{date@sprint+1sp-1}   → 解釈できず、書いたまま残る
```

スプリント名と並んで需要が高い値（「7/27 〜 8/9」の後半）なのに出せない。
いまは `{date@sprint+13}` と日数を直書きするしかなく、
設定でスプリント長を変えたときに追従しない。

繰り返しを許すだけで解決する:

```
(?:(?<sign>[+\-])(?<num>\d+)(?<unit>[A-Za-z]*))*
```

`Shift()` を一致した数だけ順に適用する。`Match` の `Groups["num"].Captures` を回せばよい。
基準文法とは独立に効き、ほかにも次のような表現が開く:

```
{date+1mo-1}          翌月の前日
{date@sprint+2sp-1}   次のスプリントの最終日
```

コストが小さく効果が具体的なので、**#1 より先に入れてもよい**。

#### 対応

正規表現の `?` を `*` に変え、`Groups["num"].Captures` を順に回して `Shift()` を重ねた。
`{random:-5-5}` や `{daysuntil:2026-12-31}` のように書式に `-` を含むものが
オフセットとして誤読されないことを確認済み（オフセットは必ず符号で始まり、
書式は `:` 以降なので切り出し方は一通りに決まる）。

`{seq}` は日付ではないので、オフセットを重ねて書いた場合は誤りとして扱う。

**当初の記述に誤りがあったので訂正する。**
このメモの初版に `{monthstart+1mo-1d}` が「今月の末日（`{monthend}` と同じ）」と書いてあったが、これは**誤り**。
`{monthstart}` はオフセットを適用した**あとで**月初を求めるため、実際には次のようになる:

```
{monthstart+1mo-1d}  →  8/1 → (+1mo) 9/1 → (-1d) 8/31 → (月初) 8/1
```

`{monthstart}` `{monthend}` `{weekstart}` `{weekend}` は同じ性質を持つ。
月末が欲しければ素直に `{monthend}` を使う。README にも注記を入れた。

---

## 優先度: 中

### 4. 既存の日付トークンをそのまま基準にする

`@` の右に `monthstart` `monthend` `weekstart` `weekend` `date` などを許す。

```
{week@monthstart}      今月 1 日の ISO 週番号
{dow@monthend}         今月末の曜日番号
{year@monthend+1mo}    翌月末の年
```

#### 評価

これは**機能追加というより文法の一般化**である。
入れると `sprint` の特別扱いが消え、基準の解決が「日付トークンを 1 つ評価する」だけになる。
実装から分岐が減るという意味では気持ちがよい。

ただし実用面での価値は低い。多くは既存の書き方で足りるからだ:

| やりたいこと | 基準なしで書けるか |
| --- | --- |
| 月初の曜日名 | `{monthstart:ddd}` で書ける |
| 月末の日 | `{monthend:d}` で書ける |
| 月初の ISO 週番号 | **書けない**（`{week@monthstart}` が要る） |
| 月末の曜日**番号** | **書けない** |

本当に新しく開くのは、`{week}` `{dow}` `{doy}` `{daysinmonth}` という
**数値トークンを、今日以外の日付に対して使えるようにする**ことだけ。かなりニッチ。

#### 判断

**単独ではやらない。#1 と一緒にやるなら安い。**

`@clip` を入れる時点で「基準名 → 日付」の解決テーブルを作ることになるので、
そこに既存トークンを流し込むだけなら追加コストはほぼゼロ。
逆にこれ単独のために再帰評価と深さ制限を持ち込むのは割に合わない。

（基準の中に基準は書けない、というルールは維持する。`{year@sprint@fy}` のような
入れ子を許すと `MaxDepth` の管理が要るうえ、読み手にも分からなくなる）

#### 対応

`#1` と同時に実装。読み通りコストはほぼゼロで、`ResolveBase()` の `switch` に
5 行足しただけで済んだ。再帰評価は使っておらず、`MonthStart()` などの
既存のヘルパーを呼ぶだけなので深さ制限も不要。

結果として `sprint` の特別扱いが消え、基準の解決が 1 か所の `switch` に収まった。

---

## 優先度: 低

### 5. 日付リテラル `@2026-04-06`

```
{dow@2026-04-06}        その日の曜日番号
{date@2026-04-06+100}   その日から 100 日後
{week@2026-04-06}       その日の ISO 週番号
```

正規表現の基準部を `[A-Za-z]+` から日付も許す形に広げるだけなので、コストは小さい。

#### なぜ優先度が低いか

**起点が動かないなら、結果も動かない。動かない値を毎回計算する理由が薄い。**

「2026-04-06 の曜日」は一度調べれば固定なので、コピー文字列に直接
`月曜` と書いておけば済む。差し込みにする意味があるのは、
今日やクリップボードのように**値が動く**場合だけである。

固定日が絡む動的な計算は、すでに `{daysuntil:2026-12-31}` が担当している。
これで足りない場面が具体的に出てくるまでは、入れなくてよい。

強いて言えば「基準日の違うスプリントを併用したい」（チーム A は 2 週、チーム B は 3 週）が
動機になりうるが、それには周期も要るので `@2026-04-06/2w` のような別の文法になる。
そのときに改めて設計する。

---

## 見送り

### 6. `@lastcopy` — 前回この項目をコピーした日時

「前回から N 日ぶり」を書けるようになる。日報や進捗メモに使える、かもしれない。

**コストが価値に見合わない。**

- `ClipItem` に `LastCopiedAt` を持たせ、コピーのたびに `settings.json` へ書き込む。
  連番（`SequenceValue`）と同じ永続化の重さがかかる
- 連番と同じ「設定画面を開いている間にトレイ側で値が進む」問題が再発する。
  `BUG_REPORT` で一度苦労した箇所を、もう一度別の値で作ることになる
- **初回コピー時に基準が無い**。今日を使うと `0 日ぶり` になり、意味のない値が出る
- そもそも「前回いつコピーしたか」を文章に埋め込みたい場面が具体的に思いつかない

用途が具体化してから再検討する。

### 7. `@ask` — コピー時にダイアログで日付を聞く

これは基準ではなく**別機能（コピー時の入力）**である。
トレイメニューをクリックしたらダイアログが出る、というのは
「クリックしたら即コピー」というこのアプリの前提を壊す。

そして `@clip` があれば大半は代替できる。日付をコピー → メニューをクリック、で 2 手。
ダイアログを出しても手数は変わらない。

対象外とする。

---

## 実装順の推奨

```
1. #3  オフセットの複数指定          小さく、単独で完結し、すぐ効く
2. #2  ContainsClipboard() の修正     #1 の前提。単独でも「潜在バグの解消」として意味がある
3. #1  @clip                          本命
4. #4  既存の日付トークンを基準に      #1 のついでに入るなら。単独ではやらない
```

#3 → #2 → #1 の順にすると、それぞれが独立してリリースでき、
`@clip` を入れる頃には土台（複数オフセット・クリップボード検出）が揃っている。

#5 と #6 は、具体的な用途が出てくるまで保留。

---

## 実装後のまとめ

#1〜#4 をこの順で実装した。触ったのは `Services/TemplateEngine.cs` のみで、
呼び出し側（`TrayIconManager` / `SettingsViewModel` / 設定画面）の変更は不要だった。

- 正規表現のオフセット部を `?` → `*` に（#3）
- `IsToken()` が基準名も見るように（#2）
- `ResolveBase()` と `TryParseClipboardDate()` を追加（#1・#4）
- 差し込み一覧に「クリップボードの日付」グループを追加

### 残っている課題

- **`{clip:正規表現}` と `@clip` は合成できない。** 「クリップボードから正規表現で抜いた文字列を
  日付として読む」は書けない。文中からの抽出で実用上は埋まっているが、
  `2026-08-15` と `2026-09-01` が両方入っている文字列からは前者しか取れない
  → **保留**。理由は下記
- **`{time@clip}` は `00:00` になる。** クリップボードの時刻は読んでいない → 未対応
- ~~**`@clip` の日付が読めなかったとき、原因が「空」なのか「日付でない」なのか区別できない。**~~
  → **対応済み**（下記）

### 追補: 「日付として読めない」ときの扱い（対応済み）

初出時の記述は #2 の修正を反映しておらず、誤っていた。訂正しておく。

`ContainsClipboard()` が基準も見るようになった時点で、**クリップボードが空の場合は既に解決していた**。
`{date@clip}` を含む項目でも「クリップボードが空です」のトーストが出てコピーが中止される。

残っていたのは「**空ではないが日付として読めない**」場合で、これには実害があった。
クリップボードが `こんにちは` のとき `{date@clip:yyyyMMdd}` は展開されず、
`{date@clip:yyyyMMdd}` という文字列がそのままクリップボードに入り、気付かず貼り付けられる。
空のときは止まるのに日付でないときは止まらない、という一貫しない挙動でもあった。

空チェックと同じ形で `CopyToClipboard()` にガードを足して対応:

```csharp
if (TemplateEngine.ContainsClipboardDate(item.Text)
    && !TemplateEngine.CanParseClipboardDate(clipboard))
{
    ToastWindow.ShowToast("日付として読み取れません", $"「{…}」から日付を…");
    return;
}
```

`ContainsClipboardDate()` は「基準が `clip` の差し込み」だけを数える
（`{clip}` は日付でなくてよいので数えない）。
このために `ContainsToken()` / `IsToken()` の述語を `Func<string, bool>` から
`Func<string, string, bool>`（名前, 基準）に変えた。
副次的に、名前と基準のどちらを見るかが呼び出し側に明示され、
#2 で `IsToken()` の中に埋め込んだ暗黙のルールが表に出た。

**残っている穴**: 設定画面の「コピーして試す」にはこのガードが無い。
ただしプレビューに展開結果がそのまま出ているため、押す前に気付ける。

### なぜ #1（正規表現との合成）を保留にしたか

当初「文法を足すだけ」と見積もったが、**原理的な壁がある**。

`@clip(/…/)` のように引数を持たせても、`@{clip:/…/}` と入れ子にしても、
引数に入るのは正規表現である。正規表現には `{4}` や `(...)` が普通に出てくるため、
**トークンの切り出しを正規表現 1 本でやることが成り立たなくなる**。
いま `ExpandToken()` は `InnerRegex()` 一発で名前・基準・オフセット・書式を切り出しているが、
これを手書きパーサに置き換える必要がある。エンジンの中核で、最も挙動が固まっている部分。

一方で需要は薄い。文中抽出のおかげで `リリース日: 2026/08/15 まで` は既に読める。
困るのは「`2026-08-15 〜 2026-09-01` の 2 つめが欲しい」ような場合だけ。

なお、この文法拡張は保留中の日付リテラル（#5 → `@date(2026-04-06)`）や
チーム別スプリント（`@sprint(2w@2026-03-02)`）も同じ形に吸収できる。
それらの需要が同時に出てきたら、まとめて設計し直す価値が生まれる。

### Windows 実機で確認したいこと

- `{date@clip:ddd}` の曜日が地域設定どおりに出るか（`FormatDate()` は `CurrentCulture`）
- 差し込み一覧の「クリップボードの日付」グループで、
  クリップボードが日付でないときにトークンがそのまま並ぶ見た目が許容できるか
