# 段階02: モデル・保存・互換移行

前提: 段階01まで完了。01_COMMONとPROGRESSを読む。

## 必読資料

- [03_DATA.md](../03_DATA.md)
- [specs/default-items.md](../specs/default-items.md)

## 実装・完了条件

03_DATAの全フィールドを先に定義。後段でJSON形を変えない。ClipItemはINotifyPropertyChanged、Cloneは独立、表示プロパティはJsonIgnore。
SettingsStoreは試験用にパスを差し替えられる内部経路を用意し、本物のsettingsを触らず確認。欠損/破損/読取り不能を区別。
SettingsStructureはID/カテゴリ/両レイアウト/Items順の整合を担当。初回15項目を作る。
小分け: 型と初回データ→保存と復旧→レイアウト正規化。
確認: JSON往復、Category名だけの旧形式移行、空/重複ID、同名カテゴリ統合、null Items要素、存在しない参照、通常/条件付き独立順序、Normalize2回目は変化なし。ロック時は元ファイル不変。

## 終了時

ビルドと指定の確認を実施。PROGRESSを更新し、未接続/未確認を残す。次段階へ進まない。トークン不足なら小分けで区切り、次の開始点を具体的に記録する。
