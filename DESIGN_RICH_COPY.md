# 書式付きコピー / クリップボード丸ごと保持 検討メモ

「コピーしたものの形式を保持したい」の検討。

**この文書は一度書き直している。** 初版は「v1 は最小限に絞り、Markdown ライブラリは入れず、
`{clip}` の書式保持は範囲外」という切り詰めた判断だった。
その後「労力には見合わないが、面白いので可能な限り対応したい」という方針が示されたため、
**削る前提をやめて、届く範囲を全部書く形に改めた。**

初版の判断を残しておく意味はある。あとから「なぜこんなに広げたのか」を辿れるようにするためと、
やはり重いと感じたときに戻る先が要るため。**§10 に、削るならどこから削るかを残してある。**

---

## 1. 二つの方向と、それが出会う場所

「形式を保持する」には方向が二つある。

| | 何をするか | 例 |
| --- | --- | --- |
| **出力側** | 項目のテンプレートから、複数の形式を組み立てて載せる | `# 議事録` と書いた項目が、Word では見出しになる |
| **入力側** | 他アプリでコピーされた内容を、載っている形式ごと捕まえて保持し、あとで復元する | Excel の範囲をコピー → 別作業 → もう一度あの表を罫線ごと貼る |

**両方やると、この二つは真ん中で出会う。**

どちらも最終的な仕事は「複数の形式を積んだ `DataObject` を作ってクリップボードへ載せる」で同じ。
違うのは中身の出どころだけ。片方はテンプレートから生成し、もう片方は保存済みの実体を戻す。

```
テンプレート ──展開──> 文字列 ──変換──┐
                                        ├──> DataObject ──> クリップボード
スナップショット ──読み出し──────────┘
```

**この合流点を先に決めておくと、両方の実装がほぼ同じ場所に集まる。**
逆に片方ずつ作ると `ClipboardService` を二度作り直すことになる。

### 1-1. 両方やったときにだけ生まれるもの

合流させると、片方だけでは出てこない機能がただで手に入る。

**スナップショットをテンプレートへ昇格させられる。**

Excel の表をコピー → 丸ごと保存すると、TSV と HTML の両方が手元に残る。
その HTML の中に `{date}` を書き込めば、**罫線付きの表を毎回日付だけ差し替えて貼れる項目**になる。
「今週の進捗表」のようなものが、一度コピーするだけで作れる。

これは出力側だけでも入力側だけでも作れない。
出力側だけなら HTML を手書きすることになり、入力側だけなら中身を変えられない。

**逆向きも効く。** 出力側で作った HTML が正しいかどうかは、
入力側の「クリップボードに何が載っているか見る」機能でそのまま確認できる。
§9 の実機確認が、専用のデバッグコードなしに回せるようになる。

---

## 2. 現状

`ClipboardService.TryCopy()` は `Clipboard.SetText()` を呼んでいる。
これはプレーンテキスト（`CF_UNICODETEXT`）1 本だけを載せる。

```csharp
return TryRun(() => System.Windows.Clipboard.SetText(text));
```

読み取り側の `GetText()` も同じくプレーンテキストしか見ていない。

なので `{date:yyyy/MM/dd} 定例ミーティング 議事録` を `# ` 付きでコピーしても、
Word に貼れば `# 2026/08/04 …` という文字がそのまま入る。見出しにはならない。

初期設定の「議事録の見出し」（`AppSettings.CreateDefault`）が既に Markdown なので、
**この不満は既定の項目で踏める。**

---

## 3. 出力側: 1 回のコピーに複数の形式を載せる

`SetText` をやめて `DataObject` を使う。

```csharp
var data = new System.Windows.DataObject();
data.SetData(DataFormats.UnicodeText, plainText); // 素のまま
data.SetData(DataFormats.Html, cfHtml);           // 装飾付き
System.Windows.Clipboard.SetDataObject(data, copy: true);
```

`copy: true` を忘れないこと。省略すると**アプリを終了した時点でクリップボードが空になる**。
常駐アプリなので普段は気付かないが、「終了」を選んだ直後の貼り付けが壊れる。

### 3-1. 主な形式と、効く貼り付け先

| 形式 | 実際の文字列 | 効く貼り付け先 |
| --- | --- | --- |
| `UnicodeText` | `UnicodeText` | 全部（最後の受け皿） |
| `Html` | `HTML Format` | Word / Outlook / Excel / Slack / Teams / Notion / ブラウザ |
| `Rtf` | `Rich Text Format` | Word / Outlook / ワードパッド |
| TSV | （プレーンテキストのまま） | Excel・スプレッドシートにセル分割で入る |
| `CommaSeparatedValue` | `CSV` | Excel |
| `Dib` | `DeviceIndependentBitmap` | Office 系が好む画像形式 |
| `FileDrop` | `FileDrop` | エクスプローラー（`string[]` のパス） |
| `Xaml` / `XamlPackage` | 同名 | WPF の `RichTextBox` 間 |

**どの形式が使われるかは、こちらから指定できない。** 貼り付け先が選ぶ。
だから「候補を並べておく」のが基本形になる。

### 3-2. 自動変換される組み合わせがある

`Text` / `UnicodeText` / `StringFormat` は Windows と .NET が相互に補完する。
**両方セットする必要はない。** `UnicodeText` だけ載せれば `Text` を要求するアプリにも届く。
`Bitmap` ↔ `Dib`、`MetafilePicture` ↔ `EnhancedMetafile` も同様。

一方 **`Html` と `Rtf` は自動変換されない。** 欲しければ自分で両方作る。

この非対称は入力側にも効く（§6-1）。

### 3-3. 貼り付け先ごとに結果が変わる例

同じ 1 回のコピーで、貼り付け先ごとに結果が変わる。

```
項目のテキスト: "# 議事録\n\n- 決定事項\n- 宿題"

  メモ帳 / VS Code   → CF_UNICODETEXT を選ぶ → Markdown の元テキストがそのまま入る
  Word / Outlook     → CF_RTF または CF_HTML → 見出しと箇条書きになる
  Slack              → CF_HTML を選ぶ         → 書式付きで入る
```

**これが嬉しいところ**で、貼り付け先ごとに項目を分ける必要がない。1 項目で全部に対応できる。

---

## 4. 最大の落とし穴: CF_HTML のヘッダ

`DataFormats.Html` に生の HTML を渡しても効かない。
Windows のクリップボード HTML 形式は、先頭に**バイトオフセット付きのヘッダ**を要求する。

```
Version:0.9
StartHTML:00000097
EndHTML:00000219
StartFragment:00000131
EndFragment:00000185
<html><body>
<!--StartFragment--><h1>2026/08/04 定例ミーティング 議事録</h1><!--EndFragment-->
</body></html>
```

**.NET はこのヘッダを作ってくれない。** `Clipboard.SetText(html, TextDataFormat.Html)` も同じ。
自前で組み立てる必要がある。

### 4-1. オフセットは「文字数」ではなく「UTF-8 のバイト数」

ここが日本語で必ず踏む地雷。実際に検算した結果:

| 断片 | 文字数ベースで切り出した Fragment |
| --- | --- |
| `<p>hello</p>` | `<p>hello</p>` ← たまたま合う |
| `<h1>2026/08/04 定例ミーティング 議事録</h1>` | `<h1>…</h1><!--EndFragment-->\r\n</` ← **ずれる** |
| `<table><tr><td>あ</td>…` | `<table>…</table><!--` ← **ずれる** |

ASCII だけで試すと動いてしまうので、**テストは必ず日本語で行う**こと。
英語のサンプルコードをそのまま持ってくると、この段階では気付けない。

### 4-2. 組み立て方

ヘッダの中に自分の長さが含まれるので、素直に書くと循環する。
**桁数を固定（8 桁）**すれば、ダミー値で長さを測れる。

```csharp
private const string HeaderTemplate =
    "Version:0.9\r\n"
    + "StartHTML:{0:00000000}\r\n"
    + "EndHTML:{1:00000000}\r\n"
    + "StartFragment:{2:00000000}\r\n"
    + "EndFragment:{3:00000000}\r\n";

private const string Pre  = "<html><body>\r\n<!--StartFragment-->";
private const string Post = "<!--EndFragment-->\r\n</body></html>";

private static string BuildCfHtml(string fragment)
{
    // 桁数固定なので、ダミー値で埋めたヘッダと本番のヘッダは同じ長さになる
    int headerLength = Encoding.UTF8.GetByteCount(
        string.Format(HeaderTemplate, 0, 0, 0, 0));

    int startHtml     = headerLength;
    int startFragment = startHtml + Encoding.UTF8.GetByteCount(Pre);
    int endFragment   = startFragment + Encoding.UTF8.GetByteCount(fragment);
    int endHtml       = endFragment + Encoding.UTF8.GetByteCount(Post);

    return string.Format(HeaderTemplate, startHtml, endHtml, startFragment, endFragment)
        + Pre + fragment + Post;
}
```

`Encoding.UTF8.GetByteCount` を使うところが肝。`string.Length` は使わない。

### 4-3. 文字列で渡すか、バイト列で渡すか — **決着済み**

もう 1 段ある。ヘッダのオフセットを UTF-8 バイトで計算しても、
**.NET がクリップボードへ書き込むときの符号化が UTF-8 でなければ意味がない。**

当初は「確実を期すなら UTF-8 のバイト列を明示する」と考えていた。
**実測の結果、これは逆だった。文字列で渡すのが正しい。**

```csharp
// これでよい。.NET が UTF-8 で書き、NUL 終端まで付けてくれる
data.SetData(DataFormats.Html, cfHtml);
```

#### 実測

組み立てた CF_HTML はちょうど 321 バイト（ヘッダの `EndHTML:00000321` と一致）。
これを載せてから読み戻した結果:

| 渡し方 | Win32 で読んだ実サイズ | WPF で読み戻したサイズ |
| --- | --- | --- |
| **`string`** | **322 B**（+1） | **321 B** — 完全一致 |
| `MemoryStream`（UTF-8 バイト列） | 321 B | **320 B** — 1 バイト欠ける |

**文字列で渡すと NUL 終端が 1 バイト付く。** だから実体は 322 バイトで、
読み戻すとちょうど 321 バイトに戻る。

**バイト列で渡すと終端が付かない。** 読み戻すと末尾 1 バイト、
つまり `</html>` の `>` が落ちる。読む側が「最後の 1 バイトは終端だろう」と
仮定して切り落とすためで、**CF_HTML は NUL 終端が慣習**なので、その仮定のほうが正しい。

ヘッダの `EndHTML` を見て切り出す相手なら問題は起きないが、
**終端を前提にする相手には壊れたデータを渡すことになる。** 避ける理由がない。

#### 符号化の懸念も同時に消えた

Win32 で読んだ生のバイト列を UTF-8 として解釈したところ、
日本語がそのまま読めた。**文字列で渡しても .NET は UTF-8 で書いている。**
ヘッダをバイト数で数える前提（§4-1）と食い違わない。

`DataFormats.Html` は .NET 側で特別扱いされていて、
他のテキスト形式と違って UTF-8 で書き出される、と考えると辻褄が合う。

バイト列で渡したい場合は、**末尾に NUL を 1 バイト足せばよい**
（`ClipboardProbe` のメニュー 9 がその版）。
ただし文字列で渡すほうが短く、同じ結果になる。

---

## 5. 出力側: どこまで全部盛りにするか

初版では「v1 は最小実装」としていた部分。方針変更に合わせて広げる。

### 5-1. Markdown は Markdig を入れる

初版では「このプロジェクトは `PackageReference` が 1 つも無いので、
自前の最小実装（見出し・箇条書き・太字程度）で足りる」としていた。**これを撤回する。**

可能な限り対応するなら、自前実装の対応表を延々と育てることになる。
それは面白さの中身ではなく、単なる写経になる。CommonMark の仕様に忠実であることに
面白みは無いので、**そこはライブラリに任せて、面白い部分に時間を使うほうがよい。**

| | 自前で最小実装 | Markdig を入れる |
| --- | --- | --- |
| 依存 | 増えない | 初の外部依存 |
| 対応範囲 | 見出し・箇条書き・太字程度 | CommonMark 全部 + 拡張（表・脚注・打ち消し線・タスクリスト） |
| 実装量 | 100 行くらい | ほぼゼロ |
| 保守 | 記法を足すたびに自分で書く | 不要 |
| 配布 | 単一 exe のまま | DLL が増える（`PublishSingleFile` で吸収可能） |

**外部依存が初めて入ることは、それ自体を判断として記録しておく。**
`MyTaskTray.csproj` が標準ライブラリだけで完結していたのは偶然ではなく、
配布の手軽さという性質だった。`PublishSingleFile` でその性質は保てるが、
「もう依存ゼロではない」ことは覚えておく。

変換部分は `IMarkdownRenderer` のような形で切っておく。
気が変わったときに差し替えが局所で済む。

> **実装時の判断（済）**
> 採用したのは **Markdig 1.2.0**。`net10.0` 向けの依存パッケージが 0 個だったので、
> 増える DLL はこれ 1 つだけで済んだ。ライセンスは BSD-2-Clause。
>
> インターフェースは切らず、`Services/MarkdownRenderer.cs` の 1 ファイルに閉じ込める形にした。
> 実装が 1 つしかなく、このアプリの他のサービス（`ClipboardService`、`TemplateEngine`、
> `SettingsStore`）がすべて static なので、そちらに揃えるほうが読みやすい。
> 差し替えるときに触るのは結局このファイルだけなので、目的は達している。
>
> 拡張は `UseAdvancedExtensions()` を有効にした。表・脚注・打ち消し線・タスクリストまで通る。

### 5-2. RTF も生成する

**Word と Outlook は、状況によって CF_HTML より CF_RTF を優先する。**
HTML だけだと「Word でだけフォントが変になる」といった形で表面化する。

RTF は仕様が古く、素直に書ける範囲は狭いが、
このアプリが出すもの（見出し・太字・箇条書き・表）に限れば手書きで足りる。
骨格はこれだけで済む。

```
{\rtf1\ansi\deff0{\fonttbl{\f0 Yu Gothic;}}
\b bold \b0 normal\par}
```

**問題は日本語。** RTF は本来 7bit ASCII の形式で、
そのまま日本語を書くことはできない。`\uNNNN?` の形にエスケープする。
`NNNN` が Unicode のコードポイント（十進数）、末尾の `?` は
RTF を解釈できない環境向けの代替文字。

```
太字      →  \u22826?\u23383?
```

**ここに罠が 1 つある。`NNNN` は符号付き 16bit で書く。**
つまり 32767 を超えるコードポイントは**負の数**になる。

| 文字 | コードポイント | RTF での書き方 |
| --- | --- | --- |
| `太` | U+592A (22826) | `\u22826?` |
| `高` | U+9AD8 (39640) | `\u-25896?` ← 65536 を引く |

**`高` `通` `雑` のような U+8000 以降の漢字は日常的に出てくる。**
ここを符号なしのまま書くと、その文字だけ化ける。
`太字` だけでテストすると通ってしまうので、**`高` を含む文字列で確認する。**

サロゲートペア（絵文字など）は 2 つに分けて書く。

優先度は CF_HTML の次。**HTML が通ってから着手する。**

### 5-3. 表

`Table` 形式は「タブ区切り・改行区切りのテキスト」を入力とする。

```
項目	担当	期限
設計	山田	{date+7}
実装	佐藤	{date+14}
```

これを複数の形式で載せる:

| 形式 | 中身 | 効果 |
| --- | --- | --- |
| `UnicodeText` | タブ区切りのまま | **Excel がセルに割ってくれる** |
| `Html` | `<table>` | Word / Slack で罫線付きの表になる |
| `Rtf` | `\trowd` の表 | Word |
| `CommaSeparatedValue` | CSV | 保険 |

先頭行をヘッダ（`<th>`）にするかどうかは設定で切り替える。既定は「ヘッダあり」でよい。

**注意: HTML を一緒に載せると、Excel が HTML を優先して 1 セルに収めてしまう可能性がある。**
TSV でセルに割ってほしいのに `<table>` が勝つと、目的が逆転する。§9-3 で確認する。
もし競合するなら、表の項目では HTML を載せない設定を用意する。

### 5-4. 設定画面でタブ文字をどう入力するか

タブをテキストボックスに打つとフォーカスが移動してしまう。

- 案 A: 入力中だけ Tab をフォーカス移動に使わない（`AcceptsTab="True"`）
- 案 B: 区切り文字をカンマにして、内部でタブへ変換する
- 案 C: 専用のグリッド入力欄を作る

**案 A が一番安い。** 属性 1 つで済む。
ただし「Tab で次の欄へ行けない」ことに戸惑うので、欄の下に一言添える。
案 C は §7 のスナップショット取り込みができれば需要そのものが減るので、後回しでよい。

### 5-5. 展開と変換の順序

`TemplateEngine.Expand()` で差し込みを展開してから、その結果を変換する。逆にはしない。

```
item.Text ──Expand──> 展開済みテキスト ──変換──> HTML / RTF
                            │
                            └──────────────────> プレーンテキスト
```

理由: 先に HTML 化すると、`{clip}` に `<` や `&` を含む文字列が入ってきたときに
タグとして解釈されてしまう。**展開後の値は必ずエスケープを通す。**
`{input:タイトル}` に `A & B` と入力しただけで壊れる。地味だが確実に踏む。

**ただし §7-8 のスナップショット由来テンプレート（中身が既に HTML）では話が逆になる。**
テンプレート本体はエスケープせず、差し込まれた値だけをエスケープする必要がある。
`TemplateEngine` に「差し込み値の後処理」を挟む口が要る。ここが設計上の要注意点。

---

## 6. 入力側: クリップボードを丸ごと捕まえる

ここからが初版に無かった部分。

### 6-1. 何が載っているかを列挙する

```csharp
var data = System.Windows.Clipboard.GetDataObject();
foreach (string format in data.GetFormats(autoConvert: false))
{
    object? value = data.GetData(format, autoConvert: false);
}
```

**`autoConvert: false` にすること。** `true` にすると §3-2 の自動変換で
合成された形式まで並び、実体が無いものを保存しようとして無駄が増える。
**元のアプリが実際に載せた形式だけ**を対象にする。

Excel の範囲をコピーすると、だいたいこうなる:

```
Biff12 / Biff8 / XML Spreadsheet / Csv / Rich Text Format / HTML Format /
UnicodeText / Text / DeviceIndependentBitmap / EnhancedMetafile /
Object Descriptor / Ole Private Data / Link Source / …
```

20 個以上並ぶことも珍しくない。**全部を持ち帰るのは正しくない**（§6-3）。

### 6-2. 返ってくる型がばらばら

`GetData()` の戻りは `object?` で、形式によって中身が違う。

| 形式の種類 | 実際に返るもの |
| --- | --- |
| テキスト系 | `string` |
| `FileDrop` | `string[]`（パスの配列） |
| 画像系 | `BitmapSource` |
| その他ほぼ全部 | `MemoryStream` |
| 一部 | `null`（載っているのに取れない） |

**`null` が返る形式は普通にある。** 例外を投げる形式もある。
1 つの形式で失敗しても他は使えるので、**形式ごとに try で囲んで、失敗はスキップする。**
ここで全体を諦めると、Excel の表がゴミ形式 1 つのせいで保存できなくなる。

### 6-3. 持ち帰ってはいけない形式がある

OLE 関連の形式は、**コピー元のプロセスやドキュメントを指しているだけ**で、
中身を持っていない。

```
Ole Private Data / Object Descriptor / Link Source / Link Source Descriptor /
Embed Source / OwnerLink / ObjectLink / Native
```

これを保存して後から復元すると、Word が閉じたドキュメントへリンクを張ろうとして、
最悪の場合ダイアログが出て固まる。**害があるので保存しない。**

**除外リスト（denylist）ではなく、許可リスト（allowlist）にする。**
知らない形式が出てきたときに「とりあえず保存」だと、この手の地雷を踏み続けることになる。
知っている形式だけを持ち帰り、**残りは「保存しなかった形式」として画面に出す。**
出しておけば、必要なものが漏れていたときに気付ける。

初期の許可リスト（**実測を反映済み。経緯は §6-9**）:

```
UnicodeText / HTML Format / Rich Text Format / CSV / XML Spreadsheet /
FileDrop / Bitmap / DeviceIndependentBitmap / PNG / Xaml / XamlPackage / Biff12
```

`Biff12` は Excel 専用形式で、**入れると「Excel から Excel へ完全に戻せる」**ようになる。
初版では「中身が不透明で大きいので様子見」としていたが、
実測すると 14.1 KB と最も小さかったので**入れる**ことにした。

### 6-4. 遅延レンダリングという罠

**これが入力側で一番厄介なところ。**

アプリは形式を「載せる」ときに、実際のデータを作らず**名前だけ登録できる**。
実データは貼り付け側が要求した瞬間に、コピー元のプロセスが作って返す。
Office はこれを多用する（毎回 20 個の形式を全部作っていたら重すぎるため）。

**帰結:**

- コピー元のアプリが終了していると、`GetData()` は `null` を返すか失敗する
- 全形式を舐めると、コピー元のプロセスに 20 回仕事をさせることになり、**重い**
- 大きな範囲では、1 形式の生成に数秒かかることがある

**対策:**

- 捕まえる操作は**その場で完結させる**。コピー元が生きているうちに全部読む
- 許可リストで対象を絞る（§6-3）。これは正しさだけでなく**速さの問題でもある**
- 時間がかかる可能性があるので、**UI を止めない**。捕獲中はトーストで知らせる
- 取れなかった形式は、無かったことにして続行する

### 6-5. 大きさ

Excel の範囲を丸ごと持つと、形式によっては数 MB になる。画像系が特に大きい。

> **実測で分かったこと（§6-9）**
> 画像は `Bitmap` として来て、無圧縮なら 4 MB を超えた。
> **PNG に圧縮して保存する**ことで数百 KB に収まるので、上限を上げる必要はない。
> 表のデータ自体は全部合わせても 90 KB 弱だった。

`settings.json` は `WriteIndented = true` で、
**手で編集できることを設計の前提にしている**（`SettingsStore` のコメントのとおり）。
ここに base64 で数 MB を書き込むと、その前提が壊れる。開けないファイルになる。

**別の置き場を用意する。**

```
%AppData%\MyTaskTray\
    settings.json          ← これまでどおり。人が読める大きさを保つ
    snapshots\
        {snapshotId}\
            index.json     ← 形式名 → ファイル名、大きさ、捕獲日時
            0.bin          ← 形式ごとの実体
            1.bin
```

形式名をそのままファイル名にしない。`Rich Text Format` は通るが、
アプリ独自の形式名には `/` や `:` が混ざりうる。**連番にして `index.json` で対応づける。**

**上限を設ける。**

| 対象 | 既定値 |
| --- | --- |
| 1 形式あたり | 4 MB |
| 1 項目あたり合計 | 16 MB |
| スナップショット総数 | 100 件 |

超えた形式はスキップし、「大きすぎて保存しなかった」と画面に出す。
黙って落とすと、あとで復元したときに「なぜか罫線が戻らない」になる。

### 6-6. 復元する

保存した形式を、保存したときの型で戻す。

```csharp
var data = new System.Windows.DataObject();
foreach (var entry in snapshot.Entries)
{
    data.SetData(entry.Format, entry.ToClipboardValue());
}
System.Windows.Clipboard.SetDataObject(data, copy: true);
```

- `MemoryStream` で保存したものは `MemoryStream` で戻す。**毎回新しく作る**
  （ストリームは読むと位置が進むので、使い回すと 2 回目が空になる）
- `FileDrop` は `string[]` に戻す。**元のファイルが消えている可能性**があるので、
  存在しないパスは除く。全部消えていたらその形式ごと落とす
- 画像は `BitmapSource` に戻す
- 自動変換に任せる形式（`Text` など）は、そもそも保存していないので何もしない

### 6-7. プライバシー: 捕まえてはいけない中身がある

**丸ごと保存する機能を作る以上、ここは避けて通れない。**

パスワード管理ソフトは、クリップボードに「これを履歴に残すな」という印を付ける。
Windows 標準のクリップボード履歴はこれを尊重している。
**尊重しない実装は、Windows 標準より危険なものになる。**

| 形式名 | 意味 |
| --- | --- |
| `ExcludeClipboardContentFromMonitorProcessing` | 監視ソフトは触るな |
| `CanIncludeInClipboardHistory` | DWORD が 0 なら履歴に残すな |
| `CanUploadToCloudClipboard` | DWORD が 0 ならクラウドへ上げるな |
| `Clipboard Viewer Ignore` | 古い時代からの同じ意味の慣習 |

**これらが 1 つでも載っていたら、捕獲そのものを断る。**
「保存できません（コピー元が保存を許可していません）」と出す。
判定は許可リストの照合より**先**に行う。

加えて:

- 保存したスナップショットは**設定画面から個別に削除できる**必要がある
- 「全部消す」ボタンも要る
- 項目を削除したら、対応する `snapshots\{id}` フォルダーも消す。
  消し忘れると `%AppData%` に数十 MB が残り続ける

### 6-8. いつ捕まえるか — 監視はしない

自動で捕まえるなら `AddClipboardFormatListener` で `WM_CLIPBOARDUPDATE` を受ける。
だが**このアプリの既存の姿勢に反する。**

`TrayIconManager.CopyToClipboard` にこう書いてある:

> クリップボードを開くと他アプリのコピー操作を妨げるうえ、ロックされていると
> 再試行のあいだ操作が止まる。`{clip}` を使う項目でだけ読みに行く。

**常時監視はこの判断の正反対**で、しかも §6-4 のとおり全形式の読み出しは重い。
コピーのたびに他アプリへ 20 回の仕事をさせる常駐アプリは、
利用者から見て「なんか重い」の原因そのものになる。

**したがって、捕獲は手動にする。** 既存の「クリップボードを項目に追加」と同じ形で、
メニューに 1 つ足す。ホットキーからも呼べるので、操作としては十分に速い。

```
クリップボードを項目に追加(&A)...        ← 既存。テキストのみ
クリップボードを書式ごと追加(&F)...      ← 追加。載っている形式を全部
```

監視方式は「履歴機能」を作りたくなったときに改めて考える。
そのときは `GetClipboardSequenceNumber()` で**自分の書き込みを無視する**仕掛けが要る
（自分でコピー → 監視が反応 → また保存、の無限ループを避けるため）。

### 6-9. 実測結果（Excel の範囲をコピーした状態）

`tools/ClipboardProbe` で実際に測った。**ここまでの想定のうち 3 つが外れていた。**

| 形式 | 判定 | 型 | 大きさ | ms |
| --- | --- | --- | --- | --- |
| `EnhancedMetafile` | 重複 | Metafile | – | 54 |
| `MetaFilePict` | 自動変換 | null | – | 57 |
| `Bitmap` | **保存** | BitmapSource | 1264x844 | 40 |
| `Biff12` | **保存** | MemoryStream | 14.1 KB | 4 |
| `Biff8` | 重複 | MemoryStream | 32.8 KB | 5 |
| `Biff5` | 重複 | MemoryStream | 52.7 KB | 10 |
| `SymbolicLink` | 重複 | MemoryStream | 48.0 KB | 1 |
| `DataInterchangeFormat` | 重複 | MemoryStream | 4.6 KB | 14 |
| `XML Spreadsheet` | 保存 | MemoryStream | 39.2 KB | 44 |
| **`HTML Format`** | 保存 | **null** | – | 55 |
| `UnicodeText` | 保存 | string | 1.6 KB | 6 |
| `Text` | 自動変換 | string | 1.6 KB | 0 |
| `Csv` | 保存 | MemoryStream | 1.3 KB | 0 |
| `ShadowWorkbook` | 重複 | null | – | 9 |
| `Web Custom Format Map` | 重複 | MemoryStream | 41 B | 0 |
| `Rich Text Format` | 保存 | string | 32.2 KB | 7 |
| `Object Descriptor` | 除外(OLE) | null | – | 1 |
| `Format129` | 重複 | MemoryStream | 14 B | 0 |

18 形式 / 読み取り合計 307 ms。

#### 外れていたこと 1: Excel の `HTML Format` が読めない ← **一部未解決**

**Excel は HTML を載せているのに、`GetData("HTML Format", autoConvert: false)` が null を返す。**

`Rich Text Format` は同じ経路で string として読めているので、
「テキスト系が全部だめ」という話ではない。

**切り分けの結果、範囲は狭まった。**

自分で載せた CF_HTML は、メニュー 8 の 4 経路すべてで問題なく読めた。

```
  WPF GetData(autoConvert:false) :  321 B  string
  WPF GetData(autoConvert:true)  :  321 B  string
  Clipboard.GetText(Html)        :  321 B
  Win32 直読み                    :  322 B
```

つまり **出力側は健全**で、`HTML Format` という形式そのものが
.NET から扱えないわけでもない。**問題は Excel が載せたデータに固有**ということになる。

**残っている確認は 1 つだけ。** Excel でコピーした状態でメニュー 8 を実行する。

| Win32 直読みの結果 | 意味 | 本体の実装 |
| --- | --- | --- |
| 読める | データはある。.NET の包み方が問題 | 読み取りは `RawClipboard.cs` を使う |
| 読めない | Excel は名前だけ登録して実体を渡していない | HTML は諦め、RTF と XML Spreadsheet で代替する |

**どちらに転んでも致命傷にはならない。** Excel の表を Word へ貼る用途は
`Rich Text Format`（28.6 KB）が担い、Excel へ戻す用途は `Biff12` と `XML Spreadsheet` が担う。
HTML が要るのは Slack や Notion へ貼る場面に限られる。

#### 外れていたこと 2: `DeviceIndependentBitmap` が載っていない

許可リストは「DIB を保存すれば Windows が `Bitmap` を合成してくれる」という前提だった。
**実測では DIB は載っておらず、`Bitmap` だけが来た。**
前提が成り立たないので、**来たほうを保存する**形に直した。

さらに大きさの問題がある。1264x844 を無圧縮で持つと **4 MB を超え、§6-5 の上限に引っかかる。**
**画像は PNG に圧縮して保存する。** 同じ絵が数百 KB に収まる。
上限を引き上げるのではなく、持ち方を変えるほうが筋がよい。

#### 外れていたこと 3: 「未分類」が多すぎた

初版の分類では 9 形式が「未分類」になった。これでは判断材料にならない。
**「知らないから未分類」と「知ったうえで捨てている」を分ける**必要がある。
`FormatVerdict.DropRedundant`（除外(重複)）を足して振り分けた。

- `Biff5` / `Biff8` / `Biff12` は同じ内容の新旧。**新しいものほど小さい**（52.7 → 32.8 → 14.1 KB）。
  `Biff12` だけ持てばよい
- `SymbolicLink` は OLE のリンクではなく **SYLK という表計算の交換形式**。
  害はないが 48 KB を占めるだけなので捨てる
- `DataInterchangeFormat` は DIF。同じく古い交換形式
- `Format129` は名前を持たない定義済み形式で、129 は `CF_DSPTEXT`
- `Web Custom Format Map` はブラウザが独自形式を名前で引くための対応表

**これで保存対象は 253 KB**（うち画像が 177 KB）。上限案の 16 MB に対して十分小さい。
`Biff12` は 13.9 KB しかないのに、Excel から Excel への完全な復元が付いてくる。**入れる。**

別の測定では `Biff5` が 101.6 KB まで膨らんだのに対し `Biff12` は 13.9 KB のままだった。
**新しい形式ほど小さい**という関係は安定している。

#### PNG 圧縮の効果

画像は 1163x720 で、**無圧縮なら 3.19 MB、PNG なら 176.9 KB。約 18 分の 1** になった。
無圧縮のままだと 1 形式 4 MB の上限に迫るが、PNG なら気にしなくてよい。
**上限を上げるのではなく持ち方を変える**という判断は正しかった。

#### 想定どおりだったこと

- **`Object Descriptor` は null を返した。** OLE 系を除外する判断は正しい
- **`autoConvert: false` は有効だった。** `Text` と `UnicodeText` が両方並んだので、
  Excel が実際に両方載せていることが分かる。`Text` を捨てても合成で戻る
- **読み取り時間は 307 ms。** この規模なら UI を止めても耐えられる。
  ただし 1000 行規模での測定はまだ（§9-6）

---

## 7. データモデルの統合

### 7-1. 項目の中身が 2 種類になる

```csharp
public enum ClipPayloadKind
{
    Template, // これまでどおり。Text を展開してコピーする
    Snapshot, // 保存済みの形式一式を復元する
}

public enum ClipFormat
{
    Plain,    // 従来どおり。プレーンテキストのみ
    Markdown, // Markdown として解釈し、HTML / RTF も載せる
    Html,     // Text を HTML として扱う
    Table,    // タブ区切りとして解釈し、表として載せる
}
```

`ClipItem` に足すのは 3 つ。

| プロパティ | 既定値 | 使う場面 |
| --- | --- | --- |
| `PayloadKind` | `Template` | 常に |
| `Format` | `Plain` | `Template` のとき |
| `SnapshotId` | 空文字 | `Snapshot` のとき |

**どちらも既定値が従来の挙動と一致する。**
古い設定ファイルには無いので自動的にそうなり、挙動は一切変わらない。
`MenuHotKey` を足したときと同じで、`Version` の変更は不要。

### 7-2. 継承にはしない

`TemplateItem` / `SnapshotItem` に分けたくなるが、**やらない。**

- `System.Text.Json` の多態シリアライズは型識別子が要り、
  **手で編集できる設定ファイル**という性質を損なう
- 設定画面は `ClipItem` の平坦なリストとして扱っている。
  並べ替え、複製、カテゴリ、表示条件は種類によらず共通で、分けると全部が二重になる
- `Clone()` が 2 系統になる

**フィールドを足して分岐するほうが、この規模では素直。**
`Clone()` への追加を忘れないこと。設定画面は複製を持って編集するので、
ここが漏れると「設定したのに保存されない」になる。

### 7-3. 合流点

`ClipboardService` の入口を `DataObject` に統一する。

```csharp
// 生成側（種類ごとに分かれるのはここまで）
DataObject data = item.PayloadKind switch
{
    ClipPayloadKind.Template => ClipboardPayloadBuilder.FromTemplate(expandedText, item.Format),
    ClipPayloadKind.Snapshot => SnapshotStore.Load(item.SnapshotId).ToDataObject(),
    _ => throw new…
};

// 載せる側（ここから先は共通。再試行も空判定もここに集約する）
ClipboardService.TryCopy(data);
```

**既存の `TryCopy(string)` は残す。** 中で `DataObject` を組んで新しい方へ渡す。
`SettingsWindow` からの呼び出し（プレビューのコピー）がそのまま動く。

---

## 8. 既存機能との噛み合わせ

### 8-1. 空文字列

`TryCopy` は空文字列のとき `Clipboard.Clear()` を呼び分けている。**この分岐は残す。**
空の `DataObject` を載せると、「空文字がコピーされた状態」と
「クリップボードが空の状態」が食い違って `{clip}` 側の判定が狂う。

スナップショット側でも、**形式が 1 つも保存できなかった項目**は同じ扱いにする。

### 8-2. 再試行

`TryRun` の再試行（5 回 / 80ms）はそのまま活かす。
`SetDataObject` も他プロセスがロックしていれば同じように失敗する。包む対象を差し替えるだけ。

**捕獲側にも同じ再試行が要る。** `GetDataObject()` も同様に失敗する。

### 8-3. `{clip}` は書式を読めるか

初版では「範囲外」とした。入力側を作るなら**技術的には手が届く**が、
それでも**やらないほうがよい**と考える。

- 読み込んだ HTML には貼り付け元固有のゴミ（`<span style="mso-…">` の類）が大量に混ざる
- それを自前の HTML に差し込むと、噛み合う保証がない。
  スタイルが漏れて、貼り付け先で予想外の見た目になる
- `TemplateEngine` は文字列を扱う設計で、構造を持ち込むと影響が広い

**代わりに §7-1 のスナップショットが同じ需要を満たす。**
「コピーした表を書式ごと使いたい」なら、丸ごと保存してそれを貼ればよい。
差し込みは今までどおりプレーンテキストで受け取る。この線引きなら既存の設計を崩さない。

### 8-4. クイック追加（`QuickAddWindow`）

既存の入口はテキストのみのまま残す。**書式ごとの捕獲は別の入口にする**（§6-8）。

既存の入口には、`TemplateEngine.EscapeLiteral` で波かっこを退避する処理と、
同じ内容の項目を探す `FindItemByText` がある。

**スナップショット側では、この重複検出をどうするか決める必要がある。**
バイト列を比較すると、Excel が毎回わずかに違う形式を吐くだけで別物になる。
**プレーンテキスト部分だけで突き合わせる**のが現実的。

### 8-5. 通知とプレビュー

`ToastWindow` と設定画面の `TextPreview` は、**プレーンテキスト側を出す。**
装飾を再現しようとすると、通知のためにレンダリングすることになって割に合わない。

ただし「どの形式でコピーしたか」は分かったほうがよい。

```
議事録の見出し (Markdown)
# 2026/08/04 定例ミーティング 議事録

今週の進捗表 (書式ごと・5 形式)
項目	担当	期限 …
```

スナップショットは表示名の材料が無いので、**捕獲時にプレーンテキストを抜いて
`ClipItem.Name` の初期値と `TextPreview` に使う。**
プレーンテキストが無い（画像だけ）場合は「画像 (1024×768)」のような説明を作る。

### 8-6. 設定画面

スナップショット項目は `Text` を編集できない。**編集欄を隠して、代わりに出す:**

- 保存されている形式の一覧と、それぞれの大きさ
- 保存しなかった形式と、その理由（許可リスト外 / 大きすぎ / 取得失敗）
- 「取り直す」ボタン（いまのクリップボードで上書き）
- 「テンプレートに変換」ボタン（§7-1 の昇格、下記）

### 8-7. スナップショットからテンプレートへの昇格

§1-1 の目玉。スナップショットの HTML を取り出して、
`Format = Html` のテンプレート項目に変換する。

- 変換後は `Text` が HTML 文字列になる。**設定画面で編集できる**
- そこに `{date}` などを書き込めば、差し替えの効く書式付き項目になる
- **§5-5 の後半のとおり、この場合はテンプレート本体をエスケープしてはいけない。**
  差し込まれた値だけをエスケープする
- 元のスナップショットは残す（変換を戻せるように）

Office が吐く HTML は長大なので、**変換時に整理する**手が要る。
`<style>` ブロックと `mso-` で始まる属性を落とすだけでも、かなり読めるようになる。
完璧を目指さず、「手で直せる程度の長さになればよい」とする。

---

## 9. Windows 実機で確認が必要なこと

上から順に、確認できないと先へ進めない順。

**確認用のツールを `tools/ClipboardProbe/` に用意した。**
本体には手を入れず、`dotnet run` してメニューから選ぶだけで下の項目を試せる。
どの番号がどの項目に対応するかは `tools/ClipboardProbe/README.md` にある。
`CfHtml.cs` と `FormatCatalog.cs` は、確認が済んだらそのまま本体の `Services` へ移す前提で書いてある。

1. ~~**日本語を含む CF_HTML が Word に貼れるか**（§4-3）~~
   **→ 組み立てと読み戻しは確認済み。文字列で渡す形に決定（§4-3）。**
   残っているのは Word へ実際に貼ったときの見た目だけ
2. ~~**Excel の範囲をコピーした状態で `GetFormats(false)` が何を返すか**~~
   **→ 測定済み（§6-9）。許可リストは実測を反映済み。**
   残っているのは Excel の `HTML Format` を Win32 で読めるかどうか
3. TSV と HTML を同時に載せたとき、Excel がどちらを選ぶか（§5-3）。
   HTML が勝つなら表の設計を変える必要がある
4. 貼り付け先ごとに期待どおりの形式が選ばれるか。
   最低でも メモ帳 / Word / Excel / ブラウザのテキストエリア の 4 つ
5. **コピー元アプリを終了させてから捕獲したときの挙動**（§6-4）。
   `null` が返るのか例外なのかで、失敗の扱い方が変わる
6. 大きな範囲（1000 行程度）を捕獲したとき、どれくらい時間がかかるか。
   数秒かかるなら UI の作りを変える必要がある
7. パスワード管理ソフトからコピーした状態で、
   `ExcludeClipboardContentFromMonitorProcessing` が実際に載るか（§6-7）
8. `SetDataObject(data, copy: true)` の後にアプリを終了して、貼り付けが生きているか
9. Word の表を捕獲 → 復元 → Word に貼って、罫線と書式が戻るか（往復の確認）
10. 古い設定ファイル（新しい項目の記述が無い JSON）を読んで、挙動が一切変わらないこと

---

## 10. 判断

- **両方やる価値はある。** 出力側と入力側は最終的に同じ `DataObject` 生成に合流するので、
  片方だけ作るのに比べて追加コストは思ったより小さい。
  逆に片方ずつ作ると `ClipboardService` を二度作り直す
- **面白さの中身は §1-1（スナップショットの昇格）にある。** 罫線付きの表を一度コピーするだけで、
  日付だけ差し替わる再利用可能な項目になる。これは片方だけでは作れない
- **難所は 2 つに集中している。** 出力側は CF_HTML のヘッダ（§4、日本語で必ず踏む）、
  入力側は遅延レンダリング（§6-4、コピー元が生きているうちに読む）。
  **どちらも小さな確認コードで先に潰せる**
- **既存の挙動は壊れない。** 追加するプロパティの既定値が全部「従来どおり」に一致する
- **常時監視はしない。** 既存の「必要なときだけクリップボードを開く」という判断と矛盾し、
  全形式の読み出しは重い。捕獲は手動の入口にする
- **プライバシーの除外形式（§6-7）は最初から入れる。** あとから足す種類のものではない

### 進め方の案

```
出力側                              入力側
--------------------------------    --------------------------------
1. BuildCfHtml() で日本語を Word へ  A. GetFormats(false) の一覧を出すだけ   [済]
   貼れるか試す               [済]      ← 実測は §6-9
2. ClipboardService を DataObject へ
   （Plain の挙動は変えない）  [済]
3. ClipItem.Format を足す      [済]
                                    B. 許可リストと除外形式の判定
                                    C. snapshots\ への保存と復元
                                    D. メニューの入口と設定画面の一覧
4. Markdig を入れて Markdown   [済]
6. 設定画面の形式選択          [済]
5. Table（TSV + <table>）
   と、その入力欄の AcceptsTab
7. RTF の生成（Word 対策）
                                    E. Biff12 を足すか判断  [済・入れる]

                    ↓ 両方が揃ってから
                F. スナップショット → テンプレートの昇格
```

**1 と A を先に、独立して試す。** どちらも小さく、しかもここで方針が変わりうる。
`3` まで来ると「HTML を直接書ける項目」として一度成立するので、そこで区切って使ってみられる。
`F` は両方が揃わないと作れないので最後。

#### 実際に進めた順（記録）

`6`（設定画面）は `4`（Markdown）より後にした。
先に Markdown を入れて `ClipFormat` を出揃わせてから設定画面を作れば、
選択肢を一度で作れて XAML を二度触らずに済むため。

`5`（Table）は `6` と対で考えていた（タブ入力のために `AcceptsTab` が要る）が、
`5` を後回しにしたので `AcceptsTab` も一緒に後回しにした。
形式の選択自体は `Table` が無くても成立する。

**現在地: 出力側は `5` と `7` が残り。入力側は `B` から手つかず。**

### 削るとしたらどこから

方針は「可能な限り」だが、重いと感じたときのために順序を残しておく。
**下から削る。**

```
E. Excel 専用形式        ← 効果が読めないわりに大きい
7. RTF の生成            ← HTML でだいたい足りる。日本語エスケープが面倒
F. テンプレートへの昇格  ← 面白いが、無くても両方の機能は成立する
5. Table                 ← Markdown の表で代用できる（Markdig を入れるなら）
4. Markdig               ← Html 形式を手書きすれば同じことはできる
--- ここから下は削らない ---
C/D. 捕獲と復元          ← 入力側の本体
1/2/3. CF_HTML と DataObject ← 出力側の本体。全部の土台
B. 除外形式の判定        ← 安全に関わる。機能を削っても、これは削らない
```
