# 作業ツールとアクションメニューの設計

## 1. 目的

空行の除外、JSON Minify・整形、連続コピーのような組み込み処理が増えても、
ユーザーが登録したコピー項目の場所と数字キー操作を変えずに拡張できるようにする。

組み込み処理をトップレベルへ直接追加し続けない。
通常時は「作業ツール」へまとめ、実行中の作業モードだけをメニュー先頭へ昇格する。

## 2. メニューの固定領域

上から次の順序にする。存在しない領域は表示しない。

1. 実行中の作業モード
2. この内容でできること
3. ユーザーが登録したコピー項目
4. 作業ツール
5. クリップボードから追加・設定・終了

ユーザー項目の `1`〜`9`・`0` は従来どおり維持する。
組み込みアクションには英字のアクセスキーを割り当て、数字を消費しない。

## 3. アクションの種類

`TrayActionKind` は次の 3 種類を持つ。

| 種類 | 配置 | 用途 |
| --- | --- | --- |
| `OneShot` | 作業ツール | 1 回で完了する変換。空行除外など |
| `Session` | 通常時は作業ツール、実行中は先頭 | 連続コピーなど |
| `Contextual` | この内容でできること | JSON のときだけ出す JSON Minify など |

`Contextual` は `Evaluate` で現在のクリップボードを調べ、対象外なら `Hidden` を返す。
常に場所を知らせたい機能は `OneShot` として作業ツールへ置き、前提不足時は `Disabled` と理由を返す。

## 4. 構成要素

### TrayActionDefinition / TrayActionRegistry

組み込みアクションは次の情報を宣言する。

- 安定した ID
- 表示名とツールチップ
- グループと表示順
- アクセスキー
- `OneShot` / `Session` / `Contextual`
- 初期状態でメニューに表示するか (`DefaultEnabled`)
- 別セッション中にも実行できるか
- 現在の状況を判定する `Evaluate`
- 実行処理 `Execute`

レジストリは ID の重複を拒否する。
1 件の判定に失敗してもトレイメニュー全体は開けるよう、そのアクションだけを無効にする。

### TrayMenuComposer

レジストリを現在の `TrayActionContext` で評価し、
「この内容でできること」と「作業ツール」へ振り分ける。
WinForms の `ToolStripMenuItem` は作らず、表示判断と描画を分離する。

### ActionSessionManager

クリップボード監視やキー監視を伴う一時セッションを同時に 1 つだけ保持する。
現在の ID・表示名・`IDisposable` を管理し、完了とキャンセルを区別する。

別の作業を選んでも、実行中のセッションを暗黙にキャンセルしない。
競合する項目を無効にし、先頭のキャンセル操作から明示的に終了してもらう。

現在は次の 2 つが共通管理へ参加する。

- `sequential-copy-paste`: 連続コピー＆ペースト
- `multiple-input`: `{input:名前}` の複数入力

### TrayIconManager

アクションを登録し、コンポーザーの結果を WinForms メニューとして描画する。
実行中セッションの進捗と固有コマンドも先頭へ描画する。

## 5. 表示設定

設定画面の「アクションの表示」では、組み込みアクションを実際の表示先ごとに分け、
アクションごとに表示・非表示を切り替えられる。
状態は `AppSettings.ActionStates` に安定 ID と真偽値の組として保存する。

- ID が設定ファイルにない場合は、アクション定義の `DefaultEnabled` を使う
- 非表示にしたアクションは「作業ツール」と「この内容でできること」の両方から除外する
- 実行中のセッションを非表示に切り替えても、その作業は暗黙に終了しない。完了またはキャンセルまでは先頭に残す
- 将来のバージョンで追加された未知の ID を、設定の保存時に消さない

## 6. 新しいアクションの追加手順

1. 変換処理をクリップボード UI から独立した関数として実装する
2. `CreateActionRegistry` に `TrayActionDefinition` を 1 件登録する
3. `DefaultEnabled` で初期表示を決める
4. 入力条件を `Evaluate` へ書く
5. 実行時のコピーと通知を `Execute` から呼ぶ
6. ビルドに加えて、対象外・空・巨大入力・クリップボードロックを確認する

例:

- 空行除外: `OneShot`、グループ `text-transform`。作業ツールへ常設し、対象外なら理由付きで無効化
- JSON Minify・整形: `Contextual`、グループ `json-transform`。JSON のときだけ表示
- 複数回操作する処理: `Session` とし、`ActionSessionManager` へ登録してから開始する

## 7. 増加時の階層化

作業ツールが 8 件程度までは、グループ間を区切り線で分ける。
それ以上になった段階で、既存の `Group` を使い「連続作業」「テキスト加工」「データ変換」などの
サブメニューへ切り替える。最初から深い階層にはしない。
