# 現在地と、ここからの展望

対象: リポジトリ全体。更新日: 2026-08-27。基準コミット: `428460a`。
各設計メモは 1 つの機能を掘るが、このファイルは**横断の一覧**として置く。
出典は `DESIGN_*.md`、実コード、現在のテスト結果。

| 指標 | 値 |
| --- | --- |
| 設計資料 | 設計メモ 10 本（計 4,964 行） |
| 実機確認欄が未消化の設計メモ | **9 本**（未実装機能の将来確認も含む） |
| 自動テスト | **40 件、全件成功**（失敗 0 / スキップ 0） |
| 過去のバグ調査 | 2 回・計 33 件、すべて修正済み（詳細は Git 履歴に保持） |
| コード内の TODO / FIXME | 0（判断は設計メモ側に記録） |
| 最大の C# ファイル | `TrayIconManager.cs` 1,860 行 |

---

## 1. いちばんの負債は、Windows 実機確認が残っていること

ビルドと自動テストを実行できない、という過去の前提は解消した。
現在は `net10.0-windows` のビルドが成立し、ロジック層の自動テスト 40 件も通っている。

一方、自動テストでは次を裏取りできない。

- トレイメニューや WPF ウィンドウのフォーカス
- 低レベルキーボード・マウスフックの体感とタイミング
- Word / Excel が複数のクリップボード形式から何を選ぶか
- 実際の操作経路を通した通知、メニュー表示、貼り付けの感触

したがって、現在の検証状況は**「ロジックの回帰テストはあるが、OS・アプリ統合の実機確認が残る」**である。
設計時の環境制約は現在の状態説明から除き、実機確認だけを未完了として扱う。

### 1-1. 通らなければ設計に戻る 3 つ（`DESIGN_COPY_INTENT.md` §9）

収集条件を「Ctrl+C のみ」から「操作した直後」へ広げた判断が、ここで裏取りされる。

| # | 内容 | 外れたときに起きること |
| --- | --- | --- |
| §9-2 | Web ページのコピーボタンが 2 秒の窓に間に合うか | 収集条件の拡張そのものが成立しない |
| §9-6 | マウスフックを張ったまま操作して重くならないか | 低レベルフックは全アプリの入力経路に乗る。体感の遅さが出たら設計に戻る |
| §9-3 | 収集中にトレイから定型文をコピーして混ざらないか | 収集データが黙って壊れる |

### 1-2. 複数行への一括適用

ロジック層では、次を自動テストで確認済み。

- 正規表現へ `Multiline` を付け、`^…$` を行単位で扱う
- `\r\n` を `\n` に正規化する
- 非正規表現の条件を空でない行ごとに照合する
- 上限件数で打ち切り、切り捨てを結果へ残す
- `ApplyToEachLine` の変更で設定が未保存状態になる

残るのは、トレイメニューと設定画面を通した実機確認である。とくに、
**1 行用に書いた `^…$` の項目にチェックを入れてもメニューから消えないこと**を最初に確かめる。
続いて通知件数、「コピーして試す」、複数行バッジ、1 行時の従来動作、200 行時の体感を確認する。

`DESIGN_BULK_APPLY.md` §実機で確認したいこと のうち、
「1 件ずつ貼り付け」とサブメニューに関する項目は 4-b の実装後に回す。

### 1-3. 残りの実機確認

各メモは「上から順に、確認できないと先へ進めない順」で並べている。
筆頭項目は次のとおり。

| 設計メモ | 節 | 筆頭の確認項目 |
| --- | --- | --- |
| `DESIGN_APP_CONTEXT.md` | §7 | トレイ**右クリック**経路で取った前面ウィンドウが、本当に利用者のアプリか（`ShowTrayMenu()` を通らない） |
| `DESIGN_HOTKEY.md` | §5 | メニューを出した直後に矢印キーと Enter が効くか。効かなければ案の価値が半減する |
| `DESIGN_CHOICE.md` | §9 | 2 枚目の選択メニューでキーボードが効くか。外れると案 C の前提が崩れる |
| `DESIGN_QUICK_ADD.md` | §6 | メニューを閉じた直後に出した WPF 窓が、確実に前面へ来てキー入力を受けるか |
| `DESIGN_RICH_COPY.md` | §9 | Word へ実際に貼ったときの見た目と、TSV と HTML を同時に載せたとき Excel がどちらを選ぶか |
| `DESIGN_BASE_SYNTAX.md` | 末尾 | `{date@clip:ddd}` の曜日が地域設定どおりに出るか |

> **RICH_COPY には確認用の道具がある。** `tools/ClipboardProbe/` を `dotnet run` して
> メニューから選ぶだけで §9 の項目を試せる。
> 本体に手を入れずに動かせるため、早い段階で結果を確定させる。

### 1-4. 解決済みバグ票から残した実機確認

解決済みのバグ報告書は現行ツリーから削除し、修正内容は Git 履歴へ任せる。
未確認だった次の 3 点だけは、現在の確認項目として残す。

- DPI が異なる複数モニター間で、初回のトースト位置がずれないか
- サブメニュー項目のツールチップが表示されるか
- `.NET 10` でも `NotifyIcon.ShowContextMenu` のリフレクション経路が使え、フォールバック時も表示位置が不自然でないか

---

## 2. 実装が残っているもの

優先順はおおむね決まっているが、すべてが「あとは書くだけ」ではない。
そのまま着手できるものと、実機確認または実装境界の整理が要るものを分ける。

| 項目 | 現在地 | 次に決めること |
| --- | --- | --- |
| `{default:値\|代替}` | **着手可能** | `{if:}` より先に実装し、空・非空・入れ子をテストする |
| 4-b「1 件ずつ貼り付け」 | 挙動の設計は済み | 複数件サブメニュー、展開済み値の投入、作業モードとの統合を含めて実装単位を確定する |
| Table 形式（TSV + `<table>`）と入力欄の `AcceptsTab` | **着手可能** | `ClipFormat` の追加と設定画面を同時に入れる |
| RTF の生成（Word 対策） | Table の次 | 生成後に Word 実機確認を行う |
| クリップボードのスナップショット | **実機確認待ち・大規模** | Excel の形式を `ClipboardProbe` で確かめ、入力側 B〜D を実装する |
| `{time@clip}` | 未設計部分あり | 時刻の抽出規則と、読めない場合の扱いを決める |

### 2-1. `{default:}` を `{if:}` より先に入れる

`{default:}` は単独で完結し、`{if:}` を作らなくても価値が残る。
逆に `{if:}` を先に入れると `{default:}` は「`{if:}` の 3 引数形で書ける」ことになり、
**記法が 2 つに割れる。** この順序は維持する。

### 2-2. 4-b は `StartPasting()` を足すだけではない

`SequentialCopyPasteSession` には、収集済み項目を貼り付けへ移す `TryBeginPasting()` があるが、
展開済みの複数件を外から投入して貼り付けフェーズだけを始める入口は無い。

必要なのは少なくとも次の一式。

- 複数件のときだけ「まとめてコピー」「1 件ずつ貼り付け」を出すサブメニュー
- 展開済み文字列をセッションへ渡して貼り付けフェーズから始める API
- 既存の進捗通知、タイムアウト、競合作業の無効化との統合
- 1 件だけなら従来どおり即コピーする分岐

既存セッションの再利用方針は正しいが、メソッド 1 本だけの変更とは見積もらない。

### 2-3. スナップショットは出力側と入力側の土台が揃ってから

`DESIGN_RICH_COPY.md` の依存図どおり、出力側 5（Table）・7（RTF）と、
入力側 B〜D（E の Biff12 採用判断は済み）を揃えてから、
F「スナップショット → テンプレートへの昇格」を作る。

入力側の読み取り方式は Excel での `ClipboardProbe` の結果によって変わるため、
スナップショットを「着手可能」には置かない。

---

## 3. 意識して止めてあるもの

需要が具体化するまで動かさない、と理由つきで決めた分。
思い出す必要はない——困る場面が出たときに、メモが待っている。

| 項目 | 止めた理由 |
| --- | --- |
| `{clip:正規表現}` と `@clip` の合成 | 外側の波かっこ走査は手書き化済みだが、`InnerRegex` は基準を単純な識別子として読む。引数つき・入れ子の基準を扱うには、内部構文の専用パーサと文法設計が要る |
| 日付リテラル `@2026-04-06` | 起点が動かないなら結果も動かない |
| `@lastcopy` | 永続化が連番と同じ重さになり、同じ苦労を別の値で作り直すことになる |
| `@ask` | 「クリックしたら即コピー」という前提を壊す |
| `{if:条件\|真\|偽}` 本体 | 文字列の内容から意味を推測する判定は当たらない。`{choice:}` のほうが誤判定が起きないぶん優れている |
| 複数行で件ごとに違う変換 | `{if:}` の結論待ち。そもそも項目を 2 つに分ければ書ける |

---

## 4. 構造と検証基盤の現在地

### 4-1. 主要クラスの責務分割は完了

`428460a` で、主要クラスを partial class により責務別へ分割した。

| 対象 | 現在地 |
| --- | --- |
| `TrayIconManager` | 本体 1,860 行。Choices / Copying / Sessions を分離 |
| `TemplateEngine` | 本体 1,631 行。Choices / Inputs を分離 |
| `SettingsViewModel` | 本体 820 行。MenuOutline / Preview / State を分離 |
| `SettingsWindow` | Commands / Editors / Items を分離 |

ファイル行数だけを理由に、すぐ次の分割へ進む必要はない。
今後、1 つの変更が再び複数の責務へ横断的に波及するようなら、その境界を見直す。

### 4-2. 設計メモの判断が、静かに古びる

設計メモ 10 本で計 4,964 行ある。
過去の環境制約や行数、実装前提は、その後の変更で正しくなくなる。

- 横断の現在地はこの `ROADMAP.md` で更新する
- 各メモの「見送り」は §3 に集約する
- 設計メモ中の「SDK が無い」「未ビルド」は履歴として扱い、現在の検証状況には使わない
- 大きな実装・検証・リファクタリングの完了時に、指標と「次の一手」を更新する

### 4-3. 自動テストの基準線はできた

現在の 40 テストは、次を対象にしている。

- `ClipboardMatcher`
- `TemplateEngine`
- `ExpressionEvaluator`
- `SettingsStructure`
- `SettingsViewModel` の未保存状態追跡

今後は「テストプロジェクトを作る」こと自体を作業項目にはしない。
機能追加や不具合修正ごとに、その挙動の回帰テストを既存プロジェクトへ追加する。
WPF、トレイ、低レベルフック、外部アプリとのクリップボード連携は、引き続き実機確認で補う。

---

## 5. 次の一手

現時点の推奨順は次のとおり。

```
1. 複数行への一括適用を実機で確認する
     └ とくに ^…$ の既存項目がメニューから消えないこと
     └ 通知件数、コピーして試す、バッジ、1 行時の従来動作も確認する

2. DESIGN_COPY_INTENT.md §9 の危険な 3 つを回収する
     └ 通らなければ設計に戻る項目を先に閉じる

3. 残りの実機確認を機能ごとに消す
     └ RICH_COPY §9（ClipboardProbe）→ CHOICE §9 → APP_CONTEXT §7
     └ 続いて HOTKEY、QUICK_ADD、BASE_SYNTAX

4. 小さく独立した機能から再開する
     └ {default:} を実装し、回帰テストを追加する
     └ {time@clip} は抽出規則を決めてから着手する

5. 4-b「1 件ずつ貼り付け」を実装する
     └ API だけでなく、サブメニューと作業モード統合までを 1 単位にする

6. 書式付きコピーを段階的に進める
     └ Table 形式 → RTF → スナップショット入力側 B〜D → テンプレートへの昇格 F
```

自動テストの基準線は既にある。
ここからの主張は、**OS・外部アプリとの境界を実機確認で閉じながら、
小さな機能から回帰テストつきで再開すること**である。
