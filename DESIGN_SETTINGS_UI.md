# 設定画面：項目の追加・整理 UI 改善案

設計日：2026-09-11。提案段階。アプリ本体の変更は含まない。

## 1. 方針

既存の「メニュー構成を左、選択対象の編集を右に表示する」構成を維持する。
操作位置を安定させ、名前・本文の編集に十分な面積を確保する。
カテゴリは現在の1階層のまま扱い、多階層化やタグ機能は今回追加しない。

## 2. 現状から改善する点

SettingsWindow.xaml、SettingsWindow.Items.cs、SettingsViewModel、SettingsStructure の実装を確認した。以下はコードから読み取った課題であり、実画面での操作検証結果ではない。

| 現状 | 改善 |
| --- | --- |
| 左下の同じ Grid に「追加・区切り線・複製・削除」と右寄せの上下移動を配置。左ペインは最小250 DIP | 上部に「＋ 追加」、下部に移動操作、編集ヘッダーに「操作」を分散し、狭い幅で競合させない |
| カテゴリ名の入力からカテゴリを作る。追加ボタンは項目と区切り線のみ | 「＋ 追加」に項目・カテゴリ・区切り線をまとめる |
| カテゴリ配下でもカテゴリバッジを表示し、複数の機能バッジが横に並ぶ | 階層で分かるカテゴリ名は省略。機能バッジは最大2個と残数、詳細は編集側に表示 |
| 表示条件の Expander が本文の前にあり、条件付き項目の選択時に展開される | 編集領域を「内容」「表示条件」の2タブに分け、本文領域を確保 |
| 下部に通知・ホットキー・各種設定・保存操作が集まる | 上部を「メニュー構成」「アプリ設定」に分け、下部は変更状態と保存・キャンセルに限定 |
| 検索中のドラッグ移動を処理側で禁止 | 操作を無効にし「検索中は並べ替えできません」を表示 |

## 3. 画面配置

- 初期サイズは1040×800 DIPを目安とし、現状の最小840×560 DIPでも操作できるよう設計する。サイズは実装時の表示確認で調整する。
- 左ペイン320 DIP、最小260 DIP。右ペインは残り幅を使う。GridSplitterで幅変更を維持する。
- 左上：見出しと「＋ 追加」、次の行に検索。左下：上へ・下へ・元に戻す。ボタンを重ねず、幅不足時は折り返す。
- 左一覧：「通常メニュー」「この内容でできること」、カテゴリ、項目の順。既存の意味と実際の並び順を維持する。
- 右上：配置先のパンくず、項目名、「操作 ▾」（複製・削除）。カテゴリ選択時は専用編集に切り替える。
- 「内容」：表示名、カテゴリ選択、本文。形式選択と差し込み挿入は本文の直前。連番設定は連番を使う場合だけ表示する。
- 本文の下に折りたためるプレビュー。現在の「コピーして試す」を維持し、プレビューとの違いが分かる表示にする。
- 「表示条件」：クリップボード条件、正規表現、複数行適用、アプリ名、ウィンドウタイトル。設定がある場合はタブに「設定あり」と表示する。
- アプリ設定：通知、ホットキー、アクションの表示、連続コピー、スプリント、設定フォルダーへの入口を縦に配置。
- フッター：「未保存の変更があります」「キャンセル」「保存して閉じる」。項目選択やタブ移動で入力を失わない。
- WPF本体では一覧と編集欄をそれぞれ独立してスクロール可能にし、ヘッダー・フッターは固定する。

## 4. 追加の仕様

「＋ 追加」はドロップダウン形式とし、項目・カテゴリ・区切り線の3種類を表示する。

| 選択位置 | 項目の追加位置 |
| --- | --- |
| 通常の項目 | 同じカテゴリ・領域で選択項目の直後 |
| カテゴリ | そのカテゴリの末尾 |
| 領域見出し | その領域のトップレベル末尾 |
| 選択なし | 通常メニューのトップレベル末尾 |

- 新規項目は即座に選択し、表示名にフォーカスして初期名を全選択する。本文入力へTabで移れるようにする。
- 条件付き領域への追加時は既存と同様に「文字列があるとき」を初期条件とする。配置先と条件を画面に明示する。
- 検索中の追加は検索を解除して新規項目を表示し、「追加した項目を表示するため検索を解除しました」と知らせる。
- 区切り線は通常メニューのみで追加可能。条件付き領域では追加メニューを無効にし、理由を添える。現在のサービスでは区切り線は通常項目として分類されるため、UIでもこの制約を明示する。
- カテゴリを追加したら名前にフォーカスする。右側に「このカテゴリに項目を追加」を表示する。
- 空カテゴリは編集中に表示できる必要がある。初期実装では編集セッション内の下書きとして保持し、空のまま保存するとメニューに表示されない旨を明示する。永続的な空カテゴリの配置保存は別途対応する。
- 名前空欄や既存名との重複は欄の直下に説明し、意図せず別カテゴリに統合しない。

## 5. 整理の仕様

- ドラッグ中は、行の間なら挿入線、カテゴリ上ならカテゴリ全体の強調と「仕事の末尾へ移動」などの文言を表示する。
- 同じ領域のカテゴリ間移動とトップレベルへの移動を可能にする。右側のカテゴリ選択でも同じ操作を行える。
- 上下移動はカテゴリ内、またはトップレベルの並び順を変更する。カテゴリ自体の移動は中の項目をまとめて動かす。境界では個別に無効化する。
- 「通常メニュー」と「この内容でできること」をまたぐドラッグは不可とする。表示条件の変更で移動し、移動先を通知する。単なる整理操作で実行条件を変更しない。
- 検索は名前・本文・カテゴリを対象とし、該当項目の祖先見出しを残す。カテゴリ名が一致した場合はそのカテゴリの項目を表示する。
- 検索中はドラッグと上下移動を無効にする。検索解除時は元の折りたたみ状態を復元する。
- 検索結果なし、項目が0件、カテゴリ内が0件を区別して表示する。名前未入力は一覧で「名前未入力」と表記する。
- 初期段階は単一選択を維持する。複数選択・一括移動は後続改善とする。

## 6. 削除・元に戻す・保存

- 初期実装では既存の削除確認を維持する。Undo対応後は単一項目の削除を即時実行し、フッターに「削除しました／元に戻す」を表示する。
- カテゴリ削除は影響が大きいため「カテゴリだけ削除して項目をトップレベルへ」「中のN件も削除」「キャンセル」を明示する。既定は項目を残す。
- Undo/Redoの対象：追加、複製、削除、移動、カテゴリ変更、名称・本文・条件・装飾の編集。文字入力は一連の入力をまとめて1操作とし、1文字ずつ戻さない。
- テキスト欄内のCtrl+Zはその欄の編集Undoを優先し、構造変更のUndoと二重に処理しない。一覧やツールバーにフォーカスがある場合は構造操作を戻す。
- 保存は既存の設定生成・正規表現等の検証を通す。不正な項目を選択して該当タブを開き、欄の直下にエラーを表示する。
- 保存失敗では画面を閉じず、未保存状態と入力内容を保持する。
- キャンセル／ウィンドウを閉じる操作は未保存時だけ確認する。プレビューは連番を進めず、試しコピーは既存の動作を保持する。

## 7. 見た目・キーボード

- Yu Gothic UI、既存のライト／ダークテーマとWindowsアクセント色を利用する。
- 基本文字14 DIP、補足12 DIPを目安とする。項目行は48〜56 DIP、カテゴリ行は36 DIP程度。項目名を主、本文1行を従として表示する。
- 色は選択状態とカテゴリ装飾を中心に使い、色だけで条件の有無を表さない。
- 長い名前は省略し、選択時は編集側で全文確認できる。長い本文・多数のバッジでも操作ボタンを押し出さない。
- 既存のCtrl+N、Ctrl+D、Ctrl+F、Delete、Alt+↑/↓を維持する。Deleteはテキスト編集中に項目削除を起動させない。
- アイコン操作には説明とAutomationProperties.Nameを付け、フォーカス表示を維持する。

## 8. 実装順序と確認条件

### 第1段階：配置の整理

SettingsWindow.xamlとテーマを中心に、追加メニュー・編集タブ・アプリ設定タブ・フッター・一覧表示を変更する。現行の追加・複製・移動・保存処理を再利用する。

確認：840×560 DIP、1040×800 DIP、125%/150%拡大表示でボタン重複や本文欄消失がない。タブ移動・項目選択後も未保存入力を保持する。既存条件と書式付きコピー設定を編集できる。

### 第2段階：追加先と移動を分かりやすくする

カテゴリ作成用の下書き状態、配置先の選択、検索の状態表示、ドラッグ先の強調を追加する。SettingsViewModel.MenuOutline.csとSettingsWindow.Items.csで構造操作を一元化する。

確認：カテゴリ内追加・直後への複製・カテゴリ単位移動・トップレベルへの移動・検索中の操作制限。保存後のトレイ順序が編集画面と一致する。通常／条件付きの混在カテゴリを維持する。

### 第3段階：操作の復元

編集セッション単位のUndo/Redoを実装し、変更追跡と連携する。破壊的操作の復元はID・所属・順序・条件・装飾を含めて検証する。全変更を戻すと未保存表示も解除する。

既存のSettingsStructureCharacterizationTestsとSettingsViewModelDirtyTrackingTestsを軸に、変更した構造操作・復元・保存互換性のテストを追加する。見た目だけの変更に実装をなぞるテストは追加しない。

## 9. 画面案の範囲

会話内の画面案はサンプルデータで、選択、検索、追加、複製、単一項目の上下移動、カテゴリ変更、削除と取り消し、本文編集、条件による領域移動を試せる。
ドラッグ操作、既存の全条件・全書式、カテゴリ削除確認、ファイル保存は画面案では省略している。アプリ設定タブは配置の見本。実際の設定への反映やアプリ本体の実装は行っていない。
