# クリップボードをその場で項目登録 検討メモ

> **実装済み。** 本メモの方針どおり実装した（入口は 1-1、エスケープは §2、名前は §3、
> 追加位置は §4、設定画面との競合は §5 の案 A）。`QuickAddWindow.xaml(.cs)` が名前を尋ねる窓。
> §6 の実機確認は未実施。

「これ、毎回打っているな」と気付いた瞬間に、いま手元にある文字列を項目として登録できるようにする案の検討。

## 0. 何が問題か

いまの登録手順は 5 手ある。

```
メニュー → 設定... → 追加 → コピー文字列に貼る → 名前を付ける → 保存
```

しかも設定画面はモードレスとはいえ、開いて閉じるまでのあいだ作業は止まる。
**「登録するほどでもないか」と思わせる摩擦がここにある。**

項目が育たないと、このアプリの価値そのものが育たない。
差し込みも、スマートアクションも、項目があって初めて意味を持つ。
だからここは機能の追加というより、**既存の全機能への入口を広げる**話になる。

目指す手数:

```
メニュー → クリップボードを項目に追加 → 名前を打って Enter
```

---

## 1. 入口をどこに置くか

### 1-1. 通常メニューの下部に置く

`設定(&S)...` の隣。位置としてはここが素直。

```
（項目が並ぶ）
────────────
クリップボードを項目に追加(&A)...
設定(&S)...
────────────
終了(&X)
```

**懸念は 1 つ。** ホットキー経路では番号キーで選ぶのが主なので、
下部の固定項目が増えても 2 打鍵の速さは損なわれない（番号は `BuildClipItems` が足したものにしか振られない）。
つまり実害は「メニューが 1 行伸びる」だけ。許容できる。

### 1-2. 「この内容でできること」に混ぜる案

スマートアクションの先頭に「この内容を項目にする」を出す案。
文脈としては最も自然だが、**スマートアクションが 1 件も無いときはサブメニューごと出ない**ため、
登録の入口が消える。常設にしたいものをここへ置くのは筋が悪い。

**1-1 を採る。**

### 1-3. 出す条件

- クリップボードが空、または文字列でないときは **`Enabled = false`**。消さずに残す。
  「無いから探した」を起こさないため。ツールチップで理由を出す
- `_settings.IsFallback`（設定を読めていない）のときも `Enabled = false`。
  この状態で追加して保存すると、読めなかった元の設定を既定値＋1 件で上書きしてしまう。
  `ShowSettingsWindow()` が同じ理由で画面を開かない判断をしているので、それに揃える

### 1-4. クリップボードを読む回数

`PopulateMenu()` は既に `CreateClipboardReader()` で
「必要になったとき 1 回だけ読む」仕組みを持っている。**有効・無効の判定はそれに乗せる。**
この項目のためだけに毎回クリップボードを開いてはいけない
（他アプリのコピー操作を妨げる、というのが既存の判断）。

---

## 2. 一番の落とし穴: 中括弧

**コピーした文字列に `{` や `}` が含まれていると、差し込みとして解釈される。**

```
コピー: { "id": 4329 }
そのまま Text に入れると → {…} が差し込みとして評価される
```

JSON、プログラムのコード、`{clip}` と書いた説明文――どれも普通にありうる。
そして厄介なことに、**解釈できない差し込みは書いたまま残る**という既存の親切設計のせいで、
「たまたま解釈できてしまった場合」だけが壊れる。気付きにくい壊れ方をする。

### 対処: 保存時に `{` → `{{`、`}` → `}}` にエスケープする

README に「`{{` と `}}` はそれぞれ `{` `}` そのものを表す」と既に書いてある。
その仕組みをそのまま使えばよく、新しい規則は要らない。

- **既定はエスケープする。** 登録したものが 1 文字も変わらずコピーされるほうが、予想を裏切らない
- 差し込みとして使いたい場合は、設定画面で `{{` を `{` に直せばよい。
  この操作をする人は差し込みを理解している人なので、それでよい
- エスケープが実際に発生した場合だけ、追加後のトーストで一言添える
  （「`{}` はそのままの文字として登録しました」）

登録ダイアログに「差し込みとして扱う」チェックボックスを置く案もあるが、
**大多数にとって意味が分からない選択肢**なので v1 では出さない。

### 前後の空白

`Trim()` するかどうか。

コピー操作は行末や改行を巻き込みやすいので、**前後の空白と改行は落とす**。
途中の改行は残す（複数行の定型文を登録したい需要のほうが大きい）。
これは `{clip}` の既定の扱い（前後の空白は除く）と同じで、一貫している。

---

## 3. 名前をどう決めるか

### 3-1. 名前は聞く

自動命名（先頭 40 文字を切る）だけで済ませる案もある。追加が 1 手で終わって速い。

だが**名前はメニューでの見え方そのもの**で、後で直しに設定画面を開くなら摩擦は消えていない。
`ClipItem.DisplayLabel` が「名前が空ならコピー文字列を切って表示」する作りなので
名前無しでも動くが、それは**メニューが読みにくくなる**ことを意味する。

**小さな入力窓を出して名前を聞く。** 初期値に先頭 1 行を切ったものを入れておき、
そのまま Enter でも成立させる。打ちたい人だけが打てばよい。

### 3-2. 窓の作り

WPF の小窓を 1 つ。

- カーソル位置の近くに出す（ホットキー経路と同じ考え方。視線がそこにある）
- 名前欄にフォーカスして全選択。**Enter で確定、Esc で中止**
- 登録される文字列を 1〜2 行だけプレビューする（何を登録しようとしているかの確認）
- カテゴリは**この窓では聞かない**。→ §4

`ShowMenuAtCursor()` の前面化まわりで既に踏んだ問題
（アプリが前面にならないとキー入力が届かない）が、この窓でも起きうる。
ただしこちらは通常の WPF `Window` なので `Activate()` で足りるはず。**実機で確認する**。

### 3-3. フォーカスの行方

メニューを閉じた直後に窓を出すため、`ShowMenuAtCursor()` の
「閉じたら元のウィンドウへフォーカスを戻す」処理と競合する。

既存コードは「閉じた時点で前面がまだ自分の見えない窓のときだけ戻す」という条件なので、
**先に入力窓を前面化していれば自動的に何もしない**。
順序（窓を出す → メニューが閉じる）に注意する。

---

## 4. どこに追加するか

### 4-1. 位置

**リストの末尾、カテゴリなし（トップレベル）。**

「一覧の並び順がそのままメニューの順序」という既存の考え方があるので、
勝手に良い位置を推測するより末尾が予想しやすい。整理は設定画面でやればよい。

ただし末尾に足すと**メニューの一番下、`設定...` の直前**に出る。
番号キーは 10 個までなので、項目が多い人には番号が振られない位置になる。
これは「登録した直後は目に付くところにあってほしい」という期待と少しずれる。

**先頭に足す案**もある。直後に使いやすく、番号 1 が振られる。
だが既存の並びを毎回押し下げるので、番号を覚えている人には迷惑になる。

→ **末尾を採る。** 追加後のトーストで「設定画面で並べ替えられます」と添える程度でよい。

### 4-2. カテゴリ

聞かない。トップレベルに置く。

「追加した項目」のような専用カテゴリに入れる案は、
**サブメニューに埋まって見えなくなる**ので採らない。

### 4-3. 重複

同じ `Text` の項目が既にある場合、黙って 2 つ目を作ると項目が汚れていく。
追加前に照合し、既にあれば**その項目の名前を出して知らせ、追加しない**。
エスケープ後の文字列どうしで比較する。

---

## 5. 設定画面が開いているときの競合

ここが実装上の唯一の面倒。

`ShowSettingsWindow()` は `_settings.Clone()` を渡している。
**設定画面は開いた時点の複製を持っている**ので、
トレイ側で項目を足してファイルに保存しても、設定画面が保存した瞬間に消える。

連番については同じ問題を `NotifySequenceAdvanced(id, value)` で解決している
（`Id` で突き合わせて画面側の値を追従させる）。前例があるので方針は決めやすい。

### 案 A: 設定画面へ足す

`_settingsWindow` が開いていれば、ファイルではなく**画面のリストへ足す**。
足した項目を選択状態にし、未保存の印を付ける。
利用者から見れば「追加ボタンを押して貼り付けた」のと同じ状態になり、筋が通っている。

### 案 B: できないと知らせる

`Enabled = false` にして「設定画面を閉じてから」と出す。実装は最小。
だが**設定画面を開きっぱなしにしている人ほど項目を育てたい人**で、その人だけ使えないのは倒錯している。

### 案 C: ファイルに足して、画面へ通知する

`NotifySequenceAdvanced` と同じ形。画面側に「トレイから項目が追加されました」と伝えて追従させる。
一貫はしているが、未保存の編集と追加が混ざった状態の扱いが難しい。

→ **案 A を採る。** 画面が開いていない場合はファイルへ直接（`TrySaveSettings` と同じ経路）。
分岐は 1 か所で済み、どちらの経路でも「追加された」結果は同じになる。

### 保存の失敗

連番の保存失敗は「コピー自体は成功しているので通知しない」という判断だが、
**項目の追加は保存できなければ何も起きていない**のと同じ。
失敗したらトーストで知らせる。ここは既存の方針をそのまま適用しない。

---

## 6. 実機で確認が必要なこと

1. メニューを閉じた直後に出した WPF 窓が、確実に前面へ来てキー入力を受けるか（§3-2）
2. その窓を閉じたあと、元のウィンドウへフォーカスが戻るか（§3-3）
3. 画像やファイルをコピーした状態でメニューを開いたとき、
   「追加」が無効になり、かつクリップボードを無駄に開いていないか（§1-3、§1-4）
4. 設定画面を開いたままトレイから追加したとき、画面のリストに現れ、保存で消えないか（§5）

---

## 7. 判断

- **これは新機能というより、既存機能への入口。** 実装量に対して効きが大きい
- 難所は 2 つ。**中括弧のエスケープ**（§2）と**設定画面との競合**（§5）。
  どちらも「黙って壊れる」種類なので、先に決めておく
- 名前を聞く小窓は作る価値がある。ただし Enter だけで抜けられること

### 進め方の案

```
1. メニュー項目と、名前を聞く小窓（末尾に追加してファイル保存するだけ）
2. 中括弧のエスケープと Trim、重複の検出
3. 設定画面が開いているときの経路（案 A）
4. 無効化の条件（空・非テキスト・IsFallback）とツールチップ
5. README への追記
```
